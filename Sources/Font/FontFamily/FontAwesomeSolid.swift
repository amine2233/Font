import CommonFont
import Foundation

/// FontAwesome for Solid
/// ```
/// [Doc](https://fontawesome.com/icons?d=gallery)
/// ```
public enum FontAwesomeSolid: String, CaseIterable {

    /// Icone name `Alternate Medical File` format `alternate_medical_file`
    case alternateMedicalFile = "alternate_medical_file"

    /// Icone name `exclamation` format `exclamation`
    case exclamation = "exclamation"

    /// Icone name `Compact Disc` format `compact_disc`
    case compactDisc = "compact_disc"

    /// Icone name `Syringe` format `syringe`
    case syringe = "syringe"

    /// Icone name `Area Chart` format `area_chart`
    case areaChart = "area_chart"

    /// Icone name `Hotel` format `hotel`
    case hotel = "hotel"

    /// Icone name `Folder Minus` format `folder_minus`
    case folderMinus = "folder_minus"

    /// Icone name `backward` format `backward`
    case backward = "backward"

    /// Icone name `Hushed Face` format `hushed_face`
    case hushedFace = "hushed_face"

    /// Icone name `fast-backward` format `fast-backward`
    case fastBackward = "fast-backward"

    /// Icone name `Spock (Hand)` format `spock_(hand)`
    case spockHand = "spock_(hand)"

    /// Icone name `Grinning Face With Sweat` format `grinning_face_with_sweat`
    case grinningFaceWithSweat = "grinning_face_with_sweat"

    /// Icone name `List` format `list`
    case list = "list"

    /// Icone name `Alternate External Link` format `alternate_external_link`
    case alternateExternalLink = "alternate_external_link"

    /// Icone name `microphone` format `microphone`
    case microphone = "microphone"

    /// Icone name `Glass Whiskey` format `glass_whiskey`
    case glassWhiskey = "glass_whiskey"

    /// Icone name `Eye` format `eye`
    case eye = "eye"

    /// Icone name `Star of Life` format `star_of_life`
    case starOfLife = "star_of_life"

    /// Icone name `Funnel Dollar` format `funnel_dollar`
    case funnelDollar = "funnel_dollar"

    /// Icone name `Stroopwafel` format `stroopwafel`
    case stroopwafel = "stroopwafel"

    /// Icone name `Paperclip` format `paperclip`
    case paperclip = "paperclip"

    /// Icone name `home` format `home`
    case home = "home"

    /// Icone name `Alternate Tachometer` format `alternate_tachometer`
    case alternateTachometer = "alternate_tachometer"

    /// Icone name `Genderless` format `genderless`
    case genderless = "genderless"

    /// Icone name `Laptop Code` format `laptop_code`
    case laptopCode = "laptop_code"

    /// Icone name `High Temperature` format `high_temperature`
    case highTemperature = "high_temperature"

    /// Icone name `Business Time` format `business_time`
    case businessTime = "business_time"

    /// Icone name `bookmark` format `bookmark`
    case bookmark = "bookmark"

    /// Icone name `Dumpster Fire` format `dumpster_fire`
    case dumpsterFire = "dumpster_fire"

    /// Icone name `Yin Yang` format `yin_yang`
    case yinYang = "yin_yang"

    /// Icone name `Bezier Curve` format `bezier_curve`
    case bezierCurve = "bezier_curve"

    /// Icone name `pause` format `pause`
    case pause = "pause"

    /// Icone name `Peace (Hand)` format `peace_(hand)`
    case peaceHand = "peace_(hand)"

    /// Icone name `Smiling Face` format `smiling_face`
    case smilingFace = "smiling_face"

    /// Icone name `Globe with Asia shown` format `globe_with_asia_shown`
    case globeWithAsiaShown = "globe_with_asia_shown"

    /// Icone name `Shopping Basket` format `shopping_basket`
    case shoppingBasket = "shopping_basket"

    /// Icone name `Search Minus` format `search_minus`
    case searchMinus = "search_minus"

    /// Icone name `Alternate Undo` format `alternate_undo`
    case alternateUndo = "alternate_undo"

    /// Icone name `Birthday Cake` format `birthday_cake`
    case birthdayCake = "birthday_cake"

    /// Icone name `Truck Monster` format `truck_monster`
    case truckMonster = "truck_monster"

    /// Icone name `unlock` format `unlock`
    case unlock = "unlock"

    /// Icone name `Cloud with Heavy Showers` format `cloud_with_heavy_showers`
    case cloudWithHeavyShowers = "cloud_with_heavy_showers"

    /// Icone name `User Friends` format `user_friends`
    case userFriends = "user_friends"

    /// Icone name `Calendar` format `calendar`
    case calendar = "calendar"

    /// Icone name `tags` format `tags`
    case tags = "tags"

    /// Icone name `Paint Roller` format `paint_roller`
    case paintRoller = "paint_roller"

    /// Icone name `Cloud with Sun` format `cloud_with_sun`
    case cloudWithSun = "cloud_with_sun"

    /// Icone name `Grip Lines Vertical` format `grip_lines_vertical`
    case gripLinesVertical = "grip_lines_vertical"

    /// Icone name `Store` format `store`
    case store = "store"

    /// Icone name `Comment Slash` format `comment_slash`
    case commentSlash = "comment_slash"

    /// Icone name `Alternative Trash Restore` format `alternative_trash_restore`
    case alternativeTrashRestore = "alternative_trash_restore"

    /// Icone name `Dice Four` format `dice_four`
    case diceFour = "dice_four"

    /// Icone name `Dice D20` format `dice_d20`
    case diceD20 = "dice_d20"

    /// Icone name `Alternate Long Arrow Down` format `alternate_long_arrow_down`
    case alternateLongArrowDown = "alternate_long_arrow_down"

    /// Icone name `Alternate Arrow Circle Up` format `alternate_arrow_circle_up`
    case alternateArrowCircleUp = "alternate_arrow_circle_up"

    /// Icone name `Damaged House` format `damaged_house`
    case damagedHouse = "damaged_house"

    /// Icone name `Check Square` format `check_square`
    case checkSquare = "check_square"

    /// Icone name `step-forward` format `step-forward`
    case stepForward = "step-forward"

    /// Icone name `Laughing Squinting Face` format `laughing_squinting_face`
    case laughingSquintingFace = "laughing_squinting_face"

    /// Icone name `Box Open` format `box_open`
    case boxOpen = "box_open"

    /// Icone name `Tools` format `tools`
    case tools = "tools"

    /// Icone name `Clipboard` format `clipboard`
    case clipboard = "clipboard"

    /// Icone name `Vihara` format `vihara`
    case vihara = "vihara"

    /// Icone name `flag` format `flag`
    case flag = "flag"

    /// Icone name `Pizza Slice` format `pizza_slice`
    case pizzaSlice = "pizza_slice"

    /// Icone name `Money Bill` format `money_bill`
    case moneyBill = "money_bill"

    /// Icone name `Envelope Open-text` format `envelope_open-text`
    case envelopeOpenText = "envelope_open-text"

    /// Icone name `Registered Trademark` format `registered_trademark`
    case registeredTrademark = "registered_trademark"

    /// Icone name `location-arrow` format `location-arrow`
    case locationArrow = "location-arrow"

    /// Icone name `Medical Clinic` format `medical_clinic`
    case medicalClinic = "medical_clinic"

    /// Icone name `Alternate Shield` format `alternate_shield`
    case alternateShield = "alternate_shield"

    /// Icone name `thumbs-down` format `thumbs-down`
    case thumbsDown = "thumbs-down"

    /// Icone name `gift` format `gift`
    case gift = "gift"

    /// Icone name `Powerpoint File` format `powerpoint_file`
    case powerpointFile = "powerpoint_file"

    /// Icone name `Television` format `television`
    case television = "television"

    /// Icone name `Cocktail` format `cocktail`
    case cocktail = "cocktail"

    /// Icone name `Chevron Circle Up` format `chevron_circle_up`
    case chevronCircleUp = "chevron_circle_up"

    /// Icone name `table` format `table`
    case table = "table"

    /// Icone name `arrow-left` format `arrow-left`
    case arrowLeft = "arrow-left"

    /// Icone name `Archive` format `archive`
    case archive = "archive"

    /// Icone name `Award` format `award`
    case award = "award"

    /// Icone name `hospital` format `hospital`
    case hospital = "hospital"

    /// Icone name `Sort` format `sort`
    case sort = "sort"

    /// Icone name `History` format `history`
    case history = "history"

    /// Icone name `Sort Amount Down` format `sort_amount_down`
    case sortAmountDown = "sort_amount_down"

    /// Icone name `Window Maximize` format `window_maximize`
    case windowMaximize = "window_maximize"

    /// Icone name `Square Full` format `square_full`
    case squareFull = "square_full"

    /// Icone name `Book of the Dead` format `book_of_the_dead`
    case bookOfTheDead = "book_of_the_dead"

    /// Icone name `Cut` format `cut`
    case cut = "cut"

    /// Icone name `Sort Down (Descending)` format `sort_down_(descending)`
    case sortDownDescending = "sort_down_(descending)"

    /// Icone name `Flask` format `flask`
    case flask = "flask"

    /// Icone name `Desktop` format `desktop`
    case desktop = "desktop"

    /// Icone name `Window Restore` format `window_restore`
    case windowRestore = "window_restore"

    /// Icone name `Alternate Level Down` format `alternate_level_down`
    case alternateLevelDown = "alternate_level_down"

    /// Icone name `Photo Video` format `photo_video`
    case photoVideo = "photo_video"

    /// Icone name `Bible` format `bible`
    case bible = "bible"

    /// Icone name `Ethernet` format `ethernet`
    case ethernet = "ethernet"

    /// Icone name `Spinner` format `spinner`
    case spinner = "spinner"

    /// Icone name `Hamsa` format `hamsa`
    case hamsa = "hamsa"

    /// Icone name `Audio File` format `audio_file`
    case audioFile = "audio_file"

    /// Icone name `Scissors (Hand)` format `scissors_(hand)`
    case scissorsHand = "scissors_(hand)"

    /// Icone name `Sitemap` format `sitemap`
    case sitemap = "sitemap"

    /// Icone name `Teeth Open` format `teeth_open`
    case teethOpen = "teeth_open"

    /// Icone name `Video` format `video`
    case video = "video"

    /// Icone name `Tired Face` format `tired_face`
    case tiredFace = "tired_face"

    /// Icone name `Allergies` format `allergies`
    case allergies = "allergies"

    /// Icone name `Dice Five` format `dice_five`
    case diceFive = "dice_five"

    /// Icone name `Users Cog` format `users_cog`
    case usersCog = "users_cog"

    /// Icone name `Hard Hat` format `hard_hat`
    case hardHat = "hard_hat"

    /// Icone name `American Sign Language Interpreting` format `american_sign_language_interpreting`
    case americanSignLanguageInterpreting = "american_sign_language_interpreting"

    /// Icone name `Parking` format `parking`
    case parking = "parking"

    /// Icone name `Hryvnia` format `hryvnia`
    case hryvnia = "hryvnia"

    /// Icone name `Grinning Squinting Face` format `grinning_squinting_face`
    case grinningSquintingFace = "grinning_squinting_face"

    /// Icone name `Word File` format `word_file`
    case wordFile = "word_file"

    /// Icone name `Shower` format `shower`
    case shower = "shower"

    /// Icone name `Doctor` format `doctor`
    case doctor = "doctor"

    /// Icone name `Angry Face` format `angry_face`
    case angryFace = "angry_face"

    /// Icone name `Car` format `car`
    case car = "car"

    /// Icone name `Equals` format `equals`
    case equals = "equals"

    /// Icone name `Chalkboard Teacher` format `chalkboard_teacher`
    case chalkboardTeacher = "chalkboard_teacher"

    /// Icone name `Concierge Bell` format `concierge_bell`
    case conciergeBell = "concierge_bell"

    /// Icone name `Carrot` format `carrot`
    case carrot = "carrot"

    /// Icone name `TTY` format `tty`
    case tty = "tty"

    /// Icone name `Language` format `language`
    case language = "language"

    /// Icone name `Video Slash` format `video_slash`
    case videoSlash = "video_slash"

    /// Icone name `Alternate Radiation` format `alternate_radiation`
    case alternateRadiation = "alternate_radiation"

    /// Icone name `Phone Volume` format `phone_volume`
    case phoneVolume = "phone_volume"

    /// Icone name `Hourglass End` format `hourglass_end`
    case hourglassEnd = "hourglass_end"

    /// Icone name `File Prescription` format `file_prescription`
    case filePrescription = "file_prescription"

    /// Icone name `Alternate Feather` format `alternate_feather`
    case alternateFeather = "alternate_feather"

    /// Icone name `th-list` format `th-list`
    case thList = "th-list"

    /// Icone name `Battery Empty` format `battery_empty`
    case batteryEmpty = "battery_empty"

    /// Icone name `Guitar` format `guitar`
    case guitar = "guitar"

    /// Icone name `Angle Double Right` format `angle_double_right`
    case angleDoubleRight = "angle_double_right"

    /// Icone name `First Aid` format `first_aid`
    case firstAid = "first_aid"

    /// Icone name `Dolly` format `dolly`
    case dolly = "dolly"

    /// Icone name `Chess Board` format `chess_board`
    case chessBoard = "chess_board"

    /// Icone name `Cat` format `cat`
    case cat = "cat"

    /// Icone name `Identification Badge` format `identification_badge`
    case identificationBadge = "identification_badge"

    /// Icone name `Bong` format `bong`
    case bong = "bong"

    /// Icone name `Moon` format `moon`
    case moon = "moon"

    /// Icone name `Passport` format `passport`
    case passport = "passport"

    /// Icone name `Drafting Compass` format `drafting_compass`
    case draftingCompass = "drafting_compass"

    /// Icone name `Dumbbell` format `dumbbell`
    case dumbbell = "dumbbell"

    /// Icone name `Copy` format `copy`
    case copy = "copy"

    /// Icone name `Volume Off` format `volume_off`
    case volumeOff = "volume_off"

    /// Icone name `Alternate Hospital` format `alternate_hospital`
    case alternateHospital = "alternate_hospital"

    /// Icone name `Upload` format `upload`
    case upload = "upload"

    /// Icone name `Comment Dollar` format `comment_dollar`
    case commentDollar = "comment_dollar"

    /// Icone name `Icons` format `icons`
    case icons = "icons"

    /// Icone name `X-Ray` format `x-ray`
    case xRay = "x-ray"

    /// Icone name `Face With Tongue` format `face_with_tongue`
    case faceWithTongue = "face_with_tongue"

    /// Icone name `Balance Scale (Right-Weighted)` format `balance_scale_(right-weighted)`
    case balanceScaleRightWeighted = "balance_scale_(right-weighted)"

    /// Icone name `Map Marked` format `map_marked`
    case mapMarked = "map_marked"

    /// Icone name `Futbol` format `futbol`
    case futbol = "futbol"

    /// Icone name `Caret Right` format `caret_right`
    case caretRight = "caret_right"

    /// Icone name `Pastafarianism` format `pastafarianism`
    case pastafarianism = "pastafarianism"

    /// Icone name `rss` format `rss`
    case rss = "rss"

    /// Icone name `Shekel Sign` format `shekel_sign`
    case shekelSign = "shekel_sign"

    /// Icone name `Frowning Face With Open Mouth` format `frowning_face_with_open_mouth`
    case frowningFaceWithOpenMouth = "frowning_face_with_open_mouth"

    /// Icone name `chevron-left` format `chevron-left`
    case chevronLeft = "chevron-left"

    /// Icone name `align-center` format `align-center`
    case alignCenter = "align-center"

    /// Icone name `Tenge` format `tenge`
    case tenge = "tenge"

    /// Icone name `bullhorn` format `bullhorn`
    case bullhorn = "bullhorn"

    /// Icone name `Swatchbook` format `swatchbook`
    case swatchbook = "swatchbook"

    /// Icone name `Quran` format `quran`
    case quran = "quran"

    /// Icone name `Hot Tub` format `hot_tub`
    case hotTub = "hot_tub"

    /// Icone name `Ruler Horizontal` format `ruler_horizontal`
    case rulerHorizontal = "ruler_horizontal"

    /// Icone name `Bowling Ball` format `bowling_ball`
    case bowlingBall = "bowling_ball"

    /// Icone name `I Beam Cursor` format `i_beam_cursor`
    case iBeamCursor = "i_beam_cursor"

    /// Icone name `Snowman` format `snowman`
    case snowman = "snowman"

    /// Icone name `Globe with Americas shown` format `globe_with_americas_shown`
    case globeWithAmericasShown = "globe_with_americas_shown"

    /// Icone name `Seedling` format `seedling`
    case seedling = "seedling"

    /// Icone name `Capsules` format `capsules`
    case capsules = "capsules"

    /// Icone name `Jedi` format `jedi`
    case jedi = "jedi"

    /// Icone name `Theater Masks` format `theater_masks`
    case theaterMasks = "theater_masks"

    /// Icone name `Bullseye` format `bullseye`
    case bullseye = "bullseye"

    /// Icone name `Wizard's Hat` format `wizard's_hat`
    case wizardSHat = "wizard's_hat"

    /// Icone name `Hand Pointing Down` format `hand_pointing_down`
    case handPointingDown = "hand_pointing_down"

    /// Icone name `Arrow Circle Up` format `arrow_circle_up`
    case arrowCircleUp = "arrow_circle_up"

    /// Icone name `Ribbon` format `ribbon`
    case ribbon = "ribbon"

    /// Icone name `Pencil Ruler` format `pencil_ruler`
    case pencilRuler = "pencil_ruler"

    /// Icone name `Hand with Middle Finger Raised` format `hand_with_middle_finger_raised`
    case handWithMiddleFingerRaised = "hand_with_middle_finger_raised"

    /// Icone name `chevron-up` format `chevron-up`
    case chevronUp = "chevron-up"

    /// Icone name `Dove` format `dove`
    case dove = "dove"

    /// Icone name `Broadcast Tower` format `broadcast_tower`
    case broadcastTower = "broadcast_tower"

    /// Icone name `Vial` format `vial`
    case vial = "vial"

    /// Icone name `Percentage` format `percentage`
    case percentage = "percentage"

    /// Icone name `Street View` format `street_view`
    case streetView = "street_view"

    /// Icone name `RSS Square` format `rss_square`
    case rSSSquare = "rss_square"

    /// Icone name `Alternate Level Up` format `alternate_level_up`
    case alternateLevelUp = "alternate_level_up"

    /// Icone name `Piggy Bank` format `piggy_bank`
    case piggyBank = "piggy_bank"

    /// Icone name `Assistive Listening Systems` format `assistive_listening_systems`
    case assistiveListeningSystems = "assistive_listening_systems"

    /// Icone name `Ad` format `ad`
    case ad = "ad"

    /// Icone name `Shoe Prints` format `shoe_prints`
    case shoePrints = "shoe_prints"

    /// Icone name `tag` format `tag`
    case tag = "tag"

    /// Icone name `User Injured` format `user_injured`
    case userInjured = "user_injured"

    /// Icone name `Sort Numeric Down` format `sort_numeric_down`
    case sortNumericDown = "sort_numeric_down"

    /// Icone name `Life Ring` format `life_ring`
    case lifeRing = "life_ring"

    /// Icone name `Circle Notched` format `circle_notched`
    case circleNotched = "circle_notched"

    /// Icone name `Wind` format `wind`
    case wind = "wind"

    /// Icone name `Hand Pointing Up` format `hand_pointing_up`
    case handPointingUp = "hand_pointing_up"

    /// Icone name `Audio Description` format `audio_description`
    case audioDescription = "audio_description"

    /// Icone name `Sort Amount Up` format `sort_amount_up`
    case sortAmountUp = "sort_amount_up"

    /// Icone name `Search Dollar` format `search_dollar`
    case searchDollar = "search_dollar"

    /// Icone name `Snowflake` format `snowflake`
    case snowflake = "snowflake"

    /// Icone name `Mars Stroke Horizontal` format `mars_stroke_horizontal`
    case marsStrokeHorizontal = "mars_stroke_horizontal"

    /// Icone name `Fingerprint` format `fingerprint`
    case fingerprint = "fingerprint"

    /// Icone name `trophy` format `trophy`
    case trophy = "trophy"

    /// Icone name `Image File` format `image_file`
    case imageFile = "image_file"

    /// Icone name `play` format `play`
    case play = "play"

    /// Icone name `Columns` format `columns`
    case columns = "columns"

    /// Icone name `Alternate Headphones` format `alternate_headphones`
    case alternateHeadphones = "alternate_headphones"

    /// Icone name `Bicycle` format `bicycle`
    case bicycle = "bicycle"

    /// Icone name `subscript` format `subscript`
    case `subscript` = "subscript"

    /// Icone name `Identification Card` format `identification_card`
    case identificationCard = "identification_card"

    /// Icone name `Clipboard with Check` format `clipboard_with_check`
    case clipboardWithCheck = "clipboard_with_check"

    /// Icone name `Poo` format `poo`
    case poo = "poo"

    /// Icone name `Football Ball` format `football_ball`
    case footballBall = "football_ball"

    /// Icone name `Boxes` format `boxes`
    case boxes = "boxes"

    /// Icone name `fire-extinguisher` format `fire-extinguisher`
    case fireExtinguisher = "fire-extinguisher"

    /// Icone name `Alternate Expand Arrows` format `alternate_expand_arrows`
    case alternateExpandArrows = "alternate_expand_arrows"

    /// Icone name `Couch` format `couch`
    case couch = "couch"

    /// Icone name `Pause Circle` format `pause_circle`
    case pauseCircle = "pause_circle"

    /// Icone name `Tablets` format `tablets`
    case tablets = "tablets"

    /// Icone name `Digital Tachograph` format `digital_tachograph`
    case digitalTachograph = "digital_tachograph"

    /// Icone name `Border Style` format `border_style`
    case borderStyle = "border_style"

    /// Icone name `Heart Broken` format `heart_broken`
    case heartBroken = "heart_broken"

    /// Icone name `certificate` format `certificate`
    case certificate = "certificate"

    /// Icone name `Hospital Symbol` format `hospital_symbol`
    case hospitalSymbol = "hospital_symbol"

    /// Icone name `Redo` format `redo`
    case redo = "redo"

    /// Icone name `Alternate Redo` format `alternate_redo`
    case alternateRedo = "alternate_redo"

    /// Icone name `Crying Face` format `crying_face`
    case cryingFace = "crying_face"

    /// Icone name `Caret Left` format `caret_left`
    case caretLeft = "caret_left"

    /// Icone name `Object Ungroup` format `object_ungroup`
    case objectUngroup = "object_ungroup"

    /// Icone name `Deaf` format `deaf`
    case deaf = "deaf"

    /// Icone name `Umbrella Beach` format `umbrella_beach`
    case umbrellaBeach = "umbrella_beach"

    /// Icone name `Hand Holding US Dollar` format `hand_holding_us_dollar`
    case handHoldingUSDollar = "hand_holding_us_dollar"

    /// Icone name `Slash` format `slash`
    case slash = "slash"

    /// Icone name `Paper Plane` format `paper_plane`
    case paperPlane = "paper_plane"

    /// Icone name `Pen Nib` format `pen_nib`
    case penNib = "pen_nib"

    /// Icone name `Alternate Mobile` format `alternate_mobile`
    case alternateMobile = "alternate_mobile"

    /// Icone name `Bug` format `bug`
    case bug = "bug"

    /// Icone name `Volume Down` format `volume_down`
    case volumeDown = "volume_down"

    /// Icone name `map-marker` format `map-marker`
    case mapMarker = "map-marker"

    /// Icone name `Cowboy Hat` format `cowboy_hat`
    case cowboyHat = "cowboy_hat"

    /// Icone name `Save` format `save`
    case save = "save"

    /// Icone name `Tape` format `tape`
    case tape = "tape"

    /// Icone name `Underline` format `underline`
    case underline = "underline"

    /// Icone name `User Slash` format `user_slash`
    case userSlash = "user_slash"

    /// Icone name `Smoking` format `smoking`
    case smoking = "smoking"

    /// Icone name `User Tag` format `user_tag`
    case userTag = "user_tag"

    /// Icone name `Angle Double Up` format `angle_double_up`
    case angleDoubleUp = "angle_double_up"

    /// Icone name `beer` format `beer`
    case beer = "beer"

    /// Icone name `Grimacing Face` format `grimacing_face`
    case grimacingFace = "grimacing_face"

    /// Icone name `Glass Cheers` format `glass_cheers`
    case glassCheers = "glass_cheers"

    /// Icone name `Hamburger` format `hamburger`
    case hamburger = "hamburger"

    /// Icone name `Candy Cane` format `candy_cane`
    case candyCane = "candy_cane"

    /// Icone name `Suitcase` format `suitcase`
    case suitcase = "suitcase"

    /// Icone name `Neuter` format `neuter`
    case neuter = "neuter"

    /// Icone name `Map` format `map`
    case map = "map"

    /// Icone name `comment` format `comment`
    case comment = "comment"

    /// Icone name `Weight` format `weight`
    case weight = "weight"

    /// Icone name `Spray Can` format `spray_can`
    case sprayCan = "spray_can"

    /// Icone name `Alternate Sort Amount Up` format `alternate_sort_amount_up`
    case alternateSortAmountUp = "alternate_sort_amount_up"

    /// Icone name `Alternate Map Marked` format `alternate_map_marked`
    case alternateMapMarked = "alternate_map_marked"

    /// Icone name `Chess Queen` format `chess_queen`
    case chessQueen = "chess_queen"

    /// Icone name `Alternate Arrow Circle Left` format `alternate_arrow_circle_left`
    case alternateArrowCircleLeft = "alternate_arrow_circle_left"

    /// Icone name `Dice Six` format `dice_six`
    case diceSix = "dice_six"

    /// Icone name `Lightbulb` format `lightbulb`
    case lightbulb = "lightbulb"

    /// Icone name `Chevron Circle Down` format `chevron_circle_down`
    case chevronCircleDown = "chevron_circle_down"

    /// Icone name `Satellite` format `satellite`
    case satellite = "satellite"

    /// Icone name `Horizontal Sliders` format `horizontal_sliders`
    case horizontalSliders = "horizontal_sliders"

    /// Icone name `Alternate Store` format `alternate_store`
    case alternateStore = "alternate_store"

    /// Icone name `Search` format `search`
    case search = "search"

    /// Icone name `Sort Alphabetical Down` format `sort_alphabetical_down`
    case sortAlphabeticalDown = "sort_alphabetical_down"

    /// Icone name `align-right` format `align-right`
    case alignRight = "align-right"

    /// Icone name `fast-forward` format `fast-forward`
    case fastForward = "fast-forward"

    /// Icone name `Glasses` format `glasses`
    case glasses = "glasses"

    /// Icone name `Tram` format `tram`
    case tram = "tram"

    /// Icone name `Restroom` format `restroom`
    case restroom = "restroom"

    /// Icone name `Mercury` format `mercury`
    case mercury = "mercury"

    /// Icone name `Church` format `church`
    case church = "church"

    /// Icone name `Ruble Sign` format `ruble_sign`
    case rubleSign = "ruble_sign"

    /// Icone name `Less Than` format `less_than`
    case lessThan = "less_than"

    /// Icone name `Search Location` format `search_location`
    case searchLocation = "search_location"

    /// Icone name `User Ninja` format `user_ninja`
    case userNinja = "user_ninja"

    /// Icone name `Euro Sign` format `euro_sign`
    case euroSign = "euro_sign"

    /// Icone name `T-Shirt` format `t-shirt`
    case tShirt = "t-shirt"

    /// Icone name `Hanukiah` format `hanukiah`
    case hanukiah = "hanukiah"

    /// Icone name `random` format `random`
    case random = "random"

    /// Icone name `Lightning Bolt` format `lightning_bolt`
    case lightningBolt = "lightning_bolt"

    /// Icone name `Cloud with Rain` format `cloud_with_rain`
    case cloudWithRain = "cloud_with_rain"

    /// Icone name `Grip Horizontal` format `grip_horizontal`
    case gripHorizontal = "grip_horizontal"

    /// Icone name `Microphone Slash` format `microphone_slash`
    case microphoneSlash = "microphone_slash"

    /// Icone name `Battery 3/4 Full` format `battery_3/4_full`
    case battery34Full = "battery_3/4_full"

    /// Icone name `Transgender` format `transgender`
    case transgender = "transgender"

    /// Icone name `Snowboarding` format `snowboarding`
    case snowboarding = "snowboarding"

    /// Icone name `Mail Bulk` format `mail_bulk`
    case mailBulk = "mail_bulk"

    /// Icone name `Database` format `database`
    case database = "database"

    /// Icone name `Hourglass Start` format `hourglass_start`
    case hourglassStart = "hourglass_start"

    /// Icone name `Record Vinyl` format `record_vinyl`
    case recordVinyl = "record_vinyl"

    /// Icone name `Motorcycle` format `motorcycle`
    case motorcycle = "motorcycle"

    /// Icone name `Credit Card` format `credit_card`
    case creditCard = "credit_card"

    /// Icone name `Alternate Share` format `alternate_share`
    case alternateShare = "alternate_share"

    /// Icone name `Chess Pawn` format `chess_pawn`
    case chessPawn = "chess_pawn"

    /// Icone name `Gas Pump` format `gas_pump`
    case gasPump = "gas_pump"

    /// Icone name `Atlas` format `atlas`
    case atlas = "atlas"

    /// Icone name `Alternate Medical Chat` format `alternate_medical_chat`
    case alternateMedicalChat = "alternate_medical_chat"

    /// Icone name `Alternate Ticket` format `alternate_ticket`
    case alternateTicket = "alternate_ticket"

    /// Icone name `Grinning Face With Big Eyes` format `grinning_face_with_big_eyes`
    case grinningFaceWithBigEyes = "grinning_face_with_big_eyes"

    /// Icone name `Hand Pointing Left` format `hand_pointing_left`
    case handPointingLeft = "hand_pointing_left"

    /// Icone name `Frowning Face` format `frowning_face`
    case frowningFace = "frowning_face"

    /// Icone name `Winking Face With Tongue` format `winking_face_with_tongue`
    case winkingFaceWithTongue = "winking_face_with_tongue"

    /// Icone name `Lemon` format `lemon`
    case lemon = "lemon"

    /// Icone name `angle-left` format `angle-left`
    case angleLeft = "angle-left"

    /// Icone name `Child` format `child`
    case child = "child"

    /// Icone name `Times Circle` format `times_circle`
    case timesCircle = "times_circle"

    /// Icone name `Campground` format `campground`
    case campground = "campground"

    /// Icone name `Pills` format `pills`
    case pills = "pills"

    /// Icone name `Thermometer 1/4 Full` format `thermometer_1/4_full`
    case thermometer14Full = "thermometer_1/4_full"

    /// Icone name `Gavel` format `gavel`
    case gavel = "gavel"

    /// Icone name `Map Pin` format `map_pin`
    case mapPin = "map_pin"

    /// Icone name `Cash Register` format `cash_register`
    case cashRegister = "cash_register"

    /// Icone name `Basketball Ball` format `basketball_ball`
    case basketballBall = "basketball_ball"

    /// Icone name `Hands` format `hands`
    case hands = "hands"

    /// Icone name `Alternate Phone Square` format `alternate_phone_square`
    case alternatePhoneSquare = "alternate_phone_square"

    /// Icone name `Oil Can` format `oil_can`
    case oilCan = "oil_can"

    /// Icone name `Pound Sign` format `pound_sign`
    case poundSign = "pound_sign"

    /// Icone name `Hippo` format `hippo`
    case hippo = "hippo"

    /// Icone name `Ice Cream` format `ice_cream`
    case iceCream = "ice_cream"

    /// Icone name `Alternate Car` format `alternate_car`
    case alternateCar = "alternate_car"

    /// Icone name `Heartbeat` format `heartbeat`
    case heartbeat = "heartbeat"

    /// Icone name `Running` format `running`
    case running = "running"

    /// Icone name `ambulance` format `ambulance`
    case ambulance = "ambulance"

    /// Icone name `Satellite Dish` format `satellite_dish`
    case satelliteDish = "satellite_dish"

    /// Icone name `Briefcase` format `briefcase`
    case briefcase = "briefcase"

    /// Icone name `Book Open` format `book_open`
    case bookOpen = "book_open"

    /// Icone name `Volume Up` format `volume_up`
    case volumeUp = "volume_up"

    /// Icone name `Alternate Crop` format `alternate_crop`
    case alternateCrop = "alternate_crop"

    /// Icone name `Dharmachakra` format `dharmachakra`
    case dharmachakra = "dharmachakra"

    /// Icone name `Brush` format `brush`
    case brush = "brush"

    /// Icone name `Bath` format `bath`
    case bath = "bath"

    /// Icone name `Low Temperature` format `low_temperature`
    case lowTemperature = "low_temperature"

    /// Icone name `Crutch` format `crutch`
    case crutch = "crutch"

    /// Icone name `arrow-up` format `arrow-up`
    case arrowUp = "arrow-up"

    /// Icone name `Excel File` format `excel_file`
    case excelFile = "excel_file"

    /// Icone name `Stopwatch` format `stopwatch`
    case stopwatch = "stopwatch"

    /// Icone name `Outdent` format `outdent`
    case outdent = "outdent"

    /// Icone name `Expand` format `expand`
    case expand = "expand"

    /// Icone name `Praying Hands` format `praying_hands`
    case prayingHands = "praying_hands"

    /// Icone name `Wired Network` format `wired_network`
    case wiredNetwork = "wired_network"

    /// Icone name `User Tie` format `user_tie`
    case userTie = "user_tie"

    /// Icone name `Broom` format `broom`
    case broom = "broom"

    /// Icone name `Alternate Sync` format `alternate_sync`
    case alternateSync = "alternate_sync"

    /// Icone name `Baseball Ball` format `baseball_ball`
    case baseballBall = "baseball_ball"

    /// Icone name `Dizzy Face` format `dizzy_face`
    case dizzyFace = "dizzy_face"

    /// Icone name `Low Vision` format `low_vision`
    case lowVision = "low_vision"

    /// Icone name `Alternate Pen` format `alternate_pen`
    case alternatePen = "alternate_pen"

    /// Icone name `Medal` format `medal`
    case medal = "medal"

    /// Icone name `Cloud with Moon and Rain` format `cloud_with_moon_and_rain`
    case cloudWithMoonAndRain = "cloud_with_moon_and_rain"

    /// Icone name `Laughing Winking Face` format `laughing_winking_face`
    case laughingWinkingFace = "laughing_winking_face"

    /// Icone name `User Clock` format `user_clock`
    case userClock = "user_clock"

    /// Icone name `Joint` format `joint`
    case joint = "joint"

    /// Icone name `Toilet Paper` format `toilet_paper`
    case toiletPaper = "toilet_paper"

    /// Icone name `Wavy Money Bill` format `wavy_money_bill`
    case wavyMoneyBill = "wavy_money_bill"

    /// Icone name `lock` format `lock`
    case lock = "lock"

    /// Icone name `Flushed Face` format `flushed_face`
    case flushedFace = "flushed_face"

    /// Icone name `Dice Two` format `dice_two`
    case diceTwo = "dice_two"

    /// Icone name `Chess King` format `chess_king`
    case chessKing = "chess_king"

    /// Icone name `Car Crash` format `car_crash`
    case carCrash = "car_crash"

    /// Icone name `Dot Circle` format `dot_circle`
    case dotCircle = "dot_circle"

    /// Icone name `Alternate Phone` format `alternate_phone`
    case alternatePhone = "alternate_phone"

    /// Icone name `Cloud with Moon` format `cloud_with_moon`
    case cloudWithMoon = "cloud_with_moon"

    /// Icone name `Caret Up` format `caret_up`
    case caretUp = "caret_up"

    /// Icone name `Mortar Pestle` format `mortar_pestle`
    case mortarPestle = "mortar_pestle"

    /// Icone name `Dice` format `dice`
    case dice = "dice"

    /// Icone name `Image` format `image`
    case image = "image"

    /// Icone name `Alternate Cloud Upload` format `alternate_cloud_upload`
    case alternateCloudUpload = "alternate_cloud_upload"

    /// Icone name `fighter-jet` format `fighter-jet`
    case fighterJet = "fighter-jet"

    /// Icone name `Alternate Comment` format `alternate_comment`
    case alternateComment = "alternate_comment"

    /// Icone name `Braille` format `braille`
    case braille = "braille"

    /// Icone name `Hourglass Half` format `hourglass_half`
    case hourglassHalf = "hourglass_half"

    /// Icone name `Memory` format `memory`
    case memory = "memory"

    /// Icone name `Check Circle` format `check_circle`
    case checkCircle = "check_circle"

    /// Icone name `Chess Rook` format `chess_rook`
    case chessRook = "chess_rook"

    /// Icone name `User Edit` format `user_edit`
    case userEdit = "user_edit"

    /// Icone name `Cross` format `cross`
    case cross = "cross"

    /// Icone name `Recycle` format `recycle`
    case recycle = "recycle"

    /// Icone name `Voicemail` format `voicemail`
    case voicemail = "voicemail"

    /// Icone name `Kiwi Bird` format `kiwi_bird`
    case kiwiBird = "kiwi_bird"

    /// Icone name `Link` format `link`
    case link = "link"

    /// Icone name `Helping Hands` format `helping_hands`
    case helpingHands = "helping_hands"

    /// Icone name `User Plus` format `user_plus`
    case userPlus = "user_plus"

    /// Icone name `Palette` format `palette`
    case palette = "palette"

    /// Icone name `Diagnoses` format `diagnoses`
    case diagnoses = "diagnoses"

    /// Icone name `Clone` format `clone`
    case clone = "clone"

    /// Icone name `heading` format `heading`
    case heading = "heading"

    /// Icone name `Traffic Light` format `traffic_light`
    case trafficLight = "traffic_light"

    /// Icone name `Alternate Long Arrow Left` format `alternate_long_arrow_left`
    case alternateLongArrowLeft = "alternate_long_arrow_left"

    /// Icone name `Shipping Fast` format `shipping_fast`
    case shippingFast = "shipping_fast"

    /// Icone name `Lock Open` format `lock_open`
    case lockOpen = "lock_open"

    /// Icone name `Route` format `route`
    case route = "route"

    /// Icone name `Blender Phone` format `blender_phone`
    case blenderPhone = "blender_phone"

    /// Icone name `Toggle On` format `toggle_on`
    case toggleOn = "toggle_on"

    /// Icone name `Skiing Nordic` format `skiing_nordic`
    case skiingNordic = "skiing_nordic"

    /// Icone name `Alternate Wavy Money Bill` format `alternate_wavy_money_bill`
    case alternateWavyMoneyBill = "alternate_wavy_money_bill"

    /// Icone name `Cubes` format `cubes`
    case cubes = "cubes"

    /// Icone name `arrow-down` format `arrow-down`
    case arrowDown = "arrow-down"

    /// Icone name `Alternate Transgender` format `alternate_transgender`
    case alternateTransgender = "alternate_transgender"

    /// Icone name `Plane Arrival` format `plane_arrival`
    case planeArrival = "plane_arrival"

    /// Icone name `flag-checkered` format `flag-checkered`
    case flagCheckered = "flag-checkered"

    /// Icone name `Alternate Pencil` format `alternate_pencil`
    case alternatePencil = "alternate_pencil"

    /// Icone name `PDF File` format `pdf_file`
    case pDFFile = "pdf_file"

    /// Icone name `SIM Card` format `sim_card`
    case sIMCard = "sim_card"

    /// Icone name `Hand Holding Heart` format `hand_holding_heart`
    case handHoldingHeart = "hand_holding_heart"

    /// Icone name `Divide` format `divide`
    case divide = "divide"

    /// Icone name `Alternate Long Arrow Up` format `alternate_long_arrow_up`
    case alternateLongArrowUp = "alternate_long_arrow_up"

    /// Icone name `plus` format `plus`
    case plus = "plus"

    /// Icone name `Thermometer` format `thermometer`
    case thermometer = "thermometer"

    /// Icone name `Times` format `times`
    case times = "times"

    /// Icone name `Fish` format `fish`
    case fish = "fish"

    /// Icone name `quote-right` format `quote-right`
    case quoteRight = "quote-right"

    /// Icone name `Trash` format `trash`
    case trash = "trash"

    /// Icone name `University` format `university`
    case university = "university"

    /// Icone name `Bus Alt` format `bus_alt`
    case busAlt = "bus_alt"

    /// Icone name `Toggle Off` format `toggle_off`
    case toggleOff = "toggle_off"

    /// Icone name `Globe` format `globe`
    case globe = "globe"

    /// Icone name `Face Without Mouth` format `face_without_mouth`
    case faceWithoutMouth = "face_without_mouth"

    /// Icone name `Retweet` format `retweet`
    case retweet = "retweet"

    /// Icone name `Fan` format `fan`
    case fan = "fan"

    /// Icone name `eject` format `eject`
    case eject = "eject"

    /// Icone name `Border None` format `border_none`
    case borderNone = "border_none"

    /// Icone name `Font Awesome Full Logo` format `font_awesome_full_logo`
    case fontAwesomeFullLogo = "font_awesome_full_logo"

    /// Icone name `Pager` format `pager`
    case pager = "pager"

    /// Icone name `Add to Shopping Cart` format `add_to_shopping_cart`
    case addToShoppingCart = "add_to_shopping_cart"

    /// Icone name `Fill` format `fill`
    case fill = "fill"

    /// Icone name `Person Entering Booth` format `person_entering_booth`
    case personEnteringBooth = "person_entering_booth"

    /// Icone name `Cookie` format `cookie`
    case cookie = "cookie"

    /// Icone name `Sign Language` format `sign_language`
    case signLanguage = "sign_language"

    /// Icone name `Gamepad` format `gamepad`
    case gamepad = "gamepad"

    /// Icone name `Hiking` format `hiking`
    case hiking = "hiking"

    /// Icone name `Bread Slice` format `bread_slice`
    case breadSlice = "bread_slice"

    /// Icone name `Calendar Minus` format `calendar_minus`
    case calendarMinus = "calendar_minus"

    /// Icone name `Greater Than` format `greater_than`
    case greaterThan = "greater_than"

    /// Icone name `User Graduate` format `user_graduate`
    case userGraduate = "user_graduate"

    /// Icone name `Undo` format `undo`
    case undo = "undo"

    /// Icone name `File Contract` format `file_contract`
    case fileContract = "file_contract"

    /// Icone name `Alternate Arrow Circle Right` format `alternate_arrow_circle_right`
    case alternateArrowCircleRight = "alternate_arrow_circle_right"

    /// Icone name `Haykal` format `haykal`
    case haykal = "haykal"

    /// Icone name `Kissing Face With Smiling Eyes` format `kissing_face_with_smiling_eyes`
    case kissingFaceWithSmilingEyes = "kissing_face_with_smiling_eyes"

    /// Icone name `Calculator` format `calculator`
    case calculator = "calculator"

    /// Icone name `Keyboard` format `keyboard`
    case keyboard = "keyboard"

    /// Icone name `Portrait` format `portrait`
    case portrait = "portrait"

    /// Icone name `Mobile Phone` format `mobile_phone`
    case mobilePhone = "mobile_phone"

    /// Icone name `Minus Square` format `minus_square`
    case minusSquare = "minus_square"

    /// Icone name `Compass` format `compass`
    case compass = "compass"

    /// Icone name `Truck Moving` format `truck_moving`
    case truckMoving = "truck_moving"

    /// Icone name `font` format `font`
    case font = "font"

    /// Icone name `Chair` format `chair`
    case chair = "chair"

    /// Icone name `Chalkboard` format `chalkboard`
    case chalkboard = "chalkboard"

    /// Icone name `Solar Panel` format `solar_panel`
    case solarPanel = "solar_panel"

    /// Icone name `Pray` format `pray`
    case pray = "pray"

    /// Icone name `medkit` format `medkit`
    case medkit = "medkit"

    /// Icone name `Train` format `train`
    case train = "train"

    /// Icone name `Alternate Sign In` format `alternate_sign_in`
    case alternateSignIn = "alternate_sign_in"

    /// Icone name `Martini Glass` format `martini_glass`
    case martiniGlass = "martini_glass"

    /// Icone name `Khanda` format `khanda`
    case khanda = "khanda"

    /// Icone name `Map Signs` format `map_signs`
    case mapSigns = "map_signs"

    /// Icone name `fire` format `fire`
    case fire = "fire"

    /// Icone name `H Square` format `h_square`
    case hSquare = "h_square"

    /// Icone name `Envelope` format `envelope`
    case envelope = "envelope"

    /// Icone name `Calendar Plus` format `calendar_plus`
    case calendarPlus = "calendar_plus"

    /// Icone name `Ruler Combined` format `ruler_combined`
    case rulerCombined = "ruler_combined"

    /// Icone name `Alternate Share Square` format `alternate_share_square`
    case alternateShareSquare = "alternate_share_square"

    /// Icone name `Democrat` format `democrat`
    case democrat = "democrat"

    /// Icone name `Charging Station` format `charging_station`
    case chargingStation = "charging_station"

    /// Icone name `Dungeon` format `dungeon`
    case dungeon = "dungeon"

    /// Icone name `Baby` format `baby`
    case baby = "baby"

    /// Icone name `Plus Square` format `plus_square`
    case plusSquare = "plus_square"

    /// Icone name `signal` format `signal`
    case signal = "signal"

    /// Icone name `Suitcase Rolling` format `suitcase_rolling`
    case suitcaseRolling = "suitcase_rolling"

    /// Icone name `Splotch` format `splotch`
    case splotch = "splotch"

    /// Icone name `Coins` format `coins`
    case coins = "coins"

    /// Icone name `Caret Square Right` format `caret_square_right`
    case caretSquareRight = "caret_square_right"

    /// Icone name `Meteor` format `meteor`
    case meteor = "meteor"

    /// Icone name `Chess Bishop` format `chess_bishop`
    case chessBishop = "chess_bishop"

    /// Icone name `Dice One` format `dice_one`
    case diceOne = "dice_one"

    /// Icone name `Frog` format `frog`
    case frog = "frog"

    /// Icone name `Table Tennis` format `table_tennis`
    case tableTennis = "table_tennis"

    /// Icone name `Text Width` format `text_width`
    case textWidth = "text_width"

    /// Icone name `Car Battery` format `car_battery`
    case carBattery = "car_battery"

    /// Icone name `Folder` format `folder`
    case folder = "folder"

    /// Icone name `Battery 1/4 Full` format `battery_1/4_full`
    case battery14Full = "battery_1/4_full"

    /// Icone name `Prescription Bottle` format `prescription_bottle`
    case prescriptionBottle = "prescription_bottle"

    /// Icone name `Face Blowing a Kiss` format `face_blowing_a_kiss`
    case faceBlowingAKiss = "face_blowing_a_kiss"

    /// Icone name `key` format `key`
    case key = "key"

    /// Icone name `Ruler` format `ruler`
    case ruler = "ruler"

    /// Icone name `Arrow Circle Left` format `arrow_circle_left`
    case arrowCircleLeft = "arrow_circle_left"

    /// Icone name `print` format `print`
    case print = "print"

    /// Icone name `Alternate Wine Glas` format `alternate_wine_glas`
    case alternateWineGlas = "alternate_wine_glas"

    /// Icone name `th` format `th`
    case th = "th"

    /// Icone name `Volleyball Ball` format `volleyball_ball`
    case volleyballBall = "volleyball_ball"

    /// Icone name `shopping-cart` format `shopping-cart`
    case shoppingCart = "shopping-cart"

    /// Icone name `Baby Carriage` format `baby_carriage`
    case babyCarriage = "baby_carriage"

    /// Icone name `Hand Pointing Right` format `hand_pointing_right`
    case handPointingRight = "hand_pointing_right"

    /// Icone name `Truck Side` format `truck_side`
    case truckSide = "truck_side"

    /// Icone name `Golf Ball` format `golf_ball`
    case golfBall = "golf_ball"

    /// Icone name `Trademark` format `trademark`
    case trademark = "trademark"

    /// Icone name `Hand Holding` format `hand_holding`
    case handHolding = "hand_holding"

    /// Icone name `Gem` format `gem`
    case gem = "gem"

    /// Icone name `Pie Chart` format `pie_chart`
    case pieChart = "pie_chart"

    /// Icone name `Helicopter` format `helicopter`
    case helicopter = "helicopter"

    /// Icone name `Venus` format `venus`
    case venus = "venus"

    /// Icone name `Alternate Sort Amount Down` format `alternate_sort_amount_down`
    case alternateSortAmountDown = "alternate_sort_amount_down"

    /// Icone name `Igloo` format `igloo`
    case igloo = "igloo"

    /// Icone name `Alternate Square Root` format `alternate_square_root`
    case alternateSquareRoot = "alternate_square_root"

    /// Icone name `Vertical Ellipsis` format `vertical_ellipsis`
    case verticalEllipsis = "vertical_ellipsis"

    /// Icone name `Address Card` format `address_card`
    case addressCard = "address_card"

    /// Icone name `Chess` format `chess`
    case chess = "chess"

    /// Icone name `Rolling on the Floor Laughing` format `rolling_on_the_floor_laughing`
    case rollingOnTheFloorLaughing = "rolling_on_the_floor_laughing"

    /// Icone name `Face With Tears of Joy` format `face_with_tears_of_joy`
    case faceWithTearsOfJoy = "face_with_tears_of_joy"

    /// Icone name `Mug Hot` format `mug_hot`
    case mugHot = "mug_hot"

    /// Icone name `Tree` format `tree`
    case tree = "tree"

    /// Icone name `Hanging Weight` format `hanging_weight`
    case hangingWeight = "hanging_weight"

    /// Icone name `File` format `file`
    case file = "file"

    /// Icone name `Spa` format `spa`
    case spa = "spa"

    /// Icone name `Handshake` format `handshake`
    case handshake = "handshake"

    /// Icone name `Paint Brush` format `paint_brush`
    case paintBrush = "paint_brush"

    /// Icone name `User Circle` format `user_circle`
    case userCircle = "user_circle"

    /// Icone name `Truck Loading` format `truck_loading`
    case truckLoading = "truck_loading"

    /// Icone name `step-backward` format `step-backward`
    case stepBackward = "step-backward"

    /// Icone name `Battery 1/2 Full` format `battery_1/2_full`
    case battery12Full = "battery_1/2_full"

    /// Icone name `Mouse` format `mouse`
    case mouse = "mouse"

    /// Icone name `Info` format `info`
    case info = "info"

    /// Icone name `Drum` format `drum`
    case drum = "drum"

    /// Icone name `Brain` format `brain`
    case brain = "brain"

    /// Icone name `School` format `school`
    case school = "school"

    /// Icone name `Stream` format `stream`
    case stream = "stream"

    /// Icone name `Mosque` format `mosque`
    case mosque = "mosque"

    /// Icone name `Alternate Microphone Slash` format `alternate_microphone_slash`
    case alternateMicrophoneSlash = "alternate_microphone_slash"

    /// Icone name `Star and Crescent` format `star_and_crescent`
    case starAndCrescent = "star_and_crescent"

    /// Icone name `Bus` format `bus`
    case bus = "bus"

    /// Icone name `Cardboard VR` format `cardboard_vr`
    case cardboardVR = "cardboard_vr"

    /// Icone name `Venus Mars` format `venus_mars`
    case venusMars = "venus_mars"

    /// Icone name `inbox` format `inbox`
    case inbox = "inbox"

    /// Icone name `Alternate Unlock` format `alternate_unlock`
    case alternateUnlock = "alternate_unlock"

    /// Icone name `Winking Face` format `winking_face`
    case winkingFace = "winking_face"

    /// Icone name `Share Square` format `share_square`
    case shareSquare = "share_square"

    /// Icone name `Video File` format `video_file`
    case videoFile = "video_file"

    /// Icone name `Remove Format` format `remove_format`
    case removeFormat = "remove_format"

    /// Icone name `Strikethrough` format `strikethrough`
    case strikethrough = "strikethrough"

    /// Icone name `At` format `at`
    case at = "at"

    /// Icone name `Balance Scale` format `balance_scale`
    case balanceScale = "balance_scale"

    /// Icone name `Dice D6` format `dice_d6`
    case diceD6 = "dice_d6"

    /// Icone name `Cannabis` format `cannabis`
    case cannabis = "cannabis"

    /// Icone name `Grinning Face` format `grinning_face`
    case grinningFace = "grinning_face"

    /// Icone name `Ruler Vertical` format `ruler_vertical`
    case rulerVertical = "ruler_vertical"

    /// Icone name `Toolbox` format `toolbox`
    case toolbox = "toolbox"

    /// Icone name `Blender` format `blender`
    case blender = "blender"

    /// Icone name `Alternate Arrows Vertical` format `alternate_arrows_vertical`
    case alternateArrowsVertical = "alternate_arrows_vertical"

    /// Icone name `Mars Double` format `mars_double`
    case marsDouble = "mars_double"

    /// Icone name `ban` format `ban`
    case ban = "ban"

    /// Icone name `Lizard (Hand)` format `lizard_(hand)`
    case lizardHand = "lizard_(hand)"

    /// Icone name `Smoking Ban` format `smoking_ban`
    case smokingBan = "smoking_ban"

    /// Icone name `Less Than Equal To` format `less_than_equal_to`
    case lessThanEqualTo = "less_than_equal_to"

    /// Icone name `Alternate Sort Numeric Up` format `alternate_sort_numeric_up`
    case alternateSortNumericUp = "alternate_sort_numeric_up"

    /// Icone name `Robot` format `robot`
    case robot = "robot"

    /// Icone name `Ring` format `ring`
    case ring = "ring"

    /// Icone name `Caret Square Down` format `caret_square_down`
    case caretSquareDown = "caret_square_down"

    /// Icone name `Fax` format `fax`
    case fax = "fax"

    /// Icone name `Comments Dollar` format `comments_dollar`
    case commentsDollar = "comments_dollar"

    /// Icone name `reply-all` format `reply-all`
    case replyAll = "reply-all"

    /// Icone name `cogs` format `cogs`
    case cogs = "cogs"

    /// Icone name `Alternate Arrows Horizontal` format `alternate_arrows_horizontal`
    case alternateArrowsHorizontal = "alternate_arrows_horizontal"

    /// Icone name `qrcode` format `qrcode`
    case qrcode = "qrcode"

    /// Icone name `Mars` format `mars`
    case mars = "mars"

    /// Icone name `Directions` format `directions`
    case directions = "directions"

    /// Icone name `Bars` format `bars`
    case bars = "bars"

    /// Icone name `Dumpster` format `dumpster`
    case dumpster = "dumpster"

    /// Icone name `Balance Scale (Left-Weighted)` format `balance_scale_(left-weighted)`
    case balanceScaleLeftWeighted = "balance_scale_(left-weighted)"

    /// Icone name `Chevron Circle Left` format `chevron_circle_left`
    case chevronCircleLeft = "chevron_circle_left"

    /// Icone name `magic` format `magic`
    case magic = "magic"

    /// Icone name `Microscope` format `microscope`
    case microscope = "microscope"

    /// Icone name `Dragon` format `dragon`
    case dragon = "dragon"

    /// Icone name `User Cog` format `user_cog`
    case userCog = "user_cog"

    /// Icone name `book` format `book`
    case book = "book"

    /// Icone name `Warehouse` format `warehouse`
    case warehouse = "warehouse"

    /// Icone name `Alternate Long Arrow Right` format `alternate_long_arrow_right`
    case alternateLongArrowRight = "alternate_long_arrow_right"

    /// Icone name `Cowboy Hat Side` format `cowboy_hat_side`
    case cowboyHatSide = "cowboy_hat_side"

    /// Icone name `Alternate Grinning Face` format `alternate_grinning_face`
    case alternateGrinningFace = "alternate_grinning_face"

    /// Icone name `Poll` format `poll`
    case poll = "poll"

    /// Icone name `Radiation` format `radiation`
    case radiation = "radiation"

    /// Icone name `Venus Double` format `venus_double`
    case venusDouble = "venus_double"

    /// Icone name `Anchor` format `anchor`
    case anchor = "anchor"

    /// Icone name `text-height` format `text-height`
    case textHeight = "text-height"

    /// Icone name `Folder Open` format `folder_open`
    case folderOpen = "folder_open"

    /// Icone name `Utensil Spoon` format `utensil_spoon`
    case utensilSpoon = "utensil_spoon"

    /// Icone name `File Signature` format `file_signature`
    case fileSignature = "file_signature"

    /// Icone name `Greater Than Equal To` format `greater_than_equal_to`
    case greaterThanEqualTo = "greater_than_equal_to"

    /// Icone name `Graduation Cap` format `graduation_cap`
    case graduationCap = "graduation_cap"

    /// Icone name `bold` format `bold`
    case bold = "bold"

    /// Icone name `Thermometer 3/4 Full` format `thermometer_3/4_full`
    case thermometer34Full = "thermometer_3/4_full"

    /// Icone name `Bone` format `bone`
    case bone = "bone"

    /// Icone name `Mitten` format `mitten`
    case mitten = "mitten"

    /// Icone name `Alternate Trash` format `alternate_trash`
    case alternateTrash = "alternate_trash"

    /// Icone name `Podcast` format `podcast`
    case podcast = "podcast"

    /// Icone name `Stop Circle` format `stop_circle`
    case stopCircle = "stop_circle"

    /// Icone name `Crown` format `crown`
    case crown = "crown"

    /// Icone name `paragraph` format `paragraph`
    case paragraph = "paragraph"

    /// Icone name `Headset` format `headset`
    case headset = "headset"

    /// Icone name `Alternate Tablet` format `alternate_tablet`
    case alternateTablet = "alternate_tablet"

    /// Icone name `rocket` format `rocket`
    case rocket = "rocket"

    /// Icone name `Synagogue` format `synagogue`
    case synagogue = "synagogue"

    /// Icone name `Laptop Medical` format `laptop_medical`
    case laptopMedical = "laptop_medical"

    /// Icone name `Grip Vertical` format `grip_vertical`
    case gripVertical = "grip_vertical"

    /// Icone name `Landmark` format `landmark`
    case landmark = "landmark"

    /// Icone name `Kaaba` format `kaaba`
    case kaaba = "kaaba"

    /// Icone name `Dog` format `dog`
    case dog = "dog"

    /// Icone name `Exclamation Triangle` format `exclamation_triangle`
    case exclamationTriangle = "exclamation_triangle"

    /// Icone name `Thermometer 1/2 Full` format `thermometer_1/2_full`
    case thermometer12Full = "thermometer_1/2_full"

    /// Icone name `Skull` format `skull`
    case skull = "skull"

    /// Icone name `Republican` format `republican`
    case republican = "republican"

    /// Icone name `Pointer (Hand)` format `pointer_(hand)`
    case pointerHand = "pointer_(hand)"

    /// Icone name `Hockey Puck` format `hockey_puck`
    case hockeyPuck = "hockey_puck"

    /// Icone name `tint` format `tint`
    case tint = "tint"

    /// Icone name `Envelope Open` format `envelope_open`
    case envelopeOpen = "envelope_open"

    /// Icone name `Gifts` format `gifts`
    case gifts = "gifts"

    /// Icone name `File CSV` format `file_csv`
    case fileCSV = "file_csv"

    /// Icone name `Vials` format `vials`
    case vials = "vials"

    /// Icone name `Chess Knight` format `chess_knight`
    case chessKnight = "chess_knight"

    /// Icone name `Crosshairs` format `crosshairs`
    case crosshairs = "crosshairs"

    /// Icone name `asterisk` format `asterisk`
    case asterisk = "asterisk"

    /// Icone name `Alternate External Link Square` format `alternate_external_link_square`
    case alternateExternalLinkSquare = "alternate_external_link_square"

    /// Icone name `Poll H` format `poll_h`
    case pollH = "poll_h"

    /// Icone name `Copyright` format `copyright`
    case copyright = "copyright"

    /// Icone name `Screwdriver` format `screwdriver`
    case screwdriver = "screwdriver"

    /// Icone name `Ankh` format `ankh`
    case ankh = "ankh"

    /// Icone name `road` format `road`
    case road = "road"

    /// Icone name `Compress` format `compress`
    case compress = "compress"

    /// Icone name `Pen Square` format `pen_square`
    case penSquare = "pen_square"

    /// Icone name `unlink` format `unlink`
    case unlink = "unlink"

    /// Icone name `Alternate File` format `alternate_file`
    case alternateFile = "alternate_file"

    /// Icone name `Wine Glass` format `wine_glass`
    case wineGlass = "wine_glass"

    /// Icone name `bell` format `bell`
    case bell = "bell"

    /// Icone name `File Invoice` format `file_invoice`
    case fileInvoice = "file_invoice"

    /// Icone name `Ship` format `ship`
    case ship = "ship"

    /// Icone name `Industry` format `industry`
    case industry = "industry"

    /// Icone name `Terminal` format `terminal`
    case terminal = "terminal"

    /// Icone name `tablet` format `tablet`
    case tablet = "tablet"

    /// Icone name `Drumstick with Bite Taken Out` format `drumstick_with_bite_taken_out`
    case drumstickWithBiteTakenOut = "drumstick_with_bite_taken_out"

    /// Icone name `Receipt` format `receipt`
    case receipt = "receipt"

    /// Icone name `Thermometer Full` format `thermometer_full`
    case thermometerFull = "thermometer_full"

    /// Icone name `Atom` format `atom`
    case atom = "atom"

    /// Icone name `Object Group` format `object_group`
    case objectGroup = "object_group"

    /// Icone name `Money Check` format `money_check`
    case moneyCheck = "money_check"

    /// Icone name `Hot Dog` format `hot_dog`
    case hotDog = "hot_dog"

    /// Icone name `Swimmer` format `swimmer`
    case swimmer = "swimmer"

    /// Icone name `Binoculars` format `binoculars`
    case binoculars = "binoculars"

    /// Icone name `Arrow Circle Down` format `arrow_circle_down`
    case arrowCircleDown = "arrow_circle_down"

    /// Icone name `Smiling Face With Heart-Eyes` format `smiling_face_with_heart-eyes`
    case smilingFaceWithHeartEyes = "smiling_face_with_heart-eyes"

    /// Icone name `Not Equal` format `not_equal`
    case notEqual = "not_equal"

    /// Icone name `Spider` format `spider`
    case spider = "spider"

    /// Icone name `Plane Departure` format `plane_departure`
    case planeDeparture = "plane_departure"

    /// Icone name `Alternate Calendar` format `alternate_calendar`
    case alternateCalendar = "alternate_calendar"

    /// Icone name `Grinning Face With Smiling Eyes` format `grinning_face_with_smiling_eyes`
    case grinningFaceWithSmilingEyes = "grinning_face_with_smiling_eyes"

    /// Icone name `Drum Steelpan` format `drum_steelpan`
    case drumSteelpan = "drum_steelpan"

    /// Icone name `Vector Square` format `vector_square`
    case vectorSquare = "vector_square"

    /// Icone name `Gopuram` format `gopuram`
    case gopuram = "gopuram"

    /// Icone name `Swimming Pool` format `swimming_pool`
    case swimmingPool = "swimming_pool"

    /// Icone name `Otter` format `otter`
    case otter = "otter"

    /// Icone name `Tractor` format `tractor`
    case tractor = "tractor"

    /// Icone name `Calendar Check` format `calendar_check`
    case calendarCheck = "calendar_check"

    /// Icone name `Car Side` format `car_side`
    case carSide = "car_side"

    /// Icone name `Medical File` format `medical_file`
    case medicalFile = "medical_file"

    /// Icone name `Arrow Circle Right` format `arrow_circle_right`
    case arrowCircleRight = "arrow_circle_right"

    /// Icone name `Thermometer Empty` format `thermometer_empty`
    case thermometerEmpty = "thermometer_empty"

    /// Icone name `Vote Yea` format `vote_yea`
    case voteYea = "vote_yea"

    /// Icone name `Bell Slash` format `bell_slash`
    case bellSlash = "bell_slash"

    /// Icone name `list-ol` format `list-ol`
    case listOl = "list-ol"

    /// Icone name `Wheelchair` format `wheelchair`
    case wheelchair = "wheelchair"

    /// Icone name `Menorah` format `menorah`
    case menorah = "menorah"

    /// Icone name `Blog` format `blog`
    case blog = "blog"

    /// Icone name `Puzzle Piece` format `puzzle_piece`
    case puzzlePiece = "puzzle_piece"

    /// Icone name `Neutral Face` format `neutral_face`
    case neutralFace = "neutral_face"

    /// Icone name `magnet` format `magnet`
    case magnet = "magnet"

    /// Icone name `Toilet` format `toilet`
    case toilet = "toilet"

    /// Icone name `DNA` format `dna`
    case dna = "dna"

    /// Icone name `chevron-right` format `chevron-right`
    case chevronRight = "chevron-right"

    /// Icone name `Info Circle` format `info_circle`
    case infoCircle = "info_circle"

    /// Icone name `WiFi` format `wifi`
    case wiFi = "wifi"

    /// Icone name `Sd Card` format `sd_card`
    case sdCard = "sd_card"

    /// Icone name `Air Freshener` format `air_freshener`
    case airFreshener = "air_freshener"

    /// Icone name `Server` format `server`
    case server = "server"

    /// Icone name `Pallet` format `pallet`
    case pallet = "pallet"

    /// Icone name `plane` format `plane`
    case plane = "plane"

    /// Icone name `Mars Stroke Vertical` format `mars_stroke_vertical`
    case marsStrokeVertical = "mars_stroke_vertical"

    /// Icone name `Alternate Money Bill` format `alternate_money_bill`
    case alternateMoneyBill = "alternate_money_bill"

    /// Icone name `Thumbtack` format `thumbtack`
    case thumbtack = "thumbtack"

    /// Icone name `Hot Pepper` format `hot_pepper`
    case hotPepper = "hot_pepper"

    /// Icone name `Dice Three` format `dice_three`
    case diceThree = "dice_three"

    /// Icone name `superscript` format `superscript`
    case superscript = "superscript"

    /// Icone name `Retro Camera` format `retro_camera`
    case retroCamera = "retro_camera"

    /// Icone name `Snowplow` format `snowplow`
    case snowplow = "snowplow"

    /// Icone name `Female` format `female`
    case female = "female"

    /// Icone name `Sort Up (Ascending)` format `sort_up_(ascending)`
    case sortUpAscending = "sort_up_(ascending)"

    /// Icone name `Star-Struck` format `star-struck`
    case starStruck = "star-struck"

    /// Icone name `Alternate Map Marker` format `alternate_map_marker`
    case alternateMapMarker = "alternate_map_marker"

    /// Icone name `Paw` format `paw`
    case paw = "paw"

    /// Icone name `Newspaper` format `newspaper`
    case newspaper = "newspaper"

    /// Icone name `Film` format `film`
    case film = "film"

    /// Icone name `Alternate Exchange` format `alternate_exchange`
    case alternateExchange = "alternate_exchange"

    /// Icone name `Sticky Note` format `sticky_note`
    case stickyNote = "sticky_note"

    /// Icone name `Signature` format `signature`
    case signature = "signature"

    /// Icone name `Horizontal Ellipsis` format `horizontal_ellipsis`
    case horizontalEllipsis = "horizontal_ellipsis"

    /// Icone name `Alternate Sort Alphabetical Up` format `alternate_sort_alphabetical_up`
    case alternateSortAlphabeticalUp = "alternate_sort_alphabetical_up"

    /// Icone name `arrow-right` format `arrow-right`
    case arrowRight = "arrow-right"

    /// Icone name `Angle Double Left` format `angle_double_left`
    case angleDoubleLeft = "angle_double_left"

    /// Icone name `File Upload` format `file_upload`
    case fileUpload = "file_upload"

    /// Icone name `User Minus` format `user_minus`
    case userMinus = "user_minus"

    /// Icone name `Eye Dropper` format `eye_dropper`
    case eyeDropper = "eye_dropper"

    /// Icone name `Icicles` format `icicles`
    case icicles = "icicles"

    /// Icone name `Quidditch` format `quidditch`
    case quidditch = "quidditch"

    /// Icone name `Wallet` format `wallet`
    case wallet = "wallet"

    /// Icone name `Indian Rupee Sign` format `indian_rupee_sign`
    case indianRupeeSign = "indian_rupee_sign"

    /// Icone name `User Check` format `user_check`
    case userCheck = "user_check"

    /// Icone name `HDD` format `hdd`
    case hdd = "hdd"

    /// Icone name `Reply` format `reply`
    case reply = "reply"

    /// Icone name `Plug` format `plug`
    case plug = "plug"

    /// Icone name `Mask` format `mask`
    case mask = "mask"

    /// Icone name `Alternate Sort Alphabetical Down` format `alternate_sort_alphabetical_down`
    case alternateSortAlphabeticalDown = "alternate_sort_alphabetical_down"

    /// Icone name `Om` format `om`
    case om = "om"

    /// Icone name `Holly Berry` format `holly_berry`
    case hollyBerry = "holly_berry"

    /// Icone name `Exclamation Circle` format `exclamation_circle`
    case exclamationCircle = "exclamation_circle"

    /// Icone name `Rock (Hand)` format `rock_(hand)`
    case rockHand = "rock_(hand)"

    /// Icone name `Torah` format `torah`
    case torah = "torah"

    /// Icone name `Bed` format `bed`
    case bed = "bed"

    /// Icone name `Bacon` format `bacon`
    case bacon = "bacon"

    /// Icone name `Alternate Money Check` format `alternate_money_check`
    case alternateMoneyCheck = "alternate_money_check"

    /// Icone name `Shuttle Van` format `shuttle_van`
    case shuttleVan = "shuttle_van"

    /// Icone name `Utensils` format `utensils`
    case utensils = "utensils"

    /// Icone name `Torii Gate` format `torii_gate`
    case toriiGate = "torii_gate"

    /// Icone name `Wine Bottle` format `wine_bottle`
    case wineBottle = "wine_bottle"

    /// Icone name `Shopping Bag` format `shopping_bag`
    case shoppingBag = "shopping_bag"

    /// Icone name `Marker` format `marker`
    case marker = "marker"

    /// Icone name `Door Closed` format `door_closed`
    case doorClosed = "door_closed"

    /// Icone name `Phone Square` format `phone_square`
    case phoneSquare = "phone_square"

    /// Icone name `eraser` format `eraser`
    case eraser = "eraser"

    /// Icone name `Book Reader` format `book_reader`
    case bookReader = "book_reader"

    /// Icone name `Envelope Square` format `envelope_square`
    case envelopeSquare = "envelope_square"

    /// Icone name `Percent` format `percent`
    case percent = "percent"

    /// Icone name `Wrench` format `wrench`
    case wrench = "wrench"

    /// Icone name `Alternate Star Half` format `alternate_star_half`
    case alternateStarHalf = "alternate_star_half"

    /// Icone name `Calendar Times` format `calendar_times`
    case calendarTimes = "calendar_times"

    /// Icone name `Door Open` format `door_open`
    case doorOpen = "door_open"

    /// Icone name `Alternate User Slash` format `alternate_user_slash`
    case alternateUserSlash = "alternate_user_slash"

    /// Icone name `Sort Numeric Up` format `sort_numeric_up`
    case sortNumericUp = "sort_numeric_up"

    /// Icone name `Paper (Hand)` format `paper_(hand)`
    case paperHand = "paper_(hand)"

    /// Icone name `Remove User` format `remove_user`
    case removeUser = "remove_user"

    /// Icone name `Band-Aid` format `band-aid`
    case bandAid = "band-aid"

    /// Icone name `File Export` format `file_export`
    case fileExport = "file_export"

    /// Icone name `Laptop` format `laptop`
    case laptop = "laptop"

    /// Icone name `Luggage Cart` format `luggage_cart`
    case luggageCart = "luggage_cart"

    /// Icone name `Search Plus` format `search_plus`
    case searchPlus = "search_plus"

    /// Icone name `Won Sign` format `won_sign`
    case wonSign = "won_sign"

    /// Icone name `Fill Drip` format `fill_drip`
    case fillDrip = "fill_drip"

    /// Icone name `Hourglass` format `hourglass`
    case hourglass = "hourglass"

    /// Icone name `forward` format `forward`
    case forward = "forward"

    /// Icone name `Hashtag` format `hashtag`
    case hashtag = "hashtag"

    /// Icone name `Microchip` format `microchip`
    case microchip = "microchip"

    /// Icone name `Peace` format `peace`
    case peace = "peace"

    /// Icone name `Fruit Apple` format `fruit_apple`
    case fruitApple = "fruit_apple"

    /// Icone name `Stethoscope` format `stethoscope`
    case stethoscope = "stethoscope"

    /// Icone name `Infinity` format `infinity`
    case infinity = "infinity"

    /// Icone name `Alternate Arrow Circle Down` format `alternate_arrow_circle_down`
    case alternateArrowCircleDown = "alternate_arrow_circle_down"

    /// Icone name `Check` format `check`
    case check = "check"

    /// Icone name `Egg` format `egg`
    case egg = "egg"

    /// Icone name `leaf` format `leaf`
    case leaf = "leaf"

    /// Icone name `Sun` format `sun`
    case sun = "sun"

    /// Icone name `Heart` format `heart`
    case heart = "heart"

    /// Icone name `Scroll` format `scroll`
    case scroll = "scroll"

    /// Icone name `Alternate User` format `alternate_user`
    case alternateUser = "alternate_user"

    /// Icone name `Alternate Prescription Bottle` format `alternate_prescription_bottle`
    case alternatePrescriptionBottle = "alternate_prescription_bottle"

    /// Icone name `Filter` format `filter`
    case filter = "filter"

    /// Icone name `Backspace` format `backspace`
    case backspace = "backspace"

    /// Icone name `Box` format `box`
    case box = "box"

    /// Icone name `Grip Lines` format `grip_lines`
    case gripLines = "grip_lines"

    /// Icone name `Clock` format `clock`
    case clock = "clock"

    /// Icone name `camera` format `camera`
    case camera = "camera"

    /// Icone name `Cube` format `cube`
    case cube = "cube"

    /// Icone name `Indent` format `indent`
    case indent = "indent"

    /// Icone name `Teeth` format `teeth`
    case teeth = "teeth"

    /// Icone name `Medical Book` format `medical_book`
    case medicalBook = "medical_book"

    /// Icone name `chevron-down` format `chevron-down`
    case chevronDown = "chevron-down"

    /// Icone name `Skating` format `skating`
    case skating = "skating"

    /// Icone name `Music` format `music`
    case music = "music"

    /// Icone name `angle-down` format `angle-down`
    case angleDown = "angle-down"

    /// Icone name `Ghost` format `ghost`
    case ghost = "ghost"

    /// Icone name `Space Shuttle` format `space_shuttle`
    case spaceShuttle = "space_shuttle"

    /// Icone name `Play Circle` format `play_circle`
    case playCircle = "play_circle"

    /// Icone name `Dollar Sign` format `dollar_sign`
    case dollarSign = "dollar_sign"

    /// Icone name `Biohazard` format `biohazard`
    case biohazard = "biohazard"

    /// Icone name `Umbrella` format `umbrella`
    case umbrella = "umbrella"

    /// Icone name `Tasks` format `tasks`
    case tasks = "tasks"

    /// Icone name `Crow` format `crow`
    case crow = "crow"

    /// Icone name `Cloud with (a chance of) Meatball` format `cloud_with_(a_chance_of)_meatball`
    case cloudWithAChanceOfMeatball = "cloud_with_(a_chance_of)_meatball"

    /// Icone name `Code` format `code`
    case code = "code"

    /// Icone name `Globe with Europe shown` format `globe_with_europe_shown`
    case globeWithEuropeShown = "globe_with_europe_shown"

    /// Icone name `Shapes` format `shapes`
    case shapes = "shapes"

    /// Icone name `Squinting Face With Tongue` format `squinting_face_with_tongue`
    case squintingFaceWithTongue = "squinting_face_with_tongue"

    /// Icone name `People Carry` format `people_carry`
    case peopleCarry = "people_carry"

    /// Icone name `Code Branch` format `code_branch`
    case codeBranch = "code_branch"

    /// Icone name `Edit` format `edit`
    case edit = "edit"

    /// Icone name `Sort Alphabetical Up` format `sort_alphabetical_up`
    case sortAlphabeticalUp = "sort_alphabetical_up"

    /// Icone name `Phone Slash` format `phone_slash`
    case phoneSlash = "phone_slash"

    /// Icone name `Sleigh` format `sleigh`
    case sleigh = "sleigh"

    /// Icone name `Mountain` format `mountain`
    case mountain = "mountain"

    /// Icone name `thumbs-up` format `thumbs-up`
    case thumbsUp = "thumbs-up"

    /// Icone name `User Secret` format `user_secret`
    case userSecret = "user_secret"

    /// Icone name `Highlighter` format `highlighter`
    case highlighter = "highlighter"

    /// Icone name `Volume Mute` format `volume_mute`
    case volumeMute = "volume_mute"

    /// Icone name `Caret Square Up` format `caret_square_up`
    case caretSquareUp = "caret_square_up"

    /// Icone name `Water` format `water`
    case water = "water"

    /// Icone name `Yen Sign` format `yen_sign`
    case yenSign = "yen_sign"

    /// Icone name `Rainbow` format `rainbow`
    case rainbow = "rainbow"

    /// Icone name `Feather` format `feather`
    case feather = "feather"

    /// Icone name `Alternate Sign Out` format `alternate_sign_out`
    case alternateSignOut = "alternate_sign_out"

    /// Icone name `Window Minimize` format `window_minimize`
    case windowMinimize = "window_minimize"

    /// Icone name `Parachute Box` format `parachute_box`
    case parachuteBox = "parachute_box"

    /// Icone name `Alternate Identification Card` format `alternate_identification_card`
    case alternateIdentificationCard = "alternate_identification_card"

    /// Icone name `Beaming Face With Smiling Eyes` format `beaming_face_with_smiling_eyes`
    case beamingFaceWithSmilingEyes = "beaming_face_with_smiling_eyes"

    /// Icone name `Sign` format `sign`
    case sign = "sign"

    /// Icone name `Alternate Cloud Download` format `alternate_cloud_download`
    case alternateCloudDownload = "alternate_cloud_download"

    /// Icone name `Place of Worship` format `place_of_worship`
    case placeOfWorship = "place_of_worship"

    /// Icone name `Square` format `square`
    case square = "square"

    /// Icone name `Alternate Compress Arrows` format `alternate_compress_arrows`
    case alternateCompressArrows = "alternate_compress_arrows"

    /// Icone name `Calendar with Week Focus` format `calendar_with_week_focus`
    case calendarWithWeekFocus = "calendar_with_week_focus"

    /// Icone name `Journal of the Whills` format `journal_of_the_whills`
    case journalOfTheWhills = "journal_of_the_whills"

    /// Icone name `star-half` format `star-half`
    case starHalf = "star-half"

    /// Icone name `Images` format `images`
    case images = "images"

    /// Icone name `cog` format `cog`
    case cog = "cog"

    /// Icone name `align-justify` format `align-justify`
    case alignJustify = "align-justify"

    /// Icone name `crop` format `crop`
    case crop = "crop"

    /// Icone name `Globe with Africa shown` format `globe_with_africa_shown`
    case globeWithAfricaShown = "globe_with_africa_shown"

    /// Icone name `Dolly Flatbed` format `dolly_flatbed`
    case dollyFlatbed = "dolly_flatbed"

    /// Icone name `Angle Double Down` format `angle_double_down`
    case angleDoubleDown = "angle_double_down"

    /// Icone name `Sync` format `sync`
    case sync = "sync"

    /// Icone name `Phone` format `phone`
    case phone = "phone"

    /// Icone name `Address Book` format `address_book`
    case addressBook = "address_book"

    /// Icone name `Stamp` format `stamp`
    case stamp = "stamp"

    /// Icone name `Double Check` format `double_check`
    case doubleCheck = "double_check"

    /// Icone name `Monument` format `monument`
    case monument = "monument"

    /// Icone name `Mouse Pointer` format `mouse_pointer`
    case mousePointer = "mouse_pointer"

    /// Icone name `Universal Access` format `universal_access`
    case universalAccess = "universal_access"

    /// Icone name `User Lock` format `user_lock`
    case userLock = "user_lock"

    /// Icone name `Pen Fancy` format `pen_fancy`
    case penFancy = "pen_fancy"

    /// Icone name `Line Chart` format `line_chart`
    case lineChart = "line_chart"

    /// Icone name `Taxi` format `taxi`
    case taxi = "taxi"

    /// Icone name `Calendar with Day Focus` format `calendar_with_day_focus`
    case calendarWithDayFocus = "calendar_with_day_focus"

    /// Icone name `Medical Briefcase` format `medical_briefcase`
    case medicalBriefcase = "medical_briefcase"

    /// Icone name `align-left` format `align-left`
    case alignLeft = "align-left"

    /// Icone name `Trash Restore` format `trash_restore`
    case trashRestore = "trash_restore"

    /// Icone name `Power Off` format `power_off`
    case powerOff = "power_off"

    /// Icone name `Alternate Arrows` format `alternate_arrows`
    case alternateArrows = "alternate_arrows"

    /// Icone name `Clipboard List` format `clipboard_list`
    case clipboardList = "clipboard_list"

    /// Icone name `quote-left` format `quote-left`
    case quoteLeft = "quote-left"

    /// Icone name `Poo Storm` format `poo_storm`
    case pooStorm = "poo_storm"

    /// Icone name `angle-up` format `angle-up`
    case angleUp = "angle-up"

    /// Icone name `Alternate Fire` format `alternate_fire`
    case alternateFire = "alternate_fire"

    /// Icone name `adjust` format `adjust`
    case adjust = "adjust"

    /// Icone name `comments` format `comments`
    case comments = "comments"

    /// Icone name `Donate` format `donate`
    case donate = "donate"

    /// Icone name `Male` format `male`
    case male = "male"

    /// Icone name `italic` format `italic`
    case italic = "italic"

    /// Icone name `File Invoice with US Dollar` format `file_invoice_with_us_dollar`
    case fileInvoiceWithUSDollar = "file_invoice_with_us_dollar"

    /// Icone name `Minus Circle` format `minus_circle`
    case minusCircle = "minus_circle"

    /// Icone name `Project Diagram` format `project_diagram`
    case projectDiagram = "project_diagram"

    /// Icone name `Smog` format `smog`
    case smog = "smog"

    /// Icone name `SMS` format `sms`
    case sms = "sms"

    /// Icone name `Mars Stroke` format `mars_stroke`
    case marsStroke = "mars_stroke"

    /// Icone name `Share` format `share`
    case share = "share"

    /// Icone name `Closed Captioning` format `closed_captioning`
    case closedCaptioning = "closed_captioning"

    /// Icone name `Skull & Crossbones` format `skull_&_crossbones`
    case skullCrossbones = "skull_&_crossbones"

    /// Icone name `Subway` format `subway`
    case subway = "subway"

    /// Icone name `Comment Dots` format `comment_dots`
    case commentDots = "comment_dots"

    /// Icone name `list-ul` format `list-ul`
    case listUl = "list-ul"

    /// Icone name `Horse Head` format `horse_head`
    case horseHead = "horse_head"

    /// Icone name `Face With Rolling Eyes` format `face_with_rolling_eyes`
    case faceWithRollingEyes = "face_with_rolling_eyes"

    /// Icone name `Square Wave` format `square_wave`
    case squareWave = "square_wave"

    /// Icone name `Hammer` format `hammer`
    case hammer = "hammer"

    /// Icone name `Burn` format `burn`
    case burn = "burn"

    /// Icone name `headphones` format `headphones`
    case headphones = "headphones"

    /// Icone name `Skiing` format `skiing`
    case skiing = "skiing"

    /// Icone name `Caret Square Left` format `caret_square_left`
    case caretSquareLeft = "caret_square_left"

    /// Icone name `Bar Chart` format `bar_chart`
    case barChart = "bar_chart"

    /// Icone name `Loudly Crying Face` format `loudly_crying_face`
    case loudlyCryingFace = "loudly_crying_face"

    /// Icone name `Tint Slash` format `tint_slash`
    case tintSlash = "tint_slash"

    /// Icone name `th-large` format `th-large`
    case thLarge = "th-large"

    /// Icone name `Caret Down` format `caret_down`
    case caretDown = "caret_down"

    /// Icone name `Question Circle` format `question_circle`
    case questionCircle = "question_circle"

    /// Icone name `Procedures` format `procedures`
    case procedures = "procedures"

    /// Icone name `Battery Full` format `battery_full`
    case batteryFull = "battery_full"

    /// Icone name `Alternate List` format `alternate_list`
    case alternateList = "alternate_list"

    /// Icone name `Window Close` format `window_close`
    case windowClose = "window_close"

    /// Icone name `Circle` format `circle`
    case circle = "circle"

    /// Icone name `Medical Notes` format `medical_notes`
    case medicalNotes = "medical_notes"

    /// Icone name `Border All` format `border_all`
    case borderAll = "border_all"

    /// Icone name `Cookie Bite` format `cookie_bite`
    case cookieBite = "cookie_bite"

    /// Icone name `Cloud` format `cloud`
    case cloud = "cloud"

    /// Icone name `Grinning Winking Face` format `grinning_winking_face`
    case grinningWinkingFace = "grinning_winking_face"

    /// Icone name `Pen` format `pen`
    case pen = "pen"

    /// Icone name `Cheese` format `cheese`
    case cheese = "cheese"

    /// Icone name `Code File` format `code_file`
    case codeFile = "code_file"

    /// Icone name `User Shield` format `user_shield`
    case userShield = "user_shield"

    /// Icone name `Nurse` format `nurse`
    case nurse = "nurse"

    /// Icone name `Cloud with Sun and Rain` format `cloud_with_sun_and_rain`
    case cloudWithSunAndRain = "cloud_with_sun_and_rain"

    /// Icone name `Star` format `star`
    case star = "star"

    /// Icone name `stop` format `stop`
    case stop = "stop"

    /// Icone name `User` format `user`
    case user = "user"

    /// Icone name `Bomb` format `bomb`
    case bomb = "bomb"

    /// Icone name `Raised Fist` format `raised_fist`
    case raisedFist = "raised_fist"

    /// Icone name `User Astronaut` format `user_astronaut`
    case userAstronaut = "user_astronaut"

    /// Icone name `Poop` format `poop`
    case poop = "poop"

    /// Icone name `Alternate Sort Numeric Down` format `alternate_sort_numeric_down`
    case alternateSortNumericDown = "alternate_sort_numeric_down"

    /// Icone name `Archway` format `archway`
    case archway = "archway"

    /// Icone name `Shopping Cart Arrow Down` format `shopping_cart_arrow_down`
    case shoppingCartArrowDown = "shopping_cart_arrow_down"

    /// Icone name `Draw Polygon` format `draw_polygon`
    case drawPolygon = "draw_polygon"

    /// Icone name `File Import` format `file_import`
    case fileImport = "file_import"

    /// Icone name `File Download` format `file_download`
    case fileDownload = "file_download"

    /// Icone name `minus` format `minus`
    case minus = "minus"

    /// Icone name `Question` format `question`
    case question = "question"

    /// Icone name `Tooth` format `tooth`
    case tooth = "tooth"

    /// Icone name `Turkish Lira Sign` format `turkish_lira_sign`
    case turkishLiraSign = "turkish_lira_sign"

    /// Icone name `Archive File` format `archive_file`
    case archiveFile = "archive_file"

    /// Icone name `Socks` format `socks`
    case socks = "socks"

    /// Icone name `Eye Slash` format `eye_slash`
    case eyeSlash = "eye_slash"

    /// Icone name `angle-right` format `angle-right`
    case angleRight = "angle-right"

    /// Icone name `Chevron Circle Right` format `chevron_circle_right`
    case chevronCircleRight = "chevron_circle_right"

    /// Icone name `Kissing Face` format `kissing_face`
    case kissingFace = "kissing_face"

    /// Icone name `Star of David` format `star_of_david`
    case starOfDavid = "star_of_david"

    /// Icone name `barcode` format `barcode`
    case barcode = "barcode"

    /// Icone name `Plus Circle` format `plus_circle`
    case plusCircle = "plus_circle"

    /// Icone name `Users` format `users`
    case users = "users"

    /// Icone name `Biking` format `biking`
    case biking = "biking"

    /// Icone name `Laugh Face with Beaming Eyes` format `laugh_face_with_beaming_eyes`
    case laughFaceWithBeamingEyes = "laugh_face_with_beaming_eyes"

    /// Icone name `Paste` format `paste`
    case paste = "paste"

    /// Icone name `Building` format `building`
    case building = "building"

    /// Icone name `Download` format `download`
    case download = "download"

    /// Icone name `Layer Group` format `layer_group`
    case layerGroup = "layer_group"

    /// Icone name `Coffee` format `coffee`
    case coffee = "coffee"

    /// Icone name `Folder Plus` format `folder_plus`
    case folderPlus = "folder_plus"

    /// Icone name `United States of America Flag` format `united_states_of_america_flag`
    case unitedStatesOfAmericaFlag = "united_states_of_america_flag"

    /// Icone name `Horse` format `horse`
    case horse = "horse"

    /// Icone name `Blind` format `blind`
    case blind = "blind"

    /// Icone name `Walking` format `walking`
    case walking = "walking"

    /// Icone name `Alternate Microphone` format `alternate_microphone`
    case alternateMicrophone = "alternate_microphone"

    /// Icone name `City` format `city`
    case city = "city"

    /// Icone name `Prescription` format `prescription`
    case prescription = "prescription"

    /// Icone name `Spell Check` format `spell_check`
    case spellCheck = "spell_check"

    /// Icone name `Alternate Glass Martini` format `alternate_glass_martini`
    case alternateGlassMartini = "alternate_glass_martini"

    /// Icone name `truck` format `truck`
    case truck = "truck"


    public var unicode: String {
        switch self {
        /// Code `f478` for name `Alternate Medical File` with format `alternate_medical_file`
        case .alternateMedicalFile: return "\u{f478}"

        /// Code `f12a` for name `exclamation` with format `exclamation`
        case .exclamation: return "\u{f12a}"

        /// Code `f51f` for name `Compact Disc` with format `compact_disc`
        case .compactDisc: return "\u{f51f}"

        /// Code `f48e` for name `Syringe` with format `syringe`
        case .syringe: return "\u{f48e}"

        /// Code `f1fe` for name `Area Chart` with format `area_chart`
        case .areaChart: return "\u{f1fe}"

        /// Code `f594` for name `Hotel` with format `hotel`
        case .hotel: return "\u{f594}"

        /// Code `f65d` for name `Folder Minus` with format `folder_minus`
        case .folderMinus: return "\u{f65d}"

        /// Code `f04a` for name `backward` with format `backward`
        case .backward: return "\u{f04a}"

        /// Code `f5c2` for name `Hushed Face` with format `hushed_face`
        case .hushedFace: return "\u{f5c2}"

        /// Code `f049` for name `fast-backward` with format `fast-backward`
        case .fastBackward: return "\u{f049}"

        /// Code `f259` for name `Spock (Hand)` with format `spock_(hand)`
        case .spockHand: return "\u{f259}"

        /// Code `f583` for name `Grinning Face With Sweat` with format `grinning_face_with_sweat`
        case .grinningFaceWithSweat: return "\u{f583}"

        /// Code `f03a` for name `List` with format `list`
        case .list: return "\u{f03a}"

        /// Code `f35d` for name `Alternate External Link` with format `alternate_external_link`
        case .alternateExternalLink: return "\u{f35d}"

        /// Code `f130` for name `microphone` with format `microphone`
        case .microphone: return "\u{f130}"

        /// Code `f7a0` for name `Glass Whiskey` with format `glass_whiskey`
        case .glassWhiskey: return "\u{f7a0}"

        /// Code `f06e` for name `Eye` with format `eye`
        case .eye: return "\u{f06e}"

        /// Code `f621` for name `Star of Life` with format `star_of_life`
        case .starOfLife: return "\u{f621}"

        /// Code `f662` for name `Funnel Dollar` with format `funnel_dollar`
        case .funnelDollar: return "\u{f662}"

        /// Code `f551` for name `Stroopwafel` with format `stroopwafel`
        case .stroopwafel: return "\u{f551}"

        /// Code `f0c6` for name `Paperclip` with format `paperclip`
        case .paperclip: return "\u{f0c6}"

        /// Code `f015` for name `home` with format `home`
        case .home: return "\u{f015}"

        /// Code `f3fd` for name `Alternate Tachometer` with format `alternate_tachometer`
        case .alternateTachometer: return "\u{f3fd}"

        /// Code `f22d` for name `Genderless` with format `genderless`
        case .genderless: return "\u{f22d}"

        /// Code `f5fc` for name `Laptop Code` with format `laptop_code`
        case .laptopCode: return "\u{f5fc}"

        /// Code `f769` for name `High Temperature` with format `high_temperature`
        case .highTemperature: return "\u{f769}"

        /// Code `f64a` for name `Business Time` with format `business_time`
        case .businessTime: return "\u{f64a}"

        /// Code `f02e` for name `bookmark` with format `bookmark`
        case .bookmark: return "\u{f02e}"

        /// Code `f794` for name `Dumpster Fire` with format `dumpster_fire`
        case .dumpsterFire: return "\u{f794}"

        /// Code `f6ad` for name `Yin Yang` with format `yin_yang`
        case .yinYang: return "\u{f6ad}"

        /// Code `f55b` for name `Bezier Curve` with format `bezier_curve`
        case .bezierCurve: return "\u{f55b}"

        /// Code `f04c` for name `pause` with format `pause`
        case .pause: return "\u{f04c}"

        /// Code `f25b` for name `Peace (Hand)` with format `peace_(hand)`
        case .peaceHand: return "\u{f25b}"

        /// Code `f118` for name `Smiling Face` with format `smiling_face`
        case .smilingFace: return "\u{f118}"

        /// Code `f57e` for name `Globe with Asia shown` with format `globe_with_asia_shown`
        case .globeWithAsiaShown: return "\u{f57e}"

        /// Code `f291` for name `Shopping Basket` with format `shopping_basket`
        case .shoppingBasket: return "\u{f291}"

        /// Code `f010` for name `Search Minus` with format `search_minus`
        case .searchMinus: return "\u{f010}"

        /// Code `f2ea` for name `Alternate Undo` with format `alternate_undo`
        case .alternateUndo: return "\u{f2ea}"

        /// Code `f1fd` for name `Birthday Cake` with format `birthday_cake`
        case .birthdayCake: return "\u{f1fd}"

        /// Code `f63b` for name `Truck Monster` with format `truck_monster`
        case .truckMonster: return "\u{f63b}"

        /// Code `f09c` for name `unlock` with format `unlock`
        case .unlock: return "\u{f09c}"

        /// Code `f740` for name `Cloud with Heavy Showers` with format `cloud_with_heavy_showers`
        case .cloudWithHeavyShowers: return "\u{f740}"

        /// Code `f500` for name `User Friends` with format `user_friends`
        case .userFriends: return "\u{f500}"

        /// Code `f133` for name `Calendar` with format `calendar`
        case .calendar: return "\u{f133}"

        /// Code `f02c` for name `tags` with format `tags`
        case .tags: return "\u{f02c}"

        /// Code `f5aa` for name `Paint Roller` with format `paint_roller`
        case .paintRoller: return "\u{f5aa}"

        /// Code `f6c4` for name `Cloud with Sun` with format `cloud_with_sun`
        case .cloudWithSun: return "\u{f6c4}"

        /// Code `f7a5` for name `Grip Lines Vertical` with format `grip_lines_vertical`
        case .gripLinesVertical: return "\u{f7a5}"

        /// Code `f54e` for name `Store` with format `store`
        case .store: return "\u{f54e}"

        /// Code `f4b3` for name `Comment Slash` with format `comment_slash`
        case .commentSlash: return "\u{f4b3}"

        /// Code `f82a` for name `Alternative Trash Restore` with format `alternative_trash_restore`
        case .alternativeTrashRestore: return "\u{f82a}"

        /// Code `f524` for name `Dice Four` with format `dice_four`
        case .diceFour: return "\u{f524}"

        /// Code `f6cf` for name `Dice D20` with format `dice_d20`
        case .diceD20: return "\u{f6cf}"

        /// Code `f309` for name `Alternate Long Arrow Down` with format `alternate_long_arrow_down`
        case .alternateLongArrowDown: return "\u{f309}"

        /// Code `f35b` for name `Alternate Arrow Circle Up` with format `alternate_arrow_circle_up`
        case .alternateArrowCircleUp: return "\u{f35b}"

        /// Code `f6f1` for name `Damaged House` with format `damaged_house`
        case .damagedHouse: return "\u{f6f1}"

        /// Code `f14a` for name `Check Square` with format `check_square`
        case .checkSquare: return "\u{f14a}"

        /// Code `f051` for name `step-forward` with format `step-forward`
        case .stepForward: return "\u{f051}"

        /// Code `f59b` for name `Laughing Squinting Face` with format `laughing_squinting_face`
        case .laughingSquintingFace: return "\u{f59b}"

        /// Code `f49e` for name `Box Open` with format `box_open`
        case .boxOpen: return "\u{f49e}"

        /// Code `f7d9` for name `Tools` with format `tools`
        case .tools: return "\u{f7d9}"

        /// Code `f328` for name `Clipboard` with format `clipboard`
        case .clipboard: return "\u{f328}"

        /// Code `f6a7` for name `Vihara` with format `vihara`
        case .vihara: return "\u{f6a7}"

        /// Code `f024` for name `flag` with format `flag`
        case .flag: return "\u{f024}"

        /// Code `f818` for name `Pizza Slice` with format `pizza_slice`
        case .pizzaSlice: return "\u{f818}"

        /// Code `f0d6` for name `Money Bill` with format `money_bill`
        case .moneyBill: return "\u{f0d6}"

        /// Code `f658` for name `Envelope Open-text` with format `envelope_open-text`
        case .envelopeOpenText: return "\u{f658}"

        /// Code `f25d` for name `Registered Trademark` with format `registered_trademark`
        case .registeredTrademark: return "\u{f25d}"

        /// Code `f124` for name `location-arrow` with format `location-arrow`
        case .locationArrow: return "\u{f124}"

        /// Code `f7f2` for name `Medical Clinic` with format `medical_clinic`
        case .medicalClinic: return "\u{f7f2}"

        /// Code `f3ed` for name `Alternate Shield` with format `alternate_shield`
        case .alternateShield: return "\u{f3ed}"

        /// Code `f165` for name `thumbs-down` with format `thumbs-down`
        case .thumbsDown: return "\u{f165}"

        /// Code `f06b` for name `gift` with format `gift`
        case .gift: return "\u{f06b}"

        /// Code `f1c4` for name `Powerpoint File` with format `powerpoint_file`
        case .powerpointFile: return "\u{f1c4}"

        /// Code `f26c` for name `Television` with format `television`
        case .television: return "\u{f26c}"

        /// Code `f561` for name `Cocktail` with format `cocktail`
        case .cocktail: return "\u{f561}"

        /// Code `f139` for name `Chevron Circle Up` with format `chevron_circle_up`
        case .chevronCircleUp: return "\u{f139}"

        /// Code `f0ce` for name `table` with format `table`
        case .table: return "\u{f0ce}"

        /// Code `f060` for name `arrow-left` with format `arrow-left`
        case .arrowLeft: return "\u{f060}"

        /// Code `f187` for name `Archive` with format `archive`
        case .archive: return "\u{f187}"

        /// Code `f559` for name `Award` with format `award`
        case .award: return "\u{f559}"

        /// Code `f0f8` for name `hospital` with format `hospital`
        case .hospital: return "\u{f0f8}"

        /// Code `f0dc` for name `Sort` with format `sort`
        case .sort: return "\u{f0dc}"

        /// Code `f1da` for name `History` with format `history`
        case .history: return "\u{f1da}"

        /// Code `f160` for name `Sort Amount Down` with format `sort_amount_down`
        case .sortAmountDown: return "\u{f160}"

        /// Code `f2d0` for name `Window Maximize` with format `window_maximize`
        case .windowMaximize: return "\u{f2d0}"

        /// Code `f45c` for name `Square Full` with format `square_full`
        case .squareFull: return "\u{f45c}"

        /// Code `f6b7` for name `Book of the Dead` with format `book_of_the_dead`
        case .bookOfTheDead: return "\u{f6b7}"

        /// Code `f0c4` for name `Cut` with format `cut`
        case .cut: return "\u{f0c4}"

        /// Code `f0dd` for name `Sort Down (Descending)` with format `sort_down_(descending)`
        case .sortDownDescending: return "\u{f0dd}"

        /// Code `f0c3` for name `Flask` with format `flask`
        case .flask: return "\u{f0c3}"

        /// Code `f108` for name `Desktop` with format `desktop`
        case .desktop: return "\u{f108}"

        /// Code `f2d2` for name `Window Restore` with format `window_restore`
        case .windowRestore: return "\u{f2d2}"

        /// Code `f3be` for name `Alternate Level Down` with format `alternate_level_down`
        case .alternateLevelDown: return "\u{f3be}"

        /// Code `f87c` for name `Photo Video` with format `photo_video`
        case .photoVideo: return "\u{f87c}"

        /// Code `f647` for name `Bible` with format `bible`
        case .bible: return "\u{f647}"

        /// Code `f796` for name `Ethernet` with format `ethernet`
        case .ethernet: return "\u{f796}"

        /// Code `f110` for name `Spinner` with format `spinner`
        case .spinner: return "\u{f110}"

        /// Code `f665` for name `Hamsa` with format `hamsa`
        case .hamsa: return "\u{f665}"

        /// Code `f1c7` for name `Audio File` with format `audio_file`
        case .audioFile: return "\u{f1c7}"

        /// Code `f257` for name `Scissors (Hand)` with format `scissors_(hand)`
        case .scissorsHand: return "\u{f257}"

        /// Code `f0e8` for name `Sitemap` with format `sitemap`
        case .sitemap: return "\u{f0e8}"

        /// Code `f62f` for name `Teeth Open` with format `teeth_open`
        case .teethOpen: return "\u{f62f}"

        /// Code `f03d` for name `Video` with format `video`
        case .video: return "\u{f03d}"

        /// Code `f5c8` for name `Tired Face` with format `tired_face`
        case .tiredFace: return "\u{f5c8}"

        /// Code `f461` for name `Allergies` with format `allergies`
        case .allergies: return "\u{f461}"

        /// Code `f523` for name `Dice Five` with format `dice_five`
        case .diceFive: return "\u{f523}"

        /// Code `f509` for name `Users Cog` with format `users_cog`
        case .usersCog: return "\u{f509}"

        /// Code `f807` for name `Hard Hat` with format `hard_hat`
        case .hardHat: return "\u{f807}"

        /// Code `f2a3` for name `American Sign Language Interpreting` with format `american_sign_language_interpreting`
        case .americanSignLanguageInterpreting: return "\u{f2a3}"

        /// Code `f540` for name `Parking` with format `parking`
        case .parking: return "\u{f540}"

        /// Code `f6f2` for name `Hryvnia` with format `hryvnia`
        case .hryvnia: return "\u{f6f2}"

        /// Code `f585` for name `Grinning Squinting Face` with format `grinning_squinting_face`
        case .grinningSquintingFace: return "\u{f585}"

        /// Code `f1c2` for name `Word File` with format `word_file`
        case .wordFile: return "\u{f1c2}"

        /// Code `f2cc` for name `Shower` with format `shower`
        case .shower: return "\u{f2cc}"

        /// Code `f0f0` for name `Doctor` with format `doctor`
        case .doctor: return "\u{f0f0}"

        /// Code `f556` for name `Angry Face` with format `angry_face`
        case .angryFace: return "\u{f556}"

        /// Code `f1b9` for name `Car` with format `car`
        case .car: return "\u{f1b9}"

        /// Code `f52c` for name `Equals` with format `equals`
        case .equals: return "\u{f52c}"

        /// Code `f51c` for name `Chalkboard Teacher` with format `chalkboard_teacher`
        case .chalkboardTeacher: return "\u{f51c}"

        /// Code `f562` for name `Concierge Bell` with format `concierge_bell`
        case .conciergeBell: return "\u{f562}"

        /// Code `f787` for name `Carrot` with format `carrot`
        case .carrot: return "\u{f787}"

        /// Code `f1e4` for name `TTY` with format `tty`
        case .tty: return "\u{f1e4}"

        /// Code `f1ab` for name `Language` with format `language`
        case .language: return "\u{f1ab}"

        /// Code `f4e2` for name `Video Slash` with format `video_slash`
        case .videoSlash: return "\u{f4e2}"

        /// Code `f7ba` for name `Alternate Radiation` with format `alternate_radiation`
        case .alternateRadiation: return "\u{f7ba}"

        /// Code `f2a0` for name `Phone Volume` with format `phone_volume`
        case .phoneVolume: return "\u{f2a0}"

        /// Code `f253` for name `Hourglass End` with format `hourglass_end`
        case .hourglassEnd: return "\u{f253}"

        /// Code `f572` for name `File Prescription` with format `file_prescription`
        case .filePrescription: return "\u{f572}"

        /// Code `f56b` for name `Alternate Feather` with format `alternate_feather`
        case .alternateFeather: return "\u{f56b}"

        /// Code `f00b` for name `th-list` with format `th-list`
        case .thList: return "\u{f00b}"

        /// Code `f244` for name `Battery Empty` with format `battery_empty`
        case .batteryEmpty: return "\u{f244}"

        /// Code `f7a6` for name `Guitar` with format `guitar`
        case .guitar: return "\u{f7a6}"

        /// Code `f101` for name `Angle Double Right` with format `angle_double_right`
        case .angleDoubleRight: return "\u{f101}"

        /// Code `f479` for name `First Aid` with format `first_aid`
        case .firstAid: return "\u{f479}"

        /// Code `f472` for name `Dolly` with format `dolly`
        case .dolly: return "\u{f472}"

        /// Code `f43c` for name `Chess Board` with format `chess_board`
        case .chessBoard: return "\u{f43c}"

        /// Code `f6be` for name `Cat` with format `cat`
        case .cat: return "\u{f6be}"

        /// Code `f2c1` for name `Identification Badge` with format `identification_badge`
        case .identificationBadge: return "\u{f2c1}"

        /// Code `f55c` for name `Bong` with format `bong`
        case .bong: return "\u{f55c}"

        /// Code `f186` for name `Moon` with format `moon`
        case .moon: return "\u{f186}"

        /// Code `f5ab` for name `Passport` with format `passport`
        case .passport: return "\u{f5ab}"

        /// Code `f568` for name `Drafting Compass` with format `drafting_compass`
        case .draftingCompass: return "\u{f568}"

        /// Code `f44b` for name `Dumbbell` with format `dumbbell`
        case .dumbbell: return "\u{f44b}"

        /// Code `f0c5` for name `Copy` with format `copy`
        case .copy: return "\u{f0c5}"

        /// Code `f026` for name `Volume Off` with format `volume_off`
        case .volumeOff: return "\u{f026}"

        /// Code `f47d` for name `Alternate Hospital` with format `alternate_hospital`
        case .alternateHospital: return "\u{f47d}"

        /// Code `f093` for name `Upload` with format `upload`
        case .upload: return "\u{f093}"

        /// Code `f651` for name `Comment Dollar` with format `comment_dollar`
        case .commentDollar: return "\u{f651}"

        /// Code `f86d` for name `Icons` with format `icons`
        case .icons: return "\u{f86d}"

        /// Code `f497` for name `X-Ray` with format `x-ray`
        case .xRay: return "\u{f497}"

        /// Code `f589` for name `Face With Tongue` with format `face_with_tongue`
        case .faceWithTongue: return "\u{f589}"

        /// Code `f516` for name `Balance Scale (Right-Weighted)` with format `balance_scale_(right-weighted)`
        case .balanceScaleRightWeighted: return "\u{f516}"

        /// Code `f59f` for name `Map Marked` with format `map_marked`
        case .mapMarked: return "\u{f59f}"

        /// Code `f1e3` for name `Futbol` with format `futbol`
        case .futbol: return "\u{f1e3}"

        /// Code `f0da` for name `Caret Right` with format `caret_right`
        case .caretRight: return "\u{f0da}"

        /// Code `f67b` for name `Pastafarianism` with format `pastafarianism`
        case .pastafarianism: return "\u{f67b}"

        /// Code `f09e` for name `rss` with format `rss`
        case .rss: return "\u{f09e}"

        /// Code `f20b` for name `Shekel Sign` with format `shekel_sign`
        case .shekelSign: return "\u{f20b}"

        /// Code `f57a` for name `Frowning Face With Open Mouth` with format `frowning_face_with_open_mouth`
        case .frowningFaceWithOpenMouth: return "\u{f57a}"

        /// Code `f053` for name `chevron-left` with format `chevron-left`
        case .chevronLeft: return "\u{f053}"

        /// Code `f037` for name `align-center` with format `align-center`
        case .alignCenter: return "\u{f037}"

        /// Code `f7d7` for name `Tenge` with format `tenge`
        case .tenge: return "\u{f7d7}"

        /// Code `f0a1` for name `bullhorn` with format `bullhorn`
        case .bullhorn: return "\u{f0a1}"

        /// Code `f5c3` for name `Swatchbook` with format `swatchbook`
        case .swatchbook: return "\u{f5c3}"

        /// Code `f687` for name `Quran` with format `quran`
        case .quran: return "\u{f687}"

        /// Code `f593` for name `Hot Tub` with format `hot_tub`
        case .hotTub: return "\u{f593}"

        /// Code `f547` for name `Ruler Horizontal` with format `ruler_horizontal`
        case .rulerHorizontal: return "\u{f547}"

        /// Code `f436` for name `Bowling Ball` with format `bowling_ball`
        case .bowlingBall: return "\u{f436}"

        /// Code `f246` for name `I Beam Cursor` with format `i_beam_cursor`
        case .iBeamCursor: return "\u{f246}"

        /// Code `f7d0` for name `Snowman` with format `snowman`
        case .snowman: return "\u{f7d0}"

        /// Code `f57d` for name `Globe with Americas shown` with format `globe_with_americas_shown`
        case .globeWithAmericasShown: return "\u{f57d}"

        /// Code `f4d8` for name `Seedling` with format `seedling`
        case .seedling: return "\u{f4d8}"

        /// Code `f46b` for name `Capsules` with format `capsules`
        case .capsules: return "\u{f46b}"

        /// Code `f669` for name `Jedi` with format `jedi`
        case .jedi: return "\u{f669}"

        /// Code `f630` for name `Theater Masks` with format `theater_masks`
        case .theaterMasks: return "\u{f630}"

        /// Code `f140` for name `Bullseye` with format `bullseye`
        case .bullseye: return "\u{f140}"

        /// Code `f6e8` for name `Wizard's Hat` with format `wizard's_hat`
        case .wizardSHat: return "\u{f6e8}"

        /// Code `f0a7` for name `Hand Pointing Down` with format `hand_pointing_down`
        case .handPointingDown: return "\u{f0a7}"

        /// Code `f0aa` for name `Arrow Circle Up` with format `arrow_circle_up`
        case .arrowCircleUp: return "\u{f0aa}"

        /// Code `f4d6` for name `Ribbon` with format `ribbon`
        case .ribbon: return "\u{f4d6}"

        /// Code `f5ae` for name `Pencil Ruler` with format `pencil_ruler`
        case .pencilRuler: return "\u{f5ae}"

        /// Code `f806` for name `Hand with Middle Finger Raised` with format `hand_with_middle_finger_raised`
        case .handWithMiddleFingerRaised: return "\u{f806}"

        /// Code `f077` for name `chevron-up` with format `chevron-up`
        case .chevronUp: return "\u{f077}"

        /// Code `f4ba` for name `Dove` with format `dove`
        case .dove: return "\u{f4ba}"

        /// Code `f519` for name `Broadcast Tower` with format `broadcast_tower`
        case .broadcastTower: return "\u{f519}"

        /// Code `f492` for name `Vial` with format `vial`
        case .vial: return "\u{f492}"

        /// Code `f541` for name `Percentage` with format `percentage`
        case .percentage: return "\u{f541}"

        /// Code `f21d` for name `Street View` with format `street_view`
        case .streetView: return "\u{f21d}"

        /// Code `f143` for name `RSS Square` with format `rss_square`
        case .rSSSquare: return "\u{f143}"

        /// Code `f3bf` for name `Alternate Level Up` with format `alternate_level_up`
        case .alternateLevelUp: return "\u{f3bf}"

        /// Code `f4d3` for name `Piggy Bank` with format `piggy_bank`
        case .piggyBank: return "\u{f4d3}"

        /// Code `f2a2` for name `Assistive Listening Systems` with format `assistive_listening_systems`
        case .assistiveListeningSystems: return "\u{f2a2}"

        /// Code `f641` for name `Ad` with format `ad`
        case .ad: return "\u{f641}"

        /// Code `f54b` for name `Shoe Prints` with format `shoe_prints`
        case .shoePrints: return "\u{f54b}"

        /// Code `f02b` for name `tag` with format `tag`
        case .tag: return "\u{f02b}"

        /// Code `f728` for name `User Injured` with format `user_injured`
        case .userInjured: return "\u{f728}"

        /// Code `f162` for name `Sort Numeric Down` with format `sort_numeric_down`
        case .sortNumericDown: return "\u{f162}"

        /// Code `f1cd` for name `Life Ring` with format `life_ring`
        case .lifeRing: return "\u{f1cd}"

        /// Code `f1ce` for name `Circle Notched` with format `circle_notched`
        case .circleNotched: return "\u{f1ce}"

        /// Code `f72e` for name `Wind` with format `wind`
        case .wind: return "\u{f72e}"

        /// Code `f0a6` for name `Hand Pointing Up` with format `hand_pointing_up`
        case .handPointingUp: return "\u{f0a6}"

        /// Code `f29e` for name `Audio Description` with format `audio_description`
        case .audioDescription: return "\u{f29e}"

        /// Code `f161` for name `Sort Amount Up` with format `sort_amount_up`
        case .sortAmountUp: return "\u{f161}"

        /// Code `f688` for name `Search Dollar` with format `search_dollar`
        case .searchDollar: return "\u{f688}"

        /// Code `f2dc` for name `Snowflake` with format `snowflake`
        case .snowflake: return "\u{f2dc}"

        /// Code `f22b` for name `Mars Stroke Horizontal` with format `mars_stroke_horizontal`
        case .marsStrokeHorizontal: return "\u{f22b}"

        /// Code `f577` for name `Fingerprint` with format `fingerprint`
        case .fingerprint: return "\u{f577}"

        /// Code `f091` for name `trophy` with format `trophy`
        case .trophy: return "\u{f091}"

        /// Code `f1c5` for name `Image File` with format `image_file`
        case .imageFile: return "\u{f1c5}"

        /// Code `f04b` for name `play` with format `play`
        case .play: return "\u{f04b}"

        /// Code `f0db` for name `Columns` with format `columns`
        case .columns: return "\u{f0db}"

        /// Code `f58f` for name `Alternate Headphones` with format `alternate_headphones`
        case .alternateHeadphones: return "\u{f58f}"

        /// Code `f206` for name `Bicycle` with format `bicycle`
        case .bicycle: return "\u{f206}"

        /// Code `f12c` for name `subscript` with format `subscript`
        case .`subscript`: return "\u{f12c}"

        /// Code `f2c2` for name `Identification Card` with format `identification_card`
        case .identificationCard: return "\u{f2c2}"

        /// Code `f46c` for name `Clipboard with Check` with format `clipboard_with_check`
        case .clipboardWithCheck: return "\u{f46c}"

        /// Code `f2fe` for name `Poo` with format `poo`
        case .poo: return "\u{f2fe}"

        /// Code `f44e` for name `Football Ball` with format `football_ball`
        case .footballBall: return "\u{f44e}"

        /// Code `f468` for name `Boxes` with format `boxes`
        case .boxes: return "\u{f468}"

        /// Code `f134` for name `fire-extinguisher` with format `fire-extinguisher`
        case .fireExtinguisher: return "\u{f134}"

        /// Code `f31e` for name `Alternate Expand Arrows` with format `alternate_expand_arrows`
        case .alternateExpandArrows: return "\u{f31e}"

        /// Code `f4b8` for name `Couch` with format `couch`
        case .couch: return "\u{f4b8}"

        /// Code `f28b` for name `Pause Circle` with format `pause_circle`
        case .pauseCircle: return "\u{f28b}"

        /// Code `f490` for name `Tablets` with format `tablets`
        case .tablets: return "\u{f490}"

        /// Code `f566` for name `Digital Tachograph` with format `digital_tachograph`
        case .digitalTachograph: return "\u{f566}"

        /// Code `f853` for name `Border Style` with format `border_style`
        case .borderStyle: return "\u{f853}"

        /// Code `f7a9` for name `Heart Broken` with format `heart_broken`
        case .heartBroken: return "\u{f7a9}"

        /// Code `f0a3` for name `certificate` with format `certificate`
        case .certificate: return "\u{f0a3}"

        /// Code `f47e` for name `Hospital Symbol` with format `hospital_symbol`
        case .hospitalSymbol: return "\u{f47e}"

        /// Code `f01e` for name `Redo` with format `redo`
        case .redo: return "\u{f01e}"

        /// Code `f2f9` for name `Alternate Redo` with format `alternate_redo`
        case .alternateRedo: return "\u{f2f9}"

        /// Code `f5b3` for name `Crying Face` with format `crying_face`
        case .cryingFace: return "\u{f5b3}"

        /// Code `f0d9` for name `Caret Left` with format `caret_left`
        case .caretLeft: return "\u{f0d9}"

        /// Code `f248` for name `Object Ungroup` with format `object_ungroup`
        case .objectUngroup: return "\u{f248}"

        /// Code `f2a4` for name `Deaf` with format `deaf`
        case .deaf: return "\u{f2a4}"

        /// Code `f5ca` for name `Umbrella Beach` with format `umbrella_beach`
        case .umbrellaBeach: return "\u{f5ca}"

        /// Code `f4c0` for name `Hand Holding US Dollar` with format `hand_holding_us_dollar`
        case .handHoldingUSDollar: return "\u{f4c0}"

        /// Code `f715` for name `Slash` with format `slash`
        case .slash: return "\u{f715}"

        /// Code `f1d8` for name `Paper Plane` with format `paper_plane`
        case .paperPlane: return "\u{f1d8}"

        /// Code `f5ad` for name `Pen Nib` with format `pen_nib`
        case .penNib: return "\u{f5ad}"

        /// Code `f3cd` for name `Alternate Mobile` with format `alternate_mobile`
        case .alternateMobile: return "\u{f3cd}"

        /// Code `f188` for name `Bug` with format `bug`
        case .bug: return "\u{f188}"

        /// Code `f027` for name `Volume Down` with format `volume_down`
        case .volumeDown: return "\u{f027}"

        /// Code `f041` for name `map-marker` with format `map-marker`
        case .mapMarker: return "\u{f041}"

        /// Code `f8c0` for name `Cowboy Hat` with format `cowboy_hat`
        case .cowboyHat: return "\u{f8c0}"

        /// Code `f0c7` for name `Save` with format `save`
        case .save: return "\u{f0c7}"

        /// Code `f4db` for name `Tape` with format `tape`
        case .tape: return "\u{f4db}"

        /// Code `f0cd` for name `Underline` with format `underline`
        case .underline: return "\u{f0cd}"

        /// Code `f506` for name `User Slash` with format `user_slash`
        case .userSlash: return "\u{f506}"

        /// Code `f48d` for name `Smoking` with format `smoking`
        case .smoking: return "\u{f48d}"

        /// Code `f507` for name `User Tag` with format `user_tag`
        case .userTag: return "\u{f507}"

        /// Code `f102` for name `Angle Double Up` with format `angle_double_up`
        case .angleDoubleUp: return "\u{f102}"

        /// Code `f0fc` for name `beer` with format `beer`
        case .beer: return "\u{f0fc}"

        /// Code `f57f` for name `Grimacing Face` with format `grimacing_face`
        case .grimacingFace: return "\u{f57f}"

        /// Code `f79f` for name `Glass Cheers` with format `glass_cheers`
        case .glassCheers: return "\u{f79f}"

        /// Code `f805` for name `Hamburger` with format `hamburger`
        case .hamburger: return "\u{f805}"

        /// Code `f786` for name `Candy Cane` with format `candy_cane`
        case .candyCane: return "\u{f786}"

        /// Code `f0f2` for name `Suitcase` with format `suitcase`
        case .suitcase: return "\u{f0f2}"

        /// Code `f22c` for name `Neuter` with format `neuter`
        case .neuter: return "\u{f22c}"

        /// Code `f279` for name `Map` with format `map`
        case .map: return "\u{f279}"

        /// Code `f075` for name `comment` with format `comment`
        case .comment: return "\u{f075}"

        /// Code `f496` for name `Weight` with format `weight`
        case .weight: return "\u{f496}"

        /// Code `f5bd` for name `Spray Can` with format `spray_can`
        case .sprayCan: return "\u{f5bd}"

        /// Code `f885` for name `Alternate Sort Amount Up` with format `alternate_sort_amount_up`
        case .alternateSortAmountUp: return "\u{f885}"

        /// Code `f5a0` for name `Alternate Map Marked` with format `alternate_map_marked`
        case .alternateMapMarked: return "\u{f5a0}"

        /// Code `f445` for name `Chess Queen` with format `chess_queen`
        case .chessQueen: return "\u{f445}"

        /// Code `f359` for name `Alternate Arrow Circle Left` with format `alternate_arrow_circle_left`
        case .alternateArrowCircleLeft: return "\u{f359}"

        /// Code `f526` for name `Dice Six` with format `dice_six`
        case .diceSix: return "\u{f526}"

        /// Code `f0eb` for name `Lightbulb` with format `lightbulb`
        case .lightbulb: return "\u{f0eb}"

        /// Code `f13a` for name `Chevron Circle Down` with format `chevron_circle_down`
        case .chevronCircleDown: return "\u{f13a}"

        /// Code `f7bf` for name `Satellite` with format `satellite`
        case .satellite: return "\u{f7bf}"

        /// Code `f1de` for name `Horizontal Sliders` with format `horizontal_sliders`
        case .horizontalSliders: return "\u{f1de}"

        /// Code `f54f` for name `Alternate Store` with format `alternate_store`
        case .alternateStore: return "\u{f54f}"

        /// Code `f002` for name `Search` with format `search`
        case .search: return "\u{f002}"

        /// Code `f15d` for name `Sort Alphabetical Down` with format `sort_alphabetical_down`
        case .sortAlphabeticalDown: return "\u{f15d}"

        /// Code `f038` for name `align-right` with format `align-right`
        case .alignRight: return "\u{f038}"

        /// Code `f050` for name `fast-forward` with format `fast-forward`
        case .fastForward: return "\u{f050}"

        /// Code `f530` for name `Glasses` with format `glasses`
        case .glasses: return "\u{f530}"

        /// Code `f7da` for name `Tram` with format `tram`
        case .tram: return "\u{f7da}"

        /// Code `f7bd` for name `Restroom` with format `restroom`
        case .restroom: return "\u{f7bd}"

        /// Code `f223` for name `Mercury` with format `mercury`
        case .mercury: return "\u{f223}"

        /// Code `f51d` for name `Church` with format `church`
        case .church: return "\u{f51d}"

        /// Code `f158` for name `Ruble Sign` with format `ruble_sign`
        case .rubleSign: return "\u{f158}"

        /// Code `f536` for name `Less Than` with format `less_than`
        case .lessThan: return "\u{f536}"

        /// Code `f689` for name `Search Location` with format `search_location`
        case .searchLocation: return "\u{f689}"

        /// Code `f504` for name `User Ninja` with format `user_ninja`
        case .userNinja: return "\u{f504}"

        /// Code `f153` for name `Euro Sign` with format `euro_sign`
        case .euroSign: return "\u{f153}"

        /// Code `f553` for name `T-Shirt` with format `t-shirt`
        case .tShirt: return "\u{f553}"

        /// Code `f6e6` for name `Hanukiah` with format `hanukiah`
        case .hanukiah: return "\u{f6e6}"

        /// Code `f074` for name `random` with format `random`
        case .random: return "\u{f074}"

        /// Code `f0e7` for name `Lightning Bolt` with format `lightning_bolt`
        case .lightningBolt: return "\u{f0e7}"

        /// Code `f73d` for name `Cloud with Rain` with format `cloud_with_rain`
        case .cloudWithRain: return "\u{f73d}"

        /// Code `f58d` for name `Grip Horizontal` with format `grip_horizontal`
        case .gripHorizontal: return "\u{f58d}"

        /// Code `f131` for name `Microphone Slash` with format `microphone_slash`
        case .microphoneSlash: return "\u{f131}"

        /// Code `f241` for name `Battery 3/4 Full` with format `battery_3/4_full`
        case .battery34Full: return "\u{f241}"

        /// Code `f224` for name `Transgender` with format `transgender`
        case .transgender: return "\u{f224}"

        /// Code `f7ce` for name `Snowboarding` with format `snowboarding`
        case .snowboarding: return "\u{f7ce}"

        /// Code `f674` for name `Mail Bulk` with format `mail_bulk`
        case .mailBulk: return "\u{f674}"

        /// Code `f1c0` for name `Database` with format `database`
        case .database: return "\u{f1c0}"

        /// Code `f251` for name `Hourglass Start` with format `hourglass_start`
        case .hourglassStart: return "\u{f251}"

        /// Code `f8d9` for name `Record Vinyl` with format `record_vinyl`
        case .recordVinyl: return "\u{f8d9}"

        /// Code `f21c` for name `Motorcycle` with format `motorcycle`
        case .motorcycle: return "\u{f21c}"

        /// Code `f09d` for name `Credit Card` with format `credit_card`
        case .creditCard: return "\u{f09d}"

        /// Code `f1e0` for name `Alternate Share` with format `alternate_share`
        case .alternateShare: return "\u{f1e0}"

        /// Code `f443` for name `Chess Pawn` with format `chess_pawn`
        case .chessPawn: return "\u{f443}"

        /// Code `f52f` for name `Gas Pump` with format `gas_pump`
        case .gasPump: return "\u{f52f}"

        /// Code `f558` for name `Atlas` with format `atlas`
        case .atlas: return "\u{f558}"

        /// Code `f7f5` for name `Alternate Medical Chat` with format `alternate_medical_chat`
        case .alternateMedicalChat: return "\u{f7f5}"

        /// Code `f3ff` for name `Alternate Ticket` with format `alternate_ticket`
        case .alternateTicket: return "\u{f3ff}"

        /// Code `f599` for name `Grinning Face With Big Eyes` with format `grinning_face_with_big_eyes`
        case .grinningFaceWithBigEyes: return "\u{f599}"

        /// Code `f0a5` for name `Hand Pointing Left` with format `hand_pointing_left`
        case .handPointingLeft: return "\u{f0a5}"

        /// Code `f119` for name `Frowning Face` with format `frowning_face`
        case .frowningFace: return "\u{f119}"

        /// Code `f58b` for name `Winking Face With Tongue` with format `winking_face_with_tongue`
        case .winkingFaceWithTongue: return "\u{f58b}"

        /// Code `f094` for name `Lemon` with format `lemon`
        case .lemon: return "\u{f094}"

        /// Code `f104` for name `angle-left` with format `angle-left`
        case .angleLeft: return "\u{f104}"

        /// Code `f1ae` for name `Child` with format `child`
        case .child: return "\u{f1ae}"

        /// Code `f057` for name `Times Circle` with format `times_circle`
        case .timesCircle: return "\u{f057}"

        /// Code `f6bb` for name `Campground` with format `campground`
        case .campground: return "\u{f6bb}"

        /// Code `f484` for name `Pills` with format `pills`
        case .pills: return "\u{f484}"

        /// Code `f2ca` for name `Thermometer 1/4 Full` with format `thermometer_1/4_full`
        case .thermometer14Full: return "\u{f2ca}"

        /// Code `f0e3` for name `Gavel` with format `gavel`
        case .gavel: return "\u{f0e3}"

        /// Code `f276` for name `Map Pin` with format `map_pin`
        case .mapPin: return "\u{f276}"

        /// Code `f788` for name `Cash Register` with format `cash_register`
        case .cashRegister: return "\u{f788}"

        /// Code `f434` for name `Basketball Ball` with format `basketball_ball`
        case .basketballBall: return "\u{f434}"

        /// Code `f4c2` for name `Hands` with format `hands`
        case .hands: return "\u{f4c2}"

        /// Code `f87b` for name `Alternate Phone Square` with format `alternate_phone_square`
        case .alternatePhoneSquare: return "\u{f87b}"

        /// Code `f613` for name `Oil Can` with format `oil_can`
        case .oilCan: return "\u{f613}"

        /// Code `f154` for name `Pound Sign` with format `pound_sign`
        case .poundSign: return "\u{f154}"

        /// Code `f6ed` for name `Hippo` with format `hippo`
        case .hippo: return "\u{f6ed}"

        /// Code `f810` for name `Ice Cream` with format `ice_cream`
        case .iceCream: return "\u{f810}"

        /// Code `f5de` for name `Alternate Car` with format `alternate_car`
        case .alternateCar: return "\u{f5de}"

        /// Code `f21e` for name `Heartbeat` with format `heartbeat`
        case .heartbeat: return "\u{f21e}"

        /// Code `f70c` for name `Running` with format `running`
        case .running: return "\u{f70c}"

        /// Code `f0f9` for name `ambulance` with format `ambulance`
        case .ambulance: return "\u{f0f9}"

        /// Code `f7c0` for name `Satellite Dish` with format `satellite_dish`
        case .satelliteDish: return "\u{f7c0}"

        /// Code `f0b1` for name `Briefcase` with format `briefcase`
        case .briefcase: return "\u{f0b1}"

        /// Code `f518` for name `Book Open` with format `book_open`
        case .bookOpen: return "\u{f518}"

        /// Code `f028` for name `Volume Up` with format `volume_up`
        case .volumeUp: return "\u{f028}"

        /// Code `f565` for name `Alternate Crop` with format `alternate_crop`
        case .alternateCrop: return "\u{f565}"

        /// Code `f655` for name `Dharmachakra` with format `dharmachakra`
        case .dharmachakra: return "\u{f655}"

        /// Code `f55d` for name `Brush` with format `brush`
        case .brush: return "\u{f55d}"

        /// Code `f2cd` for name `Bath` with format `bath`
        case .bath: return "\u{f2cd}"

        /// Code `f76b` for name `Low Temperature` with format `low_temperature`
        case .lowTemperature: return "\u{f76b}"

        /// Code `f7f7` for name `Crutch` with format `crutch`
        case .crutch: return "\u{f7f7}"

        /// Code `f062` for name `arrow-up` with format `arrow-up`
        case .arrowUp: return "\u{f062}"

        /// Code `f1c3` for name `Excel File` with format `excel_file`
        case .excelFile: return "\u{f1c3}"

        /// Code `f2f2` for name `Stopwatch` with format `stopwatch`
        case .stopwatch: return "\u{f2f2}"

        /// Code `f03b` for name `Outdent` with format `outdent`
        case .outdent: return "\u{f03b}"

        /// Code `f065` for name `Expand` with format `expand`
        case .expand: return "\u{f065}"

        /// Code `f684` for name `Praying Hands` with format `praying_hands`
        case .prayingHands: return "\u{f684}"

        /// Code `f6ff` for name `Wired Network` with format `wired_network`
        case .wiredNetwork: return "\u{f6ff}"

        /// Code `f508` for name `User Tie` with format `user_tie`
        case .userTie: return "\u{f508}"

        /// Code `f51a` for name `Broom` with format `broom`
        case .broom: return "\u{f51a}"

        /// Code `f2f1` for name `Alternate Sync` with format `alternate_sync`
        case .alternateSync: return "\u{f2f1}"

        /// Code `f433` for name `Baseball Ball` with format `baseball_ball`
        case .baseballBall: return "\u{f433}"

        /// Code `f567` for name `Dizzy Face` with format `dizzy_face`
        case .dizzyFace: return "\u{f567}"

        /// Code `f2a8` for name `Low Vision` with format `low_vision`
        case .lowVision: return "\u{f2a8}"

        /// Code `f305` for name `Alternate Pen` with format `alternate_pen`
        case .alternatePen: return "\u{f305}"

        /// Code `f5a2` for name `Medal` with format `medal`
        case .medal: return "\u{f5a2}"

        /// Code `f73c` for name `Cloud with Moon and Rain` with format `cloud_with_moon_and_rain`
        case .cloudWithMoonAndRain: return "\u{f73c}"

        /// Code `f59c` for name `Laughing Winking Face` with format `laughing_winking_face`
        case .laughingWinkingFace: return "\u{f59c}"

        /// Code `f4fd` for name `User Clock` with format `user_clock`
        case .userClock: return "\u{f4fd}"

        /// Code `f595` for name `Joint` with format `joint`
        case .joint: return "\u{f595}"

        /// Code `f71e` for name `Toilet Paper` with format `toilet_paper`
        case .toiletPaper: return "\u{f71e}"

        /// Code `f53a` for name `Wavy Money Bill` with format `wavy_money_bill`
        case .wavyMoneyBill: return "\u{f53a}"

        /// Code `f023` for name `lock` with format `lock`
        case .lock: return "\u{f023}"

        /// Code `f579` for name `Flushed Face` with format `flushed_face`
        case .flushedFace: return "\u{f579}"

        /// Code `f528` for name `Dice Two` with format `dice_two`
        case .diceTwo: return "\u{f528}"

        /// Code `f43f` for name `Chess King` with format `chess_king`
        case .chessKing: return "\u{f43f}"

        /// Code `f5e1` for name `Car Crash` with format `car_crash`
        case .carCrash: return "\u{f5e1}"

        /// Code `f192` for name `Dot Circle` with format `dot_circle`
        case .dotCircle: return "\u{f192}"

        /// Code `f879` for name `Alternate Phone` with format `alternate_phone`
        case .alternatePhone: return "\u{f879}"

        /// Code `f6c3` for name `Cloud with Moon` with format `cloud_with_moon`
        case .cloudWithMoon: return "\u{f6c3}"

        /// Code `f0d8` for name `Caret Up` with format `caret_up`
        case .caretUp: return "\u{f0d8}"

        /// Code `f5a7` for name `Mortar Pestle` with format `mortar_pestle`
        case .mortarPestle: return "\u{f5a7}"

        /// Code `f522` for name `Dice` with format `dice`
        case .dice: return "\u{f522}"

        /// Code `f03e` for name `Image` with format `image`
        case .image: return "\u{f03e}"

        /// Code `f382` for name `Alternate Cloud Upload` with format `alternate_cloud_upload`
        case .alternateCloudUpload: return "\u{f382}"

        /// Code `f0fb` for name `fighter-jet` with format `fighter-jet`
        case .fighterJet: return "\u{f0fb}"

        /// Code `f27a` for name `Alternate Comment` with format `alternate_comment`
        case .alternateComment: return "\u{f27a}"

        /// Code `f2a1` for name `Braille` with format `braille`
        case .braille: return "\u{f2a1}"

        /// Code `f252` for name `Hourglass Half` with format `hourglass_half`
        case .hourglassHalf: return "\u{f252}"

        /// Code `f538` for name `Memory` with format `memory`
        case .memory: return "\u{f538}"

        /// Code `f058` for name `Check Circle` with format `check_circle`
        case .checkCircle: return "\u{f058}"

        /// Code `f447` for name `Chess Rook` with format `chess_rook`
        case .chessRook: return "\u{f447}"

        /// Code `f4ff` for name `User Edit` with format `user_edit`
        case .userEdit: return "\u{f4ff}"

        /// Code `f654` for name `Cross` with format `cross`
        case .cross: return "\u{f654}"

        /// Code `f1b8` for name `Recycle` with format `recycle`
        case .recycle: return "\u{f1b8}"

        /// Code `f897` for name `Voicemail` with format `voicemail`
        case .voicemail: return "\u{f897}"

        /// Code `f535` for name `Kiwi Bird` with format `kiwi_bird`
        case .kiwiBird: return "\u{f535}"

        /// Code `f0c1` for name `Link` with format `link`
        case .link: return "\u{f0c1}"

        /// Code `f4c4` for name `Helping Hands` with format `helping_hands`
        case .helpingHands: return "\u{f4c4}"

        /// Code `f234` for name `User Plus` with format `user_plus`
        case .userPlus: return "\u{f234}"

        /// Code `f53f` for name `Palette` with format `palette`
        case .palette: return "\u{f53f}"

        /// Code `f470` for name `Diagnoses` with format `diagnoses`
        case .diagnoses: return "\u{f470}"

        /// Code `f24d` for name `Clone` with format `clone`
        case .clone: return "\u{f24d}"

        /// Code `f1dc` for name `heading` with format `heading`
        case .heading: return "\u{f1dc}"

        /// Code `f637` for name `Traffic Light` with format `traffic_light`
        case .trafficLight: return "\u{f637}"

        /// Code `f30a` for name `Alternate Long Arrow Left` with format `alternate_long_arrow_left`
        case .alternateLongArrowLeft: return "\u{f30a}"

        /// Code `f48b` for name `Shipping Fast` with format `shipping_fast`
        case .shippingFast: return "\u{f48b}"

        /// Code `f3c1` for name `Lock Open` with format `lock_open`
        case .lockOpen: return "\u{f3c1}"

        /// Code `f4d7` for name `Route` with format `route`
        case .route: return "\u{f4d7}"

        /// Code `f6b6` for name `Blender Phone` with format `blender_phone`
        case .blenderPhone: return "\u{f6b6}"

        /// Code `f205` for name `Toggle On` with format `toggle_on`
        case .toggleOn: return "\u{f205}"

        /// Code `f7ca` for name `Skiing Nordic` with format `skiing_nordic`
        case .skiingNordic: return "\u{f7ca}"

        /// Code `f53b` for name `Alternate Wavy Money Bill` with format `alternate_wavy_money_bill`
        case .alternateWavyMoneyBill: return "\u{f53b}"

        /// Code `f1b3` for name `Cubes` with format `cubes`
        case .cubes: return "\u{f1b3}"

        /// Code `f063` for name `arrow-down` with format `arrow-down`
        case .arrowDown: return "\u{f063}"

        /// Code `f225` for name `Alternate Transgender` with format `alternate_transgender`
        case .alternateTransgender: return "\u{f225}"

        /// Code `f5af` for name `Plane Arrival` with format `plane_arrival`
        case .planeArrival: return "\u{f5af}"

        /// Code `f11e` for name `flag-checkered` with format `flag-checkered`
        case .flagCheckered: return "\u{f11e}"

        /// Code `f303` for name `Alternate Pencil` with format `alternate_pencil`
        case .alternatePencil: return "\u{f303}"

        /// Code `f1c1` for name `PDF File` with format `pdf_file`
        case .pDFFile: return "\u{f1c1}"

        /// Code `f7c4` for name `SIM Card` with format `sim_card`
        case .sIMCard: return "\u{f7c4}"

        /// Code `f4be` for name `Hand Holding Heart` with format `hand_holding_heart`
        case .handHoldingHeart: return "\u{f4be}"

        /// Code `f529` for name `Divide` with format `divide`
        case .divide: return "\u{f529}"

        /// Code `f30c` for name `Alternate Long Arrow Up` with format `alternate_long_arrow_up`
        case .alternateLongArrowUp: return "\u{f30c}"

        /// Code `f067` for name `plus` with format `plus`
        case .plus: return "\u{f067}"

        /// Code `f491` for name `Thermometer` with format `thermometer`
        case .thermometer: return "\u{f491}"

        /// Code `f00d` for name `Times` with format `times`
        case .times: return "\u{f00d}"

        /// Code `f578` for name `Fish` with format `fish`
        case .fish: return "\u{f578}"

        /// Code `f10e` for name `quote-right` with format `quote-right`
        case .quoteRight: return "\u{f10e}"

        /// Code `f1f8` for name `Trash` with format `trash`
        case .trash: return "\u{f1f8}"

        /// Code `f19c` for name `University` with format `university`
        case .university: return "\u{f19c}"

        /// Code `f55e` for name `Bus Alt` with format `bus_alt`
        case .busAlt: return "\u{f55e}"

        /// Code `f204` for name `Toggle Off` with format `toggle_off`
        case .toggleOff: return "\u{f204}"

        /// Code `f0ac` for name `Globe` with format `globe`
        case .globe: return "\u{f0ac}"

        /// Code `f5a4` for name `Face Without Mouth` with format `face_without_mouth`
        case .faceWithoutMouth: return "\u{f5a4}"

        /// Code `f079` for name `Retweet` with format `retweet`
        case .retweet: return "\u{f079}"

        /// Code `f863` for name `Fan` with format `fan`
        case .fan: return "\u{f863}"

        /// Code `f052` for name `eject` with format `eject`
        case .eject: return "\u{f052}"

        /// Code `f850` for name `Border None` with format `border_none`
        case .borderNone: return "\u{f850}"

        /// Code `f4e6` for name `Font Awesome Full Logo` with format `font_awesome_full_logo`
        case .fontAwesomeFullLogo: return "\u{f4e6}"

        /// Code `f815` for name `Pager` with format `pager`
        case .pager: return "\u{f815}"

        /// Code `f217` for name `Add to Shopping Cart` with format `add_to_shopping_cart`
        case .addToShoppingCart: return "\u{f217}"

        /// Code `f575` for name `Fill` with format `fill`
        case .fill: return "\u{f575}"

        /// Code `f756` for name `Person Entering Booth` with format `person_entering_booth`
        case .personEnteringBooth: return "\u{f756}"

        /// Code `f563` for name `Cookie` with format `cookie`
        case .cookie: return "\u{f563}"

        /// Code `f2a7` for name `Sign Language` with format `sign_language`
        case .signLanguage: return "\u{f2a7}"

        /// Code `f11b` for name `Gamepad` with format `gamepad`
        case .gamepad: return "\u{f11b}"

        /// Code `f6ec` for name `Hiking` with format `hiking`
        case .hiking: return "\u{f6ec}"

        /// Code `f7ec` for name `Bread Slice` with format `bread_slice`
        case .breadSlice: return "\u{f7ec}"

        /// Code `f272` for name `Calendar Minus` with format `calendar_minus`
        case .calendarMinus: return "\u{f272}"

        /// Code `f531` for name `Greater Than` with format `greater_than`
        case .greaterThan: return "\u{f531}"

        /// Code `f501` for name `User Graduate` with format `user_graduate`
        case .userGraduate: return "\u{f501}"

        /// Code `f0e2` for name `Undo` with format `undo`
        case .undo: return "\u{f0e2}"

        /// Code `f56c` for name `File Contract` with format `file_contract`
        case .fileContract: return "\u{f56c}"

        /// Code `f35a` for name `Alternate Arrow Circle Right` with format `alternate_arrow_circle_right`
        case .alternateArrowCircleRight: return "\u{f35a}"

        /// Code `f666` for name `Haykal` with format `haykal`
        case .haykal: return "\u{f666}"

        /// Code `f597` for name `Kissing Face With Smiling Eyes` with format `kissing_face_with_smiling_eyes`
        case .kissingFaceWithSmilingEyes: return "\u{f597}"

        /// Code `f1ec` for name `Calculator` with format `calculator`
        case .calculator: return "\u{f1ec}"

        /// Code `f11c` for name `Keyboard` with format `keyboard`
        case .keyboard: return "\u{f11c}"

        /// Code `f3e0` for name `Portrait` with format `portrait`
        case .portrait: return "\u{f3e0}"

        /// Code `f10b` for name `Mobile Phone` with format `mobile_phone`
        case .mobilePhone: return "\u{f10b}"

        /// Code `f146` for name `Minus Square` with format `minus_square`
        case .minusSquare: return "\u{f146}"

        /// Code `f14e` for name `Compass` with format `compass`
        case .compass: return "\u{f14e}"

        /// Code `f4df` for name `Truck Moving` with format `truck_moving`
        case .truckMoving: return "\u{f4df}"

        /// Code `f031` for name `font` with format `font`
        case .font: return "\u{f031}"

        /// Code `f6c0` for name `Chair` with format `chair`
        case .chair: return "\u{f6c0}"

        /// Code `f51b` for name `Chalkboard` with format `chalkboard`
        case .chalkboard: return "\u{f51b}"

        /// Code `f5ba` for name `Solar Panel` with format `solar_panel`
        case .solarPanel: return "\u{f5ba}"

        /// Code `f683` for name `Pray` with format `pray`
        case .pray: return "\u{f683}"

        /// Code `f0fa` for name `medkit` with format `medkit`
        case .medkit: return "\u{f0fa}"

        /// Code `f238` for name `Train` with format `train`
        case .train: return "\u{f238}"

        /// Code `f2f6` for name `Alternate Sign In` with format `alternate_sign_in`
        case .alternateSignIn: return "\u{f2f6}"

        /// Code `f000` for name `Martini Glass` with format `martini_glass`
        case .martiniGlass: return "\u{f000}"

        /// Code `f66d` for name `Khanda` with format `khanda`
        case .khanda: return "\u{f66d}"

        /// Code `f277` for name `Map Signs` with format `map_signs`
        case .mapSigns: return "\u{f277}"

        /// Code `f06d` for name `fire` with format `fire`
        case .fire: return "\u{f06d}"

        /// Code `f0fd` for name `H Square` with format `h_square`
        case .hSquare: return "\u{f0fd}"

        /// Code `f0e0` for name `Envelope` with format `envelope`
        case .envelope: return "\u{f0e0}"

        /// Code `f271` for name `Calendar Plus` with format `calendar_plus`
        case .calendarPlus: return "\u{f271}"

        /// Code `f546` for name `Ruler Combined` with format `ruler_combined`
        case .rulerCombined: return "\u{f546}"

        /// Code `f1e1` for name `Alternate Share Square` with format `alternate_share_square`
        case .alternateShareSquare: return "\u{f1e1}"

        /// Code `f747` for name `Democrat` with format `democrat`
        case .democrat: return "\u{f747}"

        /// Code `f5e7` for name `Charging Station` with format `charging_station`
        case .chargingStation: return "\u{f5e7}"

        /// Code `f6d9` for name `Dungeon` with format `dungeon`
        case .dungeon: return "\u{f6d9}"

        /// Code `f77c` for name `Baby` with format `baby`
        case .baby: return "\u{f77c}"

        /// Code `f0fe` for name `Plus Square` with format `plus_square`
        case .plusSquare: return "\u{f0fe}"

        /// Code `f012` for name `signal` with format `signal`
        case .signal: return "\u{f012}"

        /// Code `f5c1` for name `Suitcase Rolling` with format `suitcase_rolling`
        case .suitcaseRolling: return "\u{f5c1}"

        /// Code `f5bc` for name `Splotch` with format `splotch`
        case .splotch: return "\u{f5bc}"

        /// Code `f51e` for name `Coins` with format `coins`
        case .coins: return "\u{f51e}"

        /// Code `f152` for name `Caret Square Right` with format `caret_square_right`
        case .caretSquareRight: return "\u{f152}"

        /// Code `f753` for name `Meteor` with format `meteor`
        case .meteor: return "\u{f753}"

        /// Code `f43a` for name `Chess Bishop` with format `chess_bishop`
        case .chessBishop: return "\u{f43a}"

        /// Code `f525` for name `Dice One` with format `dice_one`
        case .diceOne: return "\u{f525}"

        /// Code `f52e` for name `Frog` with format `frog`
        case .frog: return "\u{f52e}"

        /// Code `f45d` for name `Table Tennis` with format `table_tennis`
        case .tableTennis: return "\u{f45d}"

        /// Code `f035` for name `Text Width` with format `text_width`
        case .textWidth: return "\u{f035}"

        /// Code `f5df` for name `Car Battery` with format `car_battery`
        case .carBattery: return "\u{f5df}"

        /// Code `f07b` for name `Folder` with format `folder`
        case .folder: return "\u{f07b}"

        /// Code `f243` for name `Battery 1/4 Full` with format `battery_1/4_full`
        case .battery14Full: return "\u{f243}"

        /// Code `f485` for name `Prescription Bottle` with format `prescription_bottle`
        case .prescriptionBottle: return "\u{f485}"

        /// Code `f598` for name `Face Blowing a Kiss` with format `face_blowing_a_kiss`
        case .faceBlowingAKiss: return "\u{f598}"

        /// Code `f084` for name `key` with format `key`
        case .key: return "\u{f084}"

        /// Code `f545` for name `Ruler` with format `ruler`
        case .ruler: return "\u{f545}"

        /// Code `f0a8` for name `Arrow Circle Left` with format `arrow_circle_left`
        case .arrowCircleLeft: return "\u{f0a8}"

        /// Code `f02f` for name `print` with format `print`
        case .print: return "\u{f02f}"

        /// Code `f5ce` for name `Alternate Wine Glas` with format `alternate_wine_glas`
        case .alternateWineGlas: return "\u{f5ce}"

        /// Code `f00a` for name `th` with format `th`
        case .th: return "\u{f00a}"

        /// Code `f45f` for name `Volleyball Ball` with format `volleyball_ball`
        case .volleyballBall: return "\u{f45f}"

        /// Code `f07a` for name `shopping-cart` with format `shopping-cart`
        case .shoppingCart: return "\u{f07a}"

        /// Code `f77d` for name `Baby Carriage` with format `baby_carriage`
        case .babyCarriage: return "\u{f77d}"

        /// Code `f0a4` for name `Hand Pointing Right` with format `hand_pointing_right`
        case .handPointingRight: return "\u{f0a4}"

        /// Code `f63c` for name `Truck Side` with format `truck_side`
        case .truckSide: return "\u{f63c}"

        /// Code `f450` for name `Golf Ball` with format `golf_ball`
        case .golfBall: return "\u{f450}"

        /// Code `f25c` for name `Trademark` with format `trademark`
        case .trademark: return "\u{f25c}"

        /// Code `f4bd` for name `Hand Holding` with format `hand_holding`
        case .handHolding: return "\u{f4bd}"

        /// Code `f3a5` for name `Gem` with format `gem`
        case .gem: return "\u{f3a5}"

        /// Code `f200` for name `Pie Chart` with format `pie_chart`
        case .pieChart: return "\u{f200}"

        /// Code `f533` for name `Helicopter` with format `helicopter`
        case .helicopter: return "\u{f533}"

        /// Code `f221` for name `Venus` with format `venus`
        case .venus: return "\u{f221}"

        /// Code `f884` for name `Alternate Sort Amount Down` with format `alternate_sort_amount_down`
        case .alternateSortAmountDown: return "\u{f884}"

        /// Code `f7ae` for name `Igloo` with format `igloo`
        case .igloo: return "\u{f7ae}"

        /// Code `f698` for name `Alternate Square Root` with format `alternate_square_root`
        case .alternateSquareRoot: return "\u{f698}"

        /// Code `f142` for name `Vertical Ellipsis` with format `vertical_ellipsis`
        case .verticalEllipsis: return "\u{f142}"

        /// Code `f2bb` for name `Address Card` with format `address_card`
        case .addressCard: return "\u{f2bb}"

        /// Code `f439` for name `Chess` with format `chess`
        case .chess: return "\u{f439}"

        /// Code `f586` for name `Rolling on the Floor Laughing` with format `rolling_on_the_floor_laughing`
        case .rollingOnTheFloorLaughing: return "\u{f586}"

        /// Code `f588` for name `Face With Tears of Joy` with format `face_with_tears_of_joy`
        case .faceWithTearsOfJoy: return "\u{f588}"

        /// Code `f7b6` for name `Mug Hot` with format `mug_hot`
        case .mugHot: return "\u{f7b6}"

        /// Code `f1bb` for name `Tree` with format `tree`
        case .tree: return "\u{f1bb}"

        /// Code `f5cd` for name `Hanging Weight` with format `hanging_weight`
        case .hangingWeight: return "\u{f5cd}"

        /// Code `f15b` for name `File` with format `file`
        case .file: return "\u{f15b}"

        /// Code `f5bb` for name `Spa` with format `spa`
        case .spa: return "\u{f5bb}"

        /// Code `f2b5` for name `Handshake` with format `handshake`
        case .handshake: return "\u{f2b5}"

        /// Code `f1fc` for name `Paint Brush` with format `paint_brush`
        case .paintBrush: return "\u{f1fc}"

        /// Code `f2bd` for name `User Circle` with format `user_circle`
        case .userCircle: return "\u{f2bd}"

        /// Code `f4de` for name `Truck Loading` with format `truck_loading`
        case .truckLoading: return "\u{f4de}"

        /// Code `f048` for name `step-backward` with format `step-backward`
        case .stepBackward: return "\u{f048}"

        /// Code `f242` for name `Battery 1/2 Full` with format `battery_1/2_full`
        case .battery12Full: return "\u{f242}"

        /// Code `f8cc` for name `Mouse` with format `mouse`
        case .mouse: return "\u{f8cc}"

        /// Code `f129` for name `Info` with format `info`
        case .info: return "\u{f129}"

        /// Code `f569` for name `Drum` with format `drum`
        case .drum: return "\u{f569}"

        /// Code `f5dc` for name `Brain` with format `brain`
        case .brain: return "\u{f5dc}"

        /// Code `f549` for name `School` with format `school`
        case .school: return "\u{f549}"

        /// Code `f550` for name `Stream` with format `stream`
        case .stream: return "\u{f550}"

        /// Code `f678` for name `Mosque` with format `mosque`
        case .mosque: return "\u{f678}"

        /// Code `f539` for name `Alternate Microphone Slash` with format `alternate_microphone_slash`
        case .alternateMicrophoneSlash: return "\u{f539}"

        /// Code `f699` for name `Star and Crescent` with format `star_and_crescent`
        case .starAndCrescent: return "\u{f699}"

        /// Code `f207` for name `Bus` with format `bus`
        case .bus: return "\u{f207}"

        /// Code `f729` for name `Cardboard VR` with format `cardboard_vr`
        case .cardboardVR: return "\u{f729}"

        /// Code `f228` for name `Venus Mars` with format `venus_mars`
        case .venusMars: return "\u{f228}"

        /// Code `f01c` for name `inbox` with format `inbox`
        case .inbox: return "\u{f01c}"

        /// Code `f13e` for name `Alternate Unlock` with format `alternate_unlock`
        case .alternateUnlock: return "\u{f13e}"

        /// Code `f4da` for name `Winking Face` with format `winking_face`
        case .winkingFace: return "\u{f4da}"

        /// Code `f14d` for name `Share Square` with format `share_square`
        case .shareSquare: return "\u{f14d}"

        /// Code `f1c8` for name `Video File` with format `video_file`
        case .videoFile: return "\u{f1c8}"

        /// Code `f87d` for name `Remove Format` with format `remove_format`
        case .removeFormat: return "\u{f87d}"

        /// Code `f0cc` for name `Strikethrough` with format `strikethrough`
        case .strikethrough: return "\u{f0cc}"

        /// Code `f1fa` for name `At` with format `at`
        case .at: return "\u{f1fa}"

        /// Code `f24e` for name `Balance Scale` with format `balance_scale`
        case .balanceScale: return "\u{f24e}"

        /// Code `f6d1` for name `Dice D6` with format `dice_d6`
        case .diceD6: return "\u{f6d1}"

        /// Code `f55f` for name `Cannabis` with format `cannabis`
        case .cannabis: return "\u{f55f}"

        /// Code `f580` for name `Grinning Face` with format `grinning_face`
        case .grinningFace: return "\u{f580}"

        /// Code `f548` for name `Ruler Vertical` with format `ruler_vertical`
        case .rulerVertical: return "\u{f548}"

        /// Code `f552` for name `Toolbox` with format `toolbox`
        case .toolbox: return "\u{f552}"

        /// Code `f517` for name `Blender` with format `blender`
        case .blender: return "\u{f517}"

        /// Code `f338` for name `Alternate Arrows Vertical` with format `alternate_arrows_vertical`
        case .alternateArrowsVertical: return "\u{f338}"

        /// Code `f227` for name `Mars Double` with format `mars_double`
        case .marsDouble: return "\u{f227}"

        /// Code `f05e` for name `ban` with format `ban`
        case .ban: return "\u{f05e}"

        /// Code `f258` for name `Lizard (Hand)` with format `lizard_(hand)`
        case .lizardHand: return "\u{f258}"

        /// Code `f54d` for name `Smoking Ban` with format `smoking_ban`
        case .smokingBan: return "\u{f54d}"

        /// Code `f537` for name `Less Than Equal To` with format `less_than_equal_to`
        case .lessThanEqualTo: return "\u{f537}"

        /// Code `f887` for name `Alternate Sort Numeric Up` with format `alternate_sort_numeric_up`
        case .alternateSortNumericUp: return "\u{f887}"

        /// Code `f544` for name `Robot` with format `robot`
        case .robot: return "\u{f544}"

        /// Code `f70b` for name `Ring` with format `ring`
        case .ring: return "\u{f70b}"

        /// Code `f150` for name `Caret Square Down` with format `caret_square_down`
        case .caretSquareDown: return "\u{f150}"

        /// Code `f1ac` for name `Fax` with format `fax`
        case .fax: return "\u{f1ac}"

        /// Code `f653` for name `Comments Dollar` with format `comments_dollar`
        case .commentsDollar: return "\u{f653}"

        /// Code `f122` for name `reply-all` with format `reply-all`
        case .replyAll: return "\u{f122}"

        /// Code `f085` for name `cogs` with format `cogs`
        case .cogs: return "\u{f085}"

        /// Code `f337` for name `Alternate Arrows Horizontal` with format `alternate_arrows_horizontal`
        case .alternateArrowsHorizontal: return "\u{f337}"

        /// Code `f029` for name `qrcode` with format `qrcode`
        case .qrcode: return "\u{f029}"

        /// Code `f222` for name `Mars` with format `mars`
        case .mars: return "\u{f222}"

        /// Code `f5eb` for name `Directions` with format `directions`
        case .directions: return "\u{f5eb}"

        /// Code `f0c9` for name `Bars` with format `bars`
        case .bars: return "\u{f0c9}"

        /// Code `f793` for name `Dumpster` with format `dumpster`
        case .dumpster: return "\u{f793}"

        /// Code `f515` for name `Balance Scale (Left-Weighted)` with format `balance_scale_(left-weighted)`
        case .balanceScaleLeftWeighted: return "\u{f515}"

        /// Code `f137` for name `Chevron Circle Left` with format `chevron_circle_left`
        case .chevronCircleLeft: return "\u{f137}"

        /// Code `f0d0` for name `magic` with format `magic`
        case .magic: return "\u{f0d0}"

        /// Code `f610` for name `Microscope` with format `microscope`
        case .microscope: return "\u{f610}"

        /// Code `f6d5` for name `Dragon` with format `dragon`
        case .dragon: return "\u{f6d5}"

        /// Code `f4fe` for name `User Cog` with format `user_cog`
        case .userCog: return "\u{f4fe}"

        /// Code `f02d` for name `book` with format `book`
        case .book: return "\u{f02d}"

        /// Code `f494` for name `Warehouse` with format `warehouse`
        case .warehouse: return "\u{f494}"

        /// Code `f30b` for name `Alternate Long Arrow Right` with format `alternate_long_arrow_right`
        case .alternateLongArrowRight: return "\u{f30b}"

        /// Code `f8c1` for name `Cowboy Hat Side` with format `cowboy_hat_side`
        case .cowboyHatSide: return "\u{f8c1}"

        /// Code `f581` for name `Alternate Grinning Face` with format `alternate_grinning_face`
        case .alternateGrinningFace: return "\u{f581}"

        /// Code `f681` for name `Poll` with format `poll`
        case .poll: return "\u{f681}"

        /// Code `f7b9` for name `Radiation` with format `radiation`
        case .radiation: return "\u{f7b9}"

        /// Code `f226` for name `Venus Double` with format `venus_double`
        case .venusDouble: return "\u{f226}"

        /// Code `f13d` for name `Anchor` with format `anchor`
        case .anchor: return "\u{f13d}"

        /// Code `f034` for name `text-height` with format `text-height`
        case .textHeight: return "\u{f034}"

        /// Code `f07c` for name `Folder Open` with format `folder_open`
        case .folderOpen: return "\u{f07c}"

        /// Code `f2e5` for name `Utensil Spoon` with format `utensil_spoon`
        case .utensilSpoon: return "\u{f2e5}"

        /// Code `f573` for name `File Signature` with format `file_signature`
        case .fileSignature: return "\u{f573}"

        /// Code `f532` for name `Greater Than Equal To` with format `greater_than_equal_to`
        case .greaterThanEqualTo: return "\u{f532}"

        /// Code `f19d` for name `Graduation Cap` with format `graduation_cap`
        case .graduationCap: return "\u{f19d}"

        /// Code `f032` for name `bold` with format `bold`
        case .bold: return "\u{f032}"

        /// Code `f2c8` for name `Thermometer 3/4 Full` with format `thermometer_3/4_full`
        case .thermometer34Full: return "\u{f2c8}"

        /// Code `f5d7` for name `Bone` with format `bone`
        case .bone: return "\u{f5d7}"

        /// Code `f7b5` for name `Mitten` with format `mitten`
        case .mitten: return "\u{f7b5}"

        /// Code `f2ed` for name `Alternate Trash` with format `alternate_trash`
        case .alternateTrash: return "\u{f2ed}"

        /// Code `f2ce` for name `Podcast` with format `podcast`
        case .podcast: return "\u{f2ce}"

        /// Code `f28d` for name `Stop Circle` with format `stop_circle`
        case .stopCircle: return "\u{f28d}"

        /// Code `f521` for name `Crown` with format `crown`
        case .crown: return "\u{f521}"

        /// Code `f1dd` for name `paragraph` with format `paragraph`
        case .paragraph: return "\u{f1dd}"

        /// Code `f590` for name `Headset` with format `headset`
        case .headset: return "\u{f590}"

        /// Code `f3fa` for name `Alternate Tablet` with format `alternate_tablet`
        case .alternateTablet: return "\u{f3fa}"

        /// Code `f135` for name `rocket` with format `rocket`
        case .rocket: return "\u{f135}"

        /// Code `f69b` for name `Synagogue` with format `synagogue`
        case .synagogue: return "\u{f69b}"

        /// Code `f812` for name `Laptop Medical` with format `laptop_medical`
        case .laptopMedical: return "\u{f812}"

        /// Code `f58e` for name `Grip Vertical` with format `grip_vertical`
        case .gripVertical: return "\u{f58e}"

        /// Code `f66f` for name `Landmark` with format `landmark`
        case .landmark: return "\u{f66f}"

        /// Code `f66b` for name `Kaaba` with format `kaaba`
        case .kaaba: return "\u{f66b}"

        /// Code `f6d3` for name `Dog` with format `dog`
        case .dog: return "\u{f6d3}"

        /// Code `f071` for name `Exclamation Triangle` with format `exclamation_triangle`
        case .exclamationTriangle: return "\u{f071}"

        /// Code `f2c9` for name `Thermometer 1/2 Full` with format `thermometer_1/2_full`
        case .thermometer12Full: return "\u{f2c9}"

        /// Code `f54c` for name `Skull` with format `skull`
        case .skull: return "\u{f54c}"

        /// Code `f75e` for name `Republican` with format `republican`
        case .republican: return "\u{f75e}"

        /// Code `f25a` for name `Pointer (Hand)` with format `pointer_(hand)`
        case .pointerHand: return "\u{f25a}"

        /// Code `f453` for name `Hockey Puck` with format `hockey_puck`
        case .hockeyPuck: return "\u{f453}"

        /// Code `f043` for name `tint` with format `tint`
        case .tint: return "\u{f043}"

        /// Code `f2b6` for name `Envelope Open` with format `envelope_open`
        case .envelopeOpen: return "\u{f2b6}"

        /// Code `f79c` for name `Gifts` with format `gifts`
        case .gifts: return "\u{f79c}"

        /// Code `f6dd` for name `File CSV` with format `file_csv`
        case .fileCSV: return "\u{f6dd}"

        /// Code `f493` for name `Vials` with format `vials`
        case .vials: return "\u{f493}"

        /// Code `f441` for name `Chess Knight` with format `chess_knight`
        case .chessKnight: return "\u{f441}"

        /// Code `f05b` for name `Crosshairs` with format `crosshairs`
        case .crosshairs: return "\u{f05b}"

        /// Code `f069` for name `asterisk` with format `asterisk`
        case .asterisk: return "\u{f069}"

        /// Code `f360` for name `Alternate External Link Square` with format `alternate_external_link_square`
        case .alternateExternalLinkSquare: return "\u{f360}"

        /// Code `f682` for name `Poll H` with format `poll_h`
        case .pollH: return "\u{f682}"

        /// Code `f1f9` for name `Copyright` with format `copyright`
        case .copyright: return "\u{f1f9}"

        /// Code `f54a` for name `Screwdriver` with format `screwdriver`
        case .screwdriver: return "\u{f54a}"

        /// Code `f644` for name `Ankh` with format `ankh`
        case .ankh: return "\u{f644}"

        /// Code `f018` for name `road` with format `road`
        case .road: return "\u{f018}"

        /// Code `f066` for name `Compress` with format `compress`
        case .compress: return "\u{f066}"

        /// Code `f14b` for name `Pen Square` with format `pen_square`
        case .penSquare: return "\u{f14b}"

        /// Code `f127` for name `unlink` with format `unlink`
        case .unlink: return "\u{f127}"

        /// Code `f15c` for name `Alternate File` with format `alternate_file`
        case .alternateFile: return "\u{f15c}"

        /// Code `f4e3` for name `Wine Glass` with format `wine_glass`
        case .wineGlass: return "\u{f4e3}"

        /// Code `f0f3` for name `bell` with format `bell`
        case .bell: return "\u{f0f3}"

        /// Code `f570` for name `File Invoice` with format `file_invoice`
        case .fileInvoice: return "\u{f570}"

        /// Code `f21a` for name `Ship` with format `ship`
        case .ship: return "\u{f21a}"

        /// Code `f275` for name `Industry` with format `industry`
        case .industry: return "\u{f275}"

        /// Code `f120` for name `Terminal` with format `terminal`
        case .terminal: return "\u{f120}"

        /// Code `f10a` for name `tablet` with format `tablet`
        case .tablet: return "\u{f10a}"

        /// Code `f6d7` for name `Drumstick with Bite Taken Out` with format `drumstick_with_bite_taken_out`
        case .drumstickWithBiteTakenOut: return "\u{f6d7}"

        /// Code `f543` for name `Receipt` with format `receipt`
        case .receipt: return "\u{f543}"

        /// Code `f2c7` for name `Thermometer Full` with format `thermometer_full`
        case .thermometerFull: return "\u{f2c7}"

        /// Code `f5d2` for name `Atom` with format `atom`
        case .atom: return "\u{f5d2}"

        /// Code `f247` for name `Object Group` with format `object_group`
        case .objectGroup: return "\u{f247}"

        /// Code `f53c` for name `Money Check` with format `money_check`
        case .moneyCheck: return "\u{f53c}"

        /// Code `f80f` for name `Hot Dog` with format `hot_dog`
        case .hotDog: return "\u{f80f}"

        /// Code `f5c4` for name `Swimmer` with format `swimmer`
        case .swimmer: return "\u{f5c4}"

        /// Code `f1e5` for name `Binoculars` with format `binoculars`
        case .binoculars: return "\u{f1e5}"

        /// Code `f0ab` for name `Arrow Circle Down` with format `arrow_circle_down`
        case .arrowCircleDown: return "\u{f0ab}"

        /// Code `f584` for name `Smiling Face With Heart-Eyes` with format `smiling_face_with_heart-eyes`
        case .smilingFaceWithHeartEyes: return "\u{f584}"

        /// Code `f53e` for name `Not Equal` with format `not_equal`
        case .notEqual: return "\u{f53e}"

        /// Code `f717` for name `Spider` with format `spider`
        case .spider: return "\u{f717}"

        /// Code `f5b0` for name `Plane Departure` with format `plane_departure`
        case .planeDeparture: return "\u{f5b0}"

        /// Code `f073` for name `Alternate Calendar` with format `alternate_calendar`
        case .alternateCalendar: return "\u{f073}"

        /// Code `f582` for name `Grinning Face With Smiling Eyes` with format `grinning_face_with_smiling_eyes`
        case .grinningFaceWithSmilingEyes: return "\u{f582}"

        /// Code `f56a` for name `Drum Steelpan` with format `drum_steelpan`
        case .drumSteelpan: return "\u{f56a}"

        /// Code `f5cb` for name `Vector Square` with format `vector_square`
        case .vectorSquare: return "\u{f5cb}"

        /// Code `f664` for name `Gopuram` with format `gopuram`
        case .gopuram: return "\u{f664}"

        /// Code `f5c5` for name `Swimming Pool` with format `swimming_pool`
        case .swimmingPool: return "\u{f5c5}"

        /// Code `f700` for name `Otter` with format `otter`
        case .otter: return "\u{f700}"

        /// Code `f722` for name `Tractor` with format `tractor`
        case .tractor: return "\u{f722}"

        /// Code `f274` for name `Calendar Check` with format `calendar_check`
        case .calendarCheck: return "\u{f274}"

        /// Code `f5e4` for name `Car Side` with format `car_side`
        case .carSide: return "\u{f5e4}"

        /// Code `f477` for name `Medical File` with format `medical_file`
        case .medicalFile: return "\u{f477}"

        /// Code `f0a9` for name `Arrow Circle Right` with format `arrow_circle_right`
        case .arrowCircleRight: return "\u{f0a9}"

        /// Code `f2cb` for name `Thermometer Empty` with format `thermometer_empty`
        case .thermometerEmpty: return "\u{f2cb}"

        /// Code `f772` for name `Vote Yea` with format `vote_yea`
        case .voteYea: return "\u{f772}"

        /// Code `f1f6` for name `Bell Slash` with format `bell_slash`
        case .bellSlash: return "\u{f1f6}"

        /// Code `f0cb` for name `list-ol` with format `list-ol`
        case .listOl: return "\u{f0cb}"

        /// Code `f193` for name `Wheelchair` with format `wheelchair`
        case .wheelchair: return "\u{f193}"

        /// Code `f676` for name `Menorah` with format `menorah`
        case .menorah: return "\u{f676}"

        /// Code `f781` for name `Blog` with format `blog`
        case .blog: return "\u{f781}"

        /// Code `f12e` for name `Puzzle Piece` with format `puzzle_piece`
        case .puzzlePiece: return "\u{f12e}"

        /// Code `f11a` for name `Neutral Face` with format `neutral_face`
        case .neutralFace: return "\u{f11a}"

        /// Code `f076` for name `magnet` with format `magnet`
        case .magnet: return "\u{f076}"

        /// Code `f7d8` for name `Toilet` with format `toilet`
        case .toilet: return "\u{f7d8}"

        /// Code `f471` for name `DNA` with format `dna`
        case .dna: return "\u{f471}"

        /// Code `f054` for name `chevron-right` with format `chevron-right`
        case .chevronRight: return "\u{f054}"

        /// Code `f05a` for name `Info Circle` with format `info_circle`
        case .infoCircle: return "\u{f05a}"

        /// Code `f1eb` for name `WiFi` with format `wifi`
        case .wiFi: return "\u{f1eb}"

        /// Code `f7c2` for name `Sd Card` with format `sd_card`
        case .sdCard: return "\u{f7c2}"

        /// Code `f5d0` for name `Air Freshener` with format `air_freshener`
        case .airFreshener: return "\u{f5d0}"

        /// Code `f233` for name `Server` with format `server`
        case .server: return "\u{f233}"

        /// Code `f482` for name `Pallet` with format `pallet`
        case .pallet: return "\u{f482}"

        /// Code `f072` for name `plane` with format `plane`
        case .plane: return "\u{f072}"

        /// Code `f22a` for name `Mars Stroke Vertical` with format `mars_stroke_vertical`
        case .marsStrokeVertical: return "\u{f22a}"

        /// Code `f3d1` for name `Alternate Money Bill` with format `alternate_money_bill`
        case .alternateMoneyBill: return "\u{f3d1}"

        /// Code `f08d` for name `Thumbtack` with format `thumbtack`
        case .thumbtack: return "\u{f08d}"

        /// Code `f816` for name `Hot Pepper` with format `hot_pepper`
        case .hotPepper: return "\u{f816}"

        /// Code `f527` for name `Dice Three` with format `dice_three`
        case .diceThree: return "\u{f527}"

        /// Code `f12b` for name `superscript` with format `superscript`
        case .superscript: return "\u{f12b}"

        /// Code `f083` for name `Retro Camera` with format `retro_camera`
        case .retroCamera: return "\u{f083}"

        /// Code `f7d2` for name `Snowplow` with format `snowplow`
        case .snowplow: return "\u{f7d2}"

        /// Code `f182` for name `Female` with format `female`
        case .female: return "\u{f182}"

        /// Code `f0de` for name `Sort Up (Ascending)` with format `sort_up_(ascending)`
        case .sortUpAscending: return "\u{f0de}"

        /// Code `f587` for name `Star-Struck` with format `star-struck`
        case .starStruck: return "\u{f587}"

        /// Code `f3c5` for name `Alternate Map Marker` with format `alternate_map_marker`
        case .alternateMapMarker: return "\u{f3c5}"

        /// Code `f1b0` for name `Paw` with format `paw`
        case .paw: return "\u{f1b0}"

        /// Code `f1ea` for name `Newspaper` with format `newspaper`
        case .newspaper: return "\u{f1ea}"

        /// Code `f008` for name `Film` with format `film`
        case .film: return "\u{f008}"

        /// Code `f362` for name `Alternate Exchange` with format `alternate_exchange`
        case .alternateExchange: return "\u{f362}"

        /// Code `f249` for name `Sticky Note` with format `sticky_note`
        case .stickyNote: return "\u{f249}"

        /// Code `f5b7` for name `Signature` with format `signature`
        case .signature: return "\u{f5b7}"

        /// Code `f141` for name `Horizontal Ellipsis` with format `horizontal_ellipsis`
        case .horizontalEllipsis: return "\u{f141}"

        /// Code `f882` for name `Alternate Sort Alphabetical Up` with format `alternate_sort_alphabetical_up`
        case .alternateSortAlphabeticalUp: return "\u{f882}"

        /// Code `f061` for name `arrow-right` with format `arrow-right`
        case .arrowRight: return "\u{f061}"

        /// Code `f100` for name `Angle Double Left` with format `angle_double_left`
        case .angleDoubleLeft: return "\u{f100}"

        /// Code `f574` for name `File Upload` with format `file_upload`
        case .fileUpload: return "\u{f574}"

        /// Code `f503` for name `User Minus` with format `user_minus`
        case .userMinus: return "\u{f503}"

        /// Code `f1fb` for name `Eye Dropper` with format `eye_dropper`
        case .eyeDropper: return "\u{f1fb}"

        /// Code `f7ad` for name `Icicles` with format `icicles`
        case .icicles: return "\u{f7ad}"

        /// Code `f458` for name `Quidditch` with format `quidditch`
        case .quidditch: return "\u{f458}"

        /// Code `f555` for name `Wallet` with format `wallet`
        case .wallet: return "\u{f555}"

        /// Code `f156` for name `Indian Rupee Sign` with format `indian_rupee_sign`
        case .indianRupeeSign: return "\u{f156}"

        /// Code `f4fc` for name `User Check` with format `user_check`
        case .userCheck: return "\u{f4fc}"

        /// Code `f0a0` for name `HDD` with format `hdd`
        case .hdd: return "\u{f0a0}"

        /// Code `f3e5` for name `Reply` with format `reply`
        case .reply: return "\u{f3e5}"

        /// Code `f1e6` for name `Plug` with format `plug`
        case .plug: return "\u{f1e6}"

        /// Code `f6fa` for name `Mask` with format `mask`
        case .mask: return "\u{f6fa}"

        /// Code `f881` for name `Alternate Sort Alphabetical Down` with format `alternate_sort_alphabetical_down`
        case .alternateSortAlphabeticalDown: return "\u{f881}"

        /// Code `f679` for name `Om` with format `om`
        case .om: return "\u{f679}"

        /// Code `f7aa` for name `Holly Berry` with format `holly_berry`
        case .hollyBerry: return "\u{f7aa}"

        /// Code `f06a` for name `Exclamation Circle` with format `exclamation_circle`
        case .exclamationCircle: return "\u{f06a}"

        /// Code `f255` for name `Rock (Hand)` with format `rock_(hand)`
        case .rockHand: return "\u{f255}"

        /// Code `f6a0` for name `Torah` with format `torah`
        case .torah: return "\u{f6a0}"

        /// Code `f236` for name `Bed` with format `bed`
        case .bed: return "\u{f236}"

        /// Code `f7e5` for name `Bacon` with format `bacon`
        case .bacon: return "\u{f7e5}"

        /// Code `f53d` for name `Alternate Money Check` with format `alternate_money_check`
        case .alternateMoneyCheck: return "\u{f53d}"

        /// Code `f5b6` for name `Shuttle Van` with format `shuttle_van`
        case .shuttleVan: return "\u{f5b6}"

        /// Code `f2e7` for name `Utensils` with format `utensils`
        case .utensils: return "\u{f2e7}"

        /// Code `f6a1` for name `Torii Gate` with format `torii_gate`
        case .toriiGate: return "\u{f6a1}"

        /// Code `f72f` for name `Wine Bottle` with format `wine_bottle`
        case .wineBottle: return "\u{f72f}"

        /// Code `f290` for name `Shopping Bag` with format `shopping_bag`
        case .shoppingBag: return "\u{f290}"

        /// Code `f5a1` for name `Marker` with format `marker`
        case .marker: return "\u{f5a1}"

        /// Code `f52a` for name `Door Closed` with format `door_closed`
        case .doorClosed: return "\u{f52a}"

        /// Code `f098` for name `Phone Square` with format `phone_square`
        case .phoneSquare: return "\u{f098}"

        /// Code `f12d` for name `eraser` with format `eraser`
        case .eraser: return "\u{f12d}"

        /// Code `f5da` for name `Book Reader` with format `book_reader`
        case .bookReader: return "\u{f5da}"

        /// Code `f199` for name `Envelope Square` with format `envelope_square`
        case .envelopeSquare: return "\u{f199}"

        /// Code `f295` for name `Percent` with format `percent`
        case .percent: return "\u{f295}"

        /// Code `f0ad` for name `Wrench` with format `wrench`
        case .wrench: return "\u{f0ad}"

        /// Code `f5c0` for name `Alternate Star Half` with format `alternate_star_half`
        case .alternateStarHalf: return "\u{f5c0}"

        /// Code `f273` for name `Calendar Times` with format `calendar_times`
        case .calendarTimes: return "\u{f273}"

        /// Code `f52b` for name `Door Open` with format `door_open`
        case .doorOpen: return "\u{f52b}"

        /// Code `f4fa` for name `Alternate User Slash` with format `alternate_user_slash`
        case .alternateUserSlash: return "\u{f4fa}"

        /// Code `f163` for name `Sort Numeric Up` with format `sort_numeric_up`
        case .sortNumericUp: return "\u{f163}"

        /// Code `f256` for name `Paper (Hand)` with format `paper_(hand)`
        case .paperHand: return "\u{f256}"

        /// Code `f235` for name `Remove User` with format `remove_user`
        case .removeUser: return "\u{f235}"

        /// Code `f462` for name `Band-Aid` with format `band-aid`
        case .bandAid: return "\u{f462}"

        /// Code `f56e` for name `File Export` with format `file_export`
        case .fileExport: return "\u{f56e}"

        /// Code `f109` for name `Laptop` with format `laptop`
        case .laptop: return "\u{f109}"

        /// Code `f59d` for name `Luggage Cart` with format `luggage_cart`
        case .luggageCart: return "\u{f59d}"

        /// Code `f00e` for name `Search Plus` with format `search_plus`
        case .searchPlus: return "\u{f00e}"

        /// Code `f159` for name `Won Sign` with format `won_sign`
        case .wonSign: return "\u{f159}"

        /// Code `f576` for name `Fill Drip` with format `fill_drip`
        case .fillDrip: return "\u{f576}"

        /// Code `f254` for name `Hourglass` with format `hourglass`
        case .hourglass: return "\u{f254}"

        /// Code `f04e` for name `forward` with format `forward`
        case .forward: return "\u{f04e}"

        /// Code `f292` for name `Hashtag` with format `hashtag`
        case .hashtag: return "\u{f292}"

        /// Code `f2db` for name `Microchip` with format `microchip`
        case .microchip: return "\u{f2db}"

        /// Code `f67c` for name `Peace` with format `peace`
        case .peace: return "\u{f67c}"

        /// Code `f5d1` for name `Fruit Apple` with format `fruit_apple`
        case .fruitApple: return "\u{f5d1}"

        /// Code `f0f1` for name `Stethoscope` with format `stethoscope`
        case .stethoscope: return "\u{f0f1}"

        /// Code `f534` for name `Infinity` with format `infinity`
        case .infinity: return "\u{f534}"

        /// Code `f358` for name `Alternate Arrow Circle Down` with format `alternate_arrow_circle_down`
        case .alternateArrowCircleDown: return "\u{f358}"

        /// Code `f00c` for name `Check` with format `check`
        case .check: return "\u{f00c}"

        /// Code `f7fb` for name `Egg` with format `egg`
        case .egg: return "\u{f7fb}"

        /// Code `f06c` for name `leaf` with format `leaf`
        case .leaf: return "\u{f06c}"

        /// Code `f185` for name `Sun` with format `sun`
        case .sun: return "\u{f185}"

        /// Code `f004` for name `Heart` with format `heart`
        case .heart: return "\u{f004}"

        /// Code `f70e` for name `Scroll` with format `scroll`
        case .scroll: return "\u{f70e}"

        /// Code `f406` for name `Alternate User` with format `alternate_user`
        case .alternateUser: return "\u{f406}"

        /// Code `f486` for name `Alternate Prescription Bottle` with format `alternate_prescription_bottle`
        case .alternatePrescriptionBottle: return "\u{f486}"

        /// Code `f0b0` for name `Filter` with format `filter`
        case .filter: return "\u{f0b0}"

        /// Code `f55a` for name `Backspace` with format `backspace`
        case .backspace: return "\u{f55a}"

        /// Code `f466` for name `Box` with format `box`
        case .box: return "\u{f466}"

        /// Code `f7a4` for name `Grip Lines` with format `grip_lines`
        case .gripLines: return "\u{f7a4}"

        /// Code `f017` for name `Clock` with format `clock`
        case .clock: return "\u{f017}"

        /// Code `f030` for name `camera` with format `camera`
        case .camera: return "\u{f030}"

        /// Code `f1b2` for name `Cube` with format `cube`
        case .cube: return "\u{f1b2}"

        /// Code `f03c` for name `Indent` with format `indent`
        case .indent: return "\u{f03c}"

        /// Code `f62e` for name `Teeth` with format `teeth`
        case .teeth: return "\u{f62e}"

        /// Code `f7e6` for name `Medical Book` with format `medical_book`
        case .medicalBook: return "\u{f7e6}"

        /// Code `f078` for name `chevron-down` with format `chevron-down`
        case .chevronDown: return "\u{f078}"

        /// Code `f7c5` for name `Skating` with format `skating`
        case .skating: return "\u{f7c5}"

        /// Code `f001` for name `Music` with format `music`
        case .music: return "\u{f001}"

        /// Code `f107` for name `angle-down` with format `angle-down`
        case .angleDown: return "\u{f107}"

        /// Code `f6e2` for name `Ghost` with format `ghost`
        case .ghost: return "\u{f6e2}"

        /// Code `f197` for name `Space Shuttle` with format `space_shuttle`
        case .spaceShuttle: return "\u{f197}"

        /// Code `f144` for name `Play Circle` with format `play_circle`
        case .playCircle: return "\u{f144}"

        /// Code `f155` for name `Dollar Sign` with format `dollar_sign`
        case .dollarSign: return "\u{f155}"

        /// Code `f780` for name `Biohazard` with format `biohazard`
        case .biohazard: return "\u{f780}"

        /// Code `f0e9` for name `Umbrella` with format `umbrella`
        case .umbrella: return "\u{f0e9}"

        /// Code `f0ae` for name `Tasks` with format `tasks`
        case .tasks: return "\u{f0ae}"

        /// Code `f520` for name `Crow` with format `crow`
        case .crow: return "\u{f520}"

        /// Code `f73b` for name `Cloud with (a chance of) Meatball` with format `cloud_with_(a_chance_of)_meatball`
        case .cloudWithAChanceOfMeatball: return "\u{f73b}"

        /// Code `f121` for name `Code` with format `code`
        case .code: return "\u{f121}"

        /// Code `f7a2` for name `Globe with Europe shown` with format `globe_with_europe_shown`
        case .globeWithEuropeShown: return "\u{f7a2}"

        /// Code `f61f` for name `Shapes` with format `shapes`
        case .shapes: return "\u{f61f}"

        /// Code `f58a` for name `Squinting Face With Tongue` with format `squinting_face_with_tongue`
        case .squintingFaceWithTongue: return "\u{f58a}"

        /// Code `f4ce` for name `People Carry` with format `people_carry`
        case .peopleCarry: return "\u{f4ce}"

        /// Code `f126` for name `Code Branch` with format `code_branch`
        case .codeBranch: return "\u{f126}"

        /// Code `f044` for name `Edit` with format `edit`
        case .edit: return "\u{f044}"

        /// Code `f15e` for name `Sort Alphabetical Up` with format `sort_alphabetical_up`
        case .sortAlphabeticalUp: return "\u{f15e}"

        /// Code `f3dd` for name `Phone Slash` with format `phone_slash`
        case .phoneSlash: return "\u{f3dd}"

        /// Code `f7cc` for name `Sleigh` with format `sleigh`
        case .sleigh: return "\u{f7cc}"

        /// Code `f6fc` for name `Mountain` with format `mountain`
        case .mountain: return "\u{f6fc}"

        /// Code `f164` for name `thumbs-up` with format `thumbs-up`
        case .thumbsUp: return "\u{f164}"

        /// Code `f21b` for name `User Secret` with format `user_secret`
        case .userSecret: return "\u{f21b}"

        /// Code `f591` for name `Highlighter` with format `highlighter`
        case .highlighter: return "\u{f591}"

        /// Code `f6a9` for name `Volume Mute` with format `volume_mute`
        case .volumeMute: return "\u{f6a9}"

        /// Code `f151` for name `Caret Square Up` with format `caret_square_up`
        case .caretSquareUp: return "\u{f151}"

        /// Code `f773` for name `Water` with format `water`
        case .water: return "\u{f773}"

        /// Code `f157` for name `Yen Sign` with format `yen_sign`
        case .yenSign: return "\u{f157}"

        /// Code `f75b` for name `Rainbow` with format `rainbow`
        case .rainbow: return "\u{f75b}"

        /// Code `f52d` for name `Feather` with format `feather`
        case .feather: return "\u{f52d}"

        /// Code `f2f5` for name `Alternate Sign Out` with format `alternate_sign_out`
        case .alternateSignOut: return "\u{f2f5}"

        /// Code `f2d1` for name `Window Minimize` with format `window_minimize`
        case .windowMinimize: return "\u{f2d1}"

        /// Code `f4cd` for name `Parachute Box` with format `parachute_box`
        case .parachuteBox: return "\u{f4cd}"

        /// Code `f47f` for name `Alternate Identification Card` with format `alternate_identification_card`
        case .alternateIdentificationCard: return "\u{f47f}"

        /// Code `f5b8` for name `Beaming Face With Smiling Eyes` with format `beaming_face_with_smiling_eyes`
        case .beamingFaceWithSmilingEyes: return "\u{f5b8}"

        /// Code `f4d9` for name `Sign` with format `sign`
        case .sign: return "\u{f4d9}"

        /// Code `f381` for name `Alternate Cloud Download` with format `alternate_cloud_download`
        case .alternateCloudDownload: return "\u{f381}"

        /// Code `f67f` for name `Place of Worship` with format `place_of_worship`
        case .placeOfWorship: return "\u{f67f}"

        /// Code `f0c8` for name `Square` with format `square`
        case .square: return "\u{f0c8}"

        /// Code `f78c` for name `Alternate Compress Arrows` with format `alternate_compress_arrows`
        case .alternateCompressArrows: return "\u{f78c}"

        /// Code `f784` for name `Calendar with Week Focus` with format `calendar_with_week_focus`
        case .calendarWithWeekFocus: return "\u{f784}"

        /// Code `f66a` for name `Journal of the Whills` with format `journal_of_the_whills`
        case .journalOfTheWhills: return "\u{f66a}"

        /// Code `f089` for name `star-half` with format `star-half`
        case .starHalf: return "\u{f089}"

        /// Code `f302` for name `Images` with format `images`
        case .images: return "\u{f302}"

        /// Code `f013` for name `cog` with format `cog`
        case .cog: return "\u{f013}"

        /// Code `f039` for name `align-justify` with format `align-justify`
        case .alignJustify: return "\u{f039}"

        /// Code `f125` for name `crop` with format `crop`
        case .crop: return "\u{f125}"

        /// Code `f57c` for name `Globe with Africa shown` with format `globe_with_africa_shown`
        case .globeWithAfricaShown: return "\u{f57c}"

        /// Code `f474` for name `Dolly Flatbed` with format `dolly_flatbed`
        case .dollyFlatbed: return "\u{f474}"

        /// Code `f103` for name `Angle Double Down` with format `angle_double_down`
        case .angleDoubleDown: return "\u{f103}"

        /// Code `f021` for name `Sync` with format `sync`
        case .sync: return "\u{f021}"

        /// Code `f095` for name `Phone` with format `phone`
        case .phone: return "\u{f095}"

        /// Code `f2b9` for name `Address Book` with format `address_book`
        case .addressBook: return "\u{f2b9}"

        /// Code `f5bf` for name `Stamp` with format `stamp`
        case .stamp: return "\u{f5bf}"

        /// Code `f560` for name `Double Check` with format `double_check`
        case .doubleCheck: return "\u{f560}"

        /// Code `f5a6` for name `Monument` with format `monument`
        case .monument: return "\u{f5a6}"

        /// Code `f245` for name `Mouse Pointer` with format `mouse_pointer`
        case .mousePointer: return "\u{f245}"

        /// Code `f29a` for name `Universal Access` with format `universal_access`
        case .universalAccess: return "\u{f29a}"

        /// Code `f502` for name `User Lock` with format `user_lock`
        case .userLock: return "\u{f502}"

        /// Code `f5ac` for name `Pen Fancy` with format `pen_fancy`
        case .penFancy: return "\u{f5ac}"

        /// Code `f201` for name `Line Chart` with format `line_chart`
        case .lineChart: return "\u{f201}"

        /// Code `f1ba` for name `Taxi` with format `taxi`
        case .taxi: return "\u{f1ba}"

        /// Code `f783` for name `Calendar with Day Focus` with format `calendar_with_day_focus`
        case .calendarWithDayFocus: return "\u{f783}"

        /// Code `f469` for name `Medical Briefcase` with format `medical_briefcase`
        case .medicalBriefcase: return "\u{f469}"

        /// Code `f036` for name `align-left` with format `align-left`
        case .alignLeft: return "\u{f036}"

        /// Code `f829` for name `Trash Restore` with format `trash_restore`
        case .trashRestore: return "\u{f829}"

        /// Code `f011` for name `Power Off` with format `power_off`
        case .powerOff: return "\u{f011}"

        /// Code `f0b2` for name `Alternate Arrows` with format `alternate_arrows`
        case .alternateArrows: return "\u{f0b2}"

        /// Code `f46d` for name `Clipboard List` with format `clipboard_list`
        case .clipboardList: return "\u{f46d}"

        /// Code `f10d` for name `quote-left` with format `quote-left`
        case .quoteLeft: return "\u{f10d}"

        /// Code `f75a` for name `Poo Storm` with format `poo_storm`
        case .pooStorm: return "\u{f75a}"

        /// Code `f106` for name `angle-up` with format `angle-up`
        case .angleUp: return "\u{f106}"

        /// Code `f7e4` for name `Alternate Fire` with format `alternate_fire`
        case .alternateFire: return "\u{f7e4}"

        /// Code `f042` for name `adjust` with format `adjust`
        case .adjust: return "\u{f042}"

        /// Code `f086` for name `comments` with format `comments`
        case .comments: return "\u{f086}"

        /// Code `f4b9` for name `Donate` with format `donate`
        case .donate: return "\u{f4b9}"

        /// Code `f183` for name `Male` with format `male`
        case .male: return "\u{f183}"

        /// Code `f033` for name `italic` with format `italic`
        case .italic: return "\u{f033}"

        /// Code `f571` for name `File Invoice with US Dollar` with format `file_invoice_with_us_dollar`
        case .fileInvoiceWithUSDollar: return "\u{f571}"

        /// Code `f056` for name `Minus Circle` with format `minus_circle`
        case .minusCircle: return "\u{f056}"

        /// Code `f542` for name `Project Diagram` with format `project_diagram`
        case .projectDiagram: return "\u{f542}"

        /// Code `f75f` for name `Smog` with format `smog`
        case .smog: return "\u{f75f}"

        /// Code `f7cd` for name `SMS` with format `sms`
        case .sms: return "\u{f7cd}"

        /// Code `f229` for name `Mars Stroke` with format `mars_stroke`
        case .marsStroke: return "\u{f229}"

        /// Code `f064` for name `Share` with format `share`
        case .share: return "\u{f064}"

        /// Code `f20a` for name `Closed Captioning` with format `closed_captioning`
        case .closedCaptioning: return "\u{f20a}"

        /// Code `f714` for name `Skull & Crossbones` with format `skull_&_crossbones`
        case .skullCrossbones: return "\u{f714}"

        /// Code `f239` for name `Subway` with format `subway`
        case .subway: return "\u{f239}"

        /// Code `f4ad` for name `Comment Dots` with format `comment_dots`
        case .commentDots: return "\u{f4ad}"

        /// Code `f0ca` for name `list-ul` with format `list-ul`
        case .listUl: return "\u{f0ca}"

        /// Code `f7ab` for name `Horse Head` with format `horse_head`
        case .horseHead: return "\u{f7ab}"

        /// Code `f5a5` for name `Face With Rolling Eyes` with format `face_with_rolling_eyes`
        case .faceWithRollingEyes: return "\u{f5a5}"

        /// Code `f83e` for name `Square Wave` with format `square_wave`
        case .squareWave: return "\u{f83e}"

        /// Code `f6e3` for name `Hammer` with format `hammer`
        case .hammer: return "\u{f6e3}"

        /// Code `f46a` for name `Burn` with format `burn`
        case .burn: return "\u{f46a}"

        /// Code `f025` for name `headphones` with format `headphones`
        case .headphones: return "\u{f025}"

        /// Code `f7c9` for name `Skiing` with format `skiing`
        case .skiing: return "\u{f7c9}"

        /// Code `f191` for name `Caret Square Left` with format `caret_square_left`
        case .caretSquareLeft: return "\u{f191}"

        /// Code `f080` for name `Bar Chart` with format `bar_chart`
        case .barChart: return "\u{f080}"

        /// Code `f5b4` for name `Loudly Crying Face` with format `loudly_crying_face`
        case .loudlyCryingFace: return "\u{f5b4}"

        /// Code `f5c7` for name `Tint Slash` with format `tint_slash`
        case .tintSlash: return "\u{f5c7}"

        /// Code `f009` for name `th-large` with format `th-large`
        case .thLarge: return "\u{f009}"

        /// Code `f0d7` for name `Caret Down` with format `caret_down`
        case .caretDown: return "\u{f0d7}"

        /// Code `f059` for name `Question Circle` with format `question_circle`
        case .questionCircle: return "\u{f059}"

        /// Code `f487` for name `Procedures` with format `procedures`
        case .procedures: return "\u{f487}"

        /// Code `f240` for name `Battery Full` with format `battery_full`
        case .batteryFull: return "\u{f240}"

        /// Code `f022` for name `Alternate List` with format `alternate_list`
        case .alternateList: return "\u{f022}"

        /// Code `f410` for name `Window Close` with format `window_close`
        case .windowClose: return "\u{f410}"

        /// Code `f111` for name `Circle` with format `circle`
        case .circle: return "\u{f111}"

        /// Code `f481` for name `Medical Notes` with format `medical_notes`
        case .medicalNotes: return "\u{f481}"

        /// Code `f84c` for name `Border All` with format `border_all`
        case .borderAll: return "\u{f84c}"

        /// Code `f564` for name `Cookie Bite` with format `cookie_bite`
        case .cookieBite: return "\u{f564}"

        /// Code `f0c2` for name `Cloud` with format `cloud`
        case .cloud: return "\u{f0c2}"

        /// Code `f58c` for name `Grinning Winking Face` with format `grinning_winking_face`
        case .grinningWinkingFace: return "\u{f58c}"

        /// Code `f304` for name `Pen` with format `pen`
        case .pen: return "\u{f304}"

        /// Code `f7ef` for name `Cheese` with format `cheese`
        case .cheese: return "\u{f7ef}"

        /// Code `f1c9` for name `Code File` with format `code_file`
        case .codeFile: return "\u{f1c9}"

        /// Code `f505` for name `User Shield` with format `user_shield`
        case .userShield: return "\u{f505}"

        /// Code `f82f` for name `Nurse` with format `nurse`
        case .nurse: return "\u{f82f}"

        /// Code `f743` for name `Cloud with Sun and Rain` with format `cloud_with_sun_and_rain`
        case .cloudWithSunAndRain: return "\u{f743}"

        /// Code `f005` for name `Star` with format `star`
        case .star: return "\u{f005}"

        /// Code `f04d` for name `stop` with format `stop`
        case .stop: return "\u{f04d}"

        /// Code `f007` for name `User` with format `user`
        case .user: return "\u{f007}"

        /// Code `f1e2` for name `Bomb` with format `bomb`
        case .bomb: return "\u{f1e2}"

        /// Code `f6de` for name `Raised Fist` with format `raised_fist`
        case .raisedFist: return "\u{f6de}"

        /// Code `f4fb` for name `User Astronaut` with format `user_astronaut`
        case .userAstronaut: return "\u{f4fb}"

        /// Code `f619` for name `Poop` with format `poop`
        case .poop: return "\u{f619}"

        /// Code `f886` for name `Alternate Sort Numeric Down` with format `alternate_sort_numeric_down`
        case .alternateSortNumericDown: return "\u{f886}"

        /// Code `f557` for name `Archway` with format `archway`
        case .archway: return "\u{f557}"

        /// Code `f218` for name `Shopping Cart Arrow Down` with format `shopping_cart_arrow_down`
        case .shoppingCartArrowDown: return "\u{f218}"

        /// Code `f5ee` for name `Draw Polygon` with format `draw_polygon`
        case .drawPolygon: return "\u{f5ee}"

        /// Code `f56f` for name `File Import` with format `file_import`
        case .fileImport: return "\u{f56f}"

        /// Code `f56d` for name `File Download` with format `file_download`
        case .fileDownload: return "\u{f56d}"

        /// Code `f068` for name `minus` with format `minus`
        case .minus: return "\u{f068}"

        /// Code `f128` for name `Question` with format `question`
        case .question: return "\u{f128}"

        /// Code `f5c9` for name `Tooth` with format `tooth`
        case .tooth: return "\u{f5c9}"

        /// Code `f195` for name `Turkish Lira Sign` with format `turkish_lira_sign`
        case .turkishLiraSign: return "\u{f195}"

        /// Code `f1c6` for name `Archive File` with format `archive_file`
        case .archiveFile: return "\u{f1c6}"

        /// Code `f696` for name `Socks` with format `socks`
        case .socks: return "\u{f696}"

        /// Code `f070` for name `Eye Slash` with format `eye_slash`
        case .eyeSlash: return "\u{f070}"

        /// Code `f105` for name `angle-right` with format `angle-right`
        case .angleRight: return "\u{f105}"

        /// Code `f138` for name `Chevron Circle Right` with format `chevron_circle_right`
        case .chevronCircleRight: return "\u{f138}"

        /// Code `f596` for name `Kissing Face` with format `kissing_face`
        case .kissingFace: return "\u{f596}"

        /// Code `f69a` for name `Star of David` with format `star_of_david`
        case .starOfDavid: return "\u{f69a}"

        /// Code `f02a` for name `barcode` with format `barcode`
        case .barcode: return "\u{f02a}"

        /// Code `f055` for name `Plus Circle` with format `plus_circle`
        case .plusCircle: return "\u{f055}"

        /// Code `f0c0` for name `Users` with format `users`
        case .users: return "\u{f0c0}"

        /// Code `f84a` for name `Biking` with format `biking`
        case .biking: return "\u{f84a}"

        /// Code `f59a` for name `Laugh Face with Beaming Eyes` with format `laugh_face_with_beaming_eyes`
        case .laughFaceWithBeamingEyes: return "\u{f59a}"

        /// Code `f0ea` for name `Paste` with format `paste`
        case .paste: return "\u{f0ea}"

        /// Code `f1ad` for name `Building` with format `building`
        case .building: return "\u{f1ad}"

        /// Code `f019` for name `Download` with format `download`
        case .download: return "\u{f019}"

        /// Code `f5fd` for name `Layer Group` with format `layer_group`
        case .layerGroup: return "\u{f5fd}"

        /// Code `f0f4` for name `Coffee` with format `coffee`
        case .coffee: return "\u{f0f4}"

        /// Code `f65e` for name `Folder Plus` with format `folder_plus`
        case .folderPlus: return "\u{f65e}"

        /// Code `f74d` for name `United States of America Flag` with format `united_states_of_america_flag`
        case .unitedStatesOfAmericaFlag: return "\u{f74d}"

        /// Code `f6f0` for name `Horse` with format `horse`
        case .horse: return "\u{f6f0}"

        /// Code `f29d` for name `Blind` with format `blind`
        case .blind: return "\u{f29d}"

        /// Code `f554` for name `Walking` with format `walking`
        case .walking: return "\u{f554}"

        /// Code `f3c9` for name `Alternate Microphone` with format `alternate_microphone`
        case .alternateMicrophone: return "\u{f3c9}"

        /// Code `f64f` for name `City` with format `city`
        case .city: return "\u{f64f}"

        /// Code `f5b1` for name `Prescription` with format `prescription`
        case .prescription: return "\u{f5b1}"

        /// Code `f891` for name `Spell Check` with format `spell_check`
        case .spellCheck: return "\u{f891}"

        /// Code `f57b` for name `Alternate Glass Martini` with format `alternate_glass_martini`
        case .alternateGlassMartini: return "\u{f57b}"

        /// Code `f0d1` for name `truck` with format `truck`
        case .truck: return "\u{f0d1}"

        default: return ""
        }
    }

    public init?(rawValue: String) {
        switch rawValue {

        /// Format `alternate_medical_file` to `.alternateMedicalFile`
        case "alternate_medical_file" : self = .alternateMedicalFile

        /// Format `exclamation` to `.exclamation`
        case "exclamation" : self = .exclamation

        /// Format `compact_disc` to `.compactDisc`
        case "compact_disc" : self = .compactDisc

        /// Format `syringe` to `.syringe`
        case "syringe" : self = .syringe

        /// Format `area_chart` to `.areaChart`
        case "area_chart" : self = .areaChart

        /// Format `hotel` to `.hotel`
        case "hotel" : self = .hotel

        /// Format `folder_minus` to `.folderMinus`
        case "folder_minus" : self = .folderMinus

        /// Format `backward` to `.backward`
        case "backward" : self = .backward

        /// Format `hushed_face` to `.hushedFace`
        case "hushed_face" : self = .hushedFace

        /// Format `fast-backward` to `.fastBackward`
        case "fast-backward" : self = .fastBackward

        /// Format `spock_(hand)` to `.spockHand`
        case "spock_(hand)" : self = .spockHand

        /// Format `grinning_face_with_sweat` to `.grinningFaceWithSweat`
        case "grinning_face_with_sweat" : self = .grinningFaceWithSweat

        /// Format `list` to `.list`
        case "list" : self = .list

        /// Format `alternate_external_link` to `.alternateExternalLink`
        case "alternate_external_link" : self = .alternateExternalLink

        /// Format `microphone` to `.microphone`
        case "microphone" : self = .microphone

        /// Format `glass_whiskey` to `.glassWhiskey`
        case "glass_whiskey" : self = .glassWhiskey

        /// Format `eye` to `.eye`
        case "eye" : self = .eye

        /// Format `star_of_life` to `.starOfLife`
        case "star_of_life" : self = .starOfLife

        /// Format `funnel_dollar` to `.funnelDollar`
        case "funnel_dollar" : self = .funnelDollar

        /// Format `stroopwafel` to `.stroopwafel`
        case "stroopwafel" : self = .stroopwafel

        /// Format `paperclip` to `.paperclip`
        case "paperclip" : self = .paperclip

        /// Format `home` to `.home`
        case "home" : self = .home

        /// Format `alternate_tachometer` to `.alternateTachometer`
        case "alternate_tachometer" : self = .alternateTachometer

        /// Format `genderless` to `.genderless`
        case "genderless" : self = .genderless

        /// Format `laptop_code` to `.laptopCode`
        case "laptop_code" : self = .laptopCode

        /// Format `high_temperature` to `.highTemperature`
        case "high_temperature" : self = .highTemperature

        /// Format `business_time` to `.businessTime`
        case "business_time" : self = .businessTime

        /// Format `bookmark` to `.bookmark`
        case "bookmark" : self = .bookmark

        /// Format `dumpster_fire` to `.dumpsterFire`
        case "dumpster_fire" : self = .dumpsterFire

        /// Format `yin_yang` to `.yinYang`
        case "yin_yang" : self = .yinYang

        /// Format `bezier_curve` to `.bezierCurve`
        case "bezier_curve" : self = .bezierCurve

        /// Format `pause` to `.pause`
        case "pause" : self = .pause

        /// Format `peace_(hand)` to `.peaceHand`
        case "peace_(hand)" : self = .peaceHand

        /// Format `smiling_face` to `.smilingFace`
        case "smiling_face" : self = .smilingFace

        /// Format `globe_with_asia_shown` to `.globeWithAsiaShown`
        case "globe_with_asia_shown" : self = .globeWithAsiaShown

        /// Format `shopping_basket` to `.shoppingBasket`
        case "shopping_basket" : self = .shoppingBasket

        /// Format `search_minus` to `.searchMinus`
        case "search_minus" : self = .searchMinus

        /// Format `alternate_undo` to `.alternateUndo`
        case "alternate_undo" : self = .alternateUndo

        /// Format `birthday_cake` to `.birthdayCake`
        case "birthday_cake" : self = .birthdayCake

        /// Format `truck_monster` to `.truckMonster`
        case "truck_monster" : self = .truckMonster

        /// Format `unlock` to `.unlock`
        case "unlock" : self = .unlock

        /// Format `cloud_with_heavy_showers` to `.cloudWithHeavyShowers`
        case "cloud_with_heavy_showers" : self = .cloudWithHeavyShowers

        /// Format `user_friends` to `.userFriends`
        case "user_friends" : self = .userFriends

        /// Format `calendar` to `.calendar`
        case "calendar" : self = .calendar

        /// Format `tags` to `.tags`
        case "tags" : self = .tags

        /// Format `paint_roller` to `.paintRoller`
        case "paint_roller" : self = .paintRoller

        /// Format `cloud_with_sun` to `.cloudWithSun`
        case "cloud_with_sun" : self = .cloudWithSun

        /// Format `grip_lines_vertical` to `.gripLinesVertical`
        case "grip_lines_vertical" : self = .gripLinesVertical

        /// Format `store` to `.store`
        case "store" : self = .store

        /// Format `comment_slash` to `.commentSlash`
        case "comment_slash" : self = .commentSlash

        /// Format `alternative_trash_restore` to `.alternativeTrashRestore`
        case "alternative_trash_restore" : self = .alternativeTrashRestore

        /// Format `dice_four` to `.diceFour`
        case "dice_four" : self = .diceFour

        /// Format `dice_d20` to `.diceD20`
        case "dice_d20" : self = .diceD20

        /// Format `alternate_long_arrow_down` to `.alternateLongArrowDown`
        case "alternate_long_arrow_down" : self = .alternateLongArrowDown

        /// Format `alternate_arrow_circle_up` to `.alternateArrowCircleUp`
        case "alternate_arrow_circle_up" : self = .alternateArrowCircleUp

        /// Format `damaged_house` to `.damagedHouse`
        case "damaged_house" : self = .damagedHouse

        /// Format `check_square` to `.checkSquare`
        case "check_square" : self = .checkSquare

        /// Format `step-forward` to `.stepForward`
        case "step-forward" : self = .stepForward

        /// Format `laughing_squinting_face` to `.laughingSquintingFace`
        case "laughing_squinting_face" : self = .laughingSquintingFace

        /// Format `box_open` to `.boxOpen`
        case "box_open" : self = .boxOpen

        /// Format `tools` to `.tools`
        case "tools" : self = .tools

        /// Format `clipboard` to `.clipboard`
        case "clipboard" : self = .clipboard

        /// Format `vihara` to `.vihara`
        case "vihara" : self = .vihara

        /// Format `flag` to `.flag`
        case "flag" : self = .flag

        /// Format `pizza_slice` to `.pizzaSlice`
        case "pizza_slice" : self = .pizzaSlice

        /// Format `money_bill` to `.moneyBill`
        case "money_bill" : self = .moneyBill

        /// Format `envelope_open-text` to `.envelopeOpenText`
        case "envelope_open-text" : self = .envelopeOpenText

        /// Format `registered_trademark` to `.registeredTrademark`
        case "registered_trademark" : self = .registeredTrademark

        /// Format `location-arrow` to `.locationArrow`
        case "location-arrow" : self = .locationArrow

        /// Format `medical_clinic` to `.medicalClinic`
        case "medical_clinic" : self = .medicalClinic

        /// Format `alternate_shield` to `.alternateShield`
        case "alternate_shield" : self = .alternateShield

        /// Format `thumbs-down` to `.thumbsDown`
        case "thumbs-down" : self = .thumbsDown

        /// Format `gift` to `.gift`
        case "gift" : self = .gift

        /// Format `powerpoint_file` to `.powerpointFile`
        case "powerpoint_file" : self = .powerpointFile

        /// Format `television` to `.television`
        case "television" : self = .television

        /// Format `cocktail` to `.cocktail`
        case "cocktail" : self = .cocktail

        /// Format `chevron_circle_up` to `.chevronCircleUp`
        case "chevron_circle_up" : self = .chevronCircleUp

        /// Format `table` to `.table`
        case "table" : self = .table

        /// Format `arrow-left` to `.arrowLeft`
        case "arrow-left" : self = .arrowLeft

        /// Format `archive` to `.archive`
        case "archive" : self = .archive

        /// Format `award` to `.award`
        case "award" : self = .award

        /// Format `hospital` to `.hospital`
        case "hospital" : self = .hospital

        /// Format `sort` to `.sort`
        case "sort" : self = .sort

        /// Format `history` to `.history`
        case "history" : self = .history

        /// Format `sort_amount_down` to `.sortAmountDown`
        case "sort_amount_down" : self = .sortAmountDown

        /// Format `window_maximize` to `.windowMaximize`
        case "window_maximize" : self = .windowMaximize

        /// Format `square_full` to `.squareFull`
        case "square_full" : self = .squareFull

        /// Format `book_of_the_dead` to `.bookOfTheDead`
        case "book_of_the_dead" : self = .bookOfTheDead

        /// Format `cut` to `.cut`
        case "cut" : self = .cut

        /// Format `sort_down_(descending)` to `.sortDownDescending`
        case "sort_down_(descending)" : self = .sortDownDescending

        /// Format `flask` to `.flask`
        case "flask" : self = .flask

        /// Format `desktop` to `.desktop`
        case "desktop" : self = .desktop

        /// Format `window_restore` to `.windowRestore`
        case "window_restore" : self = .windowRestore

        /// Format `alternate_level_down` to `.alternateLevelDown`
        case "alternate_level_down" : self = .alternateLevelDown

        /// Format `photo_video` to `.photoVideo`
        case "photo_video" : self = .photoVideo

        /// Format `bible` to `.bible`
        case "bible" : self = .bible

        /// Format `ethernet` to `.ethernet`
        case "ethernet" : self = .ethernet

        /// Format `spinner` to `.spinner`
        case "spinner" : self = .spinner

        /// Format `hamsa` to `.hamsa`
        case "hamsa" : self = .hamsa

        /// Format `audio_file` to `.audioFile`
        case "audio_file" : self = .audioFile

        /// Format `scissors_(hand)` to `.scissorsHand`
        case "scissors_(hand)" : self = .scissorsHand

        /// Format `sitemap` to `.sitemap`
        case "sitemap" : self = .sitemap

        /// Format `teeth_open` to `.teethOpen`
        case "teeth_open" : self = .teethOpen

        /// Format `video` to `.video`
        case "video" : self = .video

        /// Format `tired_face` to `.tiredFace`
        case "tired_face" : self = .tiredFace

        /// Format `allergies` to `.allergies`
        case "allergies" : self = .allergies

        /// Format `dice_five` to `.diceFive`
        case "dice_five" : self = .diceFive

        /// Format `users_cog` to `.usersCog`
        case "users_cog" : self = .usersCog

        /// Format `hard_hat` to `.hardHat`
        case "hard_hat" : self = .hardHat

        /// Format `american_sign_language_interpreting` to `.americanSignLanguageInterpreting`
        case "american_sign_language_interpreting" : self = .americanSignLanguageInterpreting

        /// Format `parking` to `.parking`
        case "parking" : self = .parking

        /// Format `hryvnia` to `.hryvnia`
        case "hryvnia" : self = .hryvnia

        /// Format `grinning_squinting_face` to `.grinningSquintingFace`
        case "grinning_squinting_face" : self = .grinningSquintingFace

        /// Format `word_file` to `.wordFile`
        case "word_file" : self = .wordFile

        /// Format `shower` to `.shower`
        case "shower" : self = .shower

        /// Format `doctor` to `.doctor`
        case "doctor" : self = .doctor

        /// Format `angry_face` to `.angryFace`
        case "angry_face" : self = .angryFace

        /// Format `car` to `.car`
        case "car" : self = .car

        /// Format `equals` to `.equals`
        case "equals" : self = .equals

        /// Format `chalkboard_teacher` to `.chalkboardTeacher`
        case "chalkboard_teacher" : self = .chalkboardTeacher

        /// Format `concierge_bell` to `.conciergeBell`
        case "concierge_bell" : self = .conciergeBell

        /// Format `carrot` to `.carrot`
        case "carrot" : self = .carrot

        /// Format `tty` to `.tty`
        case "tty" : self = .tty

        /// Format `language` to `.language`
        case "language" : self = .language

        /// Format `video_slash` to `.videoSlash`
        case "video_slash" : self = .videoSlash

        /// Format `alternate_radiation` to `.alternateRadiation`
        case "alternate_radiation" : self = .alternateRadiation

        /// Format `phone_volume` to `.phoneVolume`
        case "phone_volume" : self = .phoneVolume

        /// Format `hourglass_end` to `.hourglassEnd`
        case "hourglass_end" : self = .hourglassEnd

        /// Format `file_prescription` to `.filePrescription`
        case "file_prescription" : self = .filePrescription

        /// Format `alternate_feather` to `.alternateFeather`
        case "alternate_feather" : self = .alternateFeather

        /// Format `th-list` to `.thList`
        case "th-list" : self = .thList

        /// Format `battery_empty` to `.batteryEmpty`
        case "battery_empty" : self = .batteryEmpty

        /// Format `guitar` to `.guitar`
        case "guitar" : self = .guitar

        /// Format `angle_double_right` to `.angleDoubleRight`
        case "angle_double_right" : self = .angleDoubleRight

        /// Format `first_aid` to `.firstAid`
        case "first_aid" : self = .firstAid

        /// Format `dolly` to `.dolly`
        case "dolly" : self = .dolly

        /// Format `chess_board` to `.chessBoard`
        case "chess_board" : self = .chessBoard

        /// Format `cat` to `.cat`
        case "cat" : self = .cat

        /// Format `identification_badge` to `.identificationBadge`
        case "identification_badge" : self = .identificationBadge

        /// Format `bong` to `.bong`
        case "bong" : self = .bong

        /// Format `moon` to `.moon`
        case "moon" : self = .moon

        /// Format `passport` to `.passport`
        case "passport" : self = .passport

        /// Format `drafting_compass` to `.draftingCompass`
        case "drafting_compass" : self = .draftingCompass

        /// Format `dumbbell` to `.dumbbell`
        case "dumbbell" : self = .dumbbell

        /// Format `copy` to `.copy`
        case "copy" : self = .copy

        /// Format `volume_off` to `.volumeOff`
        case "volume_off" : self = .volumeOff

        /// Format `alternate_hospital` to `.alternateHospital`
        case "alternate_hospital" : self = .alternateHospital

        /// Format `upload` to `.upload`
        case "upload" : self = .upload

        /// Format `comment_dollar` to `.commentDollar`
        case "comment_dollar" : self = .commentDollar

        /// Format `icons` to `.icons`
        case "icons" : self = .icons

        /// Format `x-ray` to `.xRay`
        case "x-ray" : self = .xRay

        /// Format `face_with_tongue` to `.faceWithTongue`
        case "face_with_tongue" : self = .faceWithTongue

        /// Format `balance_scale_(right-weighted)` to `.balanceScaleRightWeighted`
        case "balance_scale_(right-weighted)" : self = .balanceScaleRightWeighted

        /// Format `map_marked` to `.mapMarked`
        case "map_marked" : self = .mapMarked

        /// Format `futbol` to `.futbol`
        case "futbol" : self = .futbol

        /// Format `caret_right` to `.caretRight`
        case "caret_right" : self = .caretRight

        /// Format `pastafarianism` to `.pastafarianism`
        case "pastafarianism" : self = .pastafarianism

        /// Format `rss` to `.rss`
        case "rss" : self = .rss

        /// Format `shekel_sign` to `.shekelSign`
        case "shekel_sign" : self = .shekelSign

        /// Format `frowning_face_with_open_mouth` to `.frowningFaceWithOpenMouth`
        case "frowning_face_with_open_mouth" : self = .frowningFaceWithOpenMouth

        /// Format `chevron-left` to `.chevronLeft`
        case "chevron-left" : self = .chevronLeft

        /// Format `align-center` to `.alignCenter`
        case "align-center" : self = .alignCenter

        /// Format `tenge` to `.tenge`
        case "tenge" : self = .tenge

        /// Format `bullhorn` to `.bullhorn`
        case "bullhorn" : self = .bullhorn

        /// Format `swatchbook` to `.swatchbook`
        case "swatchbook" : self = .swatchbook

        /// Format `quran` to `.quran`
        case "quran" : self = .quran

        /// Format `hot_tub` to `.hotTub`
        case "hot_tub" : self = .hotTub

        /// Format `ruler_horizontal` to `.rulerHorizontal`
        case "ruler_horizontal" : self = .rulerHorizontal

        /// Format `bowling_ball` to `.bowlingBall`
        case "bowling_ball" : self = .bowlingBall

        /// Format `i_beam_cursor` to `.iBeamCursor`
        case "i_beam_cursor" : self = .iBeamCursor

        /// Format `snowman` to `.snowman`
        case "snowman" : self = .snowman

        /// Format `globe_with_americas_shown` to `.globeWithAmericasShown`
        case "globe_with_americas_shown" : self = .globeWithAmericasShown

        /// Format `seedling` to `.seedling`
        case "seedling" : self = .seedling

        /// Format `capsules` to `.capsules`
        case "capsules" : self = .capsules

        /// Format `jedi` to `.jedi`
        case "jedi" : self = .jedi

        /// Format `theater_masks` to `.theaterMasks`
        case "theater_masks" : self = .theaterMasks

        /// Format `bullseye` to `.bullseye`
        case "bullseye" : self = .bullseye

        /// Format `wizard's_hat` to `.wizardSHat`
        case "wizard's_hat" : self = .wizardSHat

        /// Format `hand_pointing_down` to `.handPointingDown`
        case "hand_pointing_down" : self = .handPointingDown

        /// Format `arrow_circle_up` to `.arrowCircleUp`
        case "arrow_circle_up" : self = .arrowCircleUp

        /// Format `ribbon` to `.ribbon`
        case "ribbon" : self = .ribbon

        /// Format `pencil_ruler` to `.pencilRuler`
        case "pencil_ruler" : self = .pencilRuler

        /// Format `hand_with_middle_finger_raised` to `.handWithMiddleFingerRaised`
        case "hand_with_middle_finger_raised" : self = .handWithMiddleFingerRaised

        /// Format `chevron-up` to `.chevronUp`
        case "chevron-up" : self = .chevronUp

        /// Format `dove` to `.dove`
        case "dove" : self = .dove

        /// Format `broadcast_tower` to `.broadcastTower`
        case "broadcast_tower" : self = .broadcastTower

        /// Format `vial` to `.vial`
        case "vial" : self = .vial

        /// Format `percentage` to `.percentage`
        case "percentage" : self = .percentage

        /// Format `street_view` to `.streetView`
        case "street_view" : self = .streetView

        /// Format `rss_square` to `.rSSSquare`
        case "rss_square" : self = .rSSSquare

        /// Format `alternate_level_up` to `.alternateLevelUp`
        case "alternate_level_up" : self = .alternateLevelUp

        /// Format `piggy_bank` to `.piggyBank`
        case "piggy_bank" : self = .piggyBank

        /// Format `assistive_listening_systems` to `.assistiveListeningSystems`
        case "assistive_listening_systems" : self = .assistiveListeningSystems

        /// Format `ad` to `.ad`
        case "ad" : self = .ad

        /// Format `shoe_prints` to `.shoePrints`
        case "shoe_prints" : self = .shoePrints

        /// Format `tag` to `.tag`
        case "tag" : self = .tag

        /// Format `user_injured` to `.userInjured`
        case "user_injured" : self = .userInjured

        /// Format `sort_numeric_down` to `.sortNumericDown`
        case "sort_numeric_down" : self = .sortNumericDown

        /// Format `life_ring` to `.lifeRing`
        case "life_ring" : self = .lifeRing

        /// Format `circle_notched` to `.circleNotched`
        case "circle_notched" : self = .circleNotched

        /// Format `wind` to `.wind`
        case "wind" : self = .wind

        /// Format `hand_pointing_up` to `.handPointingUp`
        case "hand_pointing_up" : self = .handPointingUp

        /// Format `audio_description` to `.audioDescription`
        case "audio_description" : self = .audioDescription

        /// Format `sort_amount_up` to `.sortAmountUp`
        case "sort_amount_up" : self = .sortAmountUp

        /// Format `search_dollar` to `.searchDollar`
        case "search_dollar" : self = .searchDollar

        /// Format `snowflake` to `.snowflake`
        case "snowflake" : self = .snowflake

        /// Format `mars_stroke_horizontal` to `.marsStrokeHorizontal`
        case "mars_stroke_horizontal" : self = .marsStrokeHorizontal

        /// Format `fingerprint` to `.fingerprint`
        case "fingerprint" : self = .fingerprint

        /// Format `trophy` to `.trophy`
        case "trophy" : self = .trophy

        /// Format `image_file` to `.imageFile`
        case "image_file" : self = .imageFile

        /// Format `play` to `.play`
        case "play" : self = .play

        /// Format `columns` to `.columns`
        case "columns" : self = .columns

        /// Format `alternate_headphones` to `.alternateHeadphones`
        case "alternate_headphones" : self = .alternateHeadphones

        /// Format `bicycle` to `.bicycle`
        case "bicycle" : self = .bicycle

        /// Format `subscript` to `.`subscript``
        case "subscript" : self = .`subscript`

        /// Format `identification_card` to `.identificationCard`
        case "identification_card" : self = .identificationCard

        /// Format `clipboard_with_check` to `.clipboardWithCheck`
        case "clipboard_with_check" : self = .clipboardWithCheck

        /// Format `poo` to `.poo`
        case "poo" : self = .poo

        /// Format `football_ball` to `.footballBall`
        case "football_ball" : self = .footballBall

        /// Format `boxes` to `.boxes`
        case "boxes" : self = .boxes

        /// Format `fire-extinguisher` to `.fireExtinguisher`
        case "fire-extinguisher" : self = .fireExtinguisher

        /// Format `alternate_expand_arrows` to `.alternateExpandArrows`
        case "alternate_expand_arrows" : self = .alternateExpandArrows

        /// Format `couch` to `.couch`
        case "couch" : self = .couch

        /// Format `pause_circle` to `.pauseCircle`
        case "pause_circle" : self = .pauseCircle

        /// Format `tablets` to `.tablets`
        case "tablets" : self = .tablets

        /// Format `digital_tachograph` to `.digitalTachograph`
        case "digital_tachograph" : self = .digitalTachograph

        /// Format `border_style` to `.borderStyle`
        case "border_style" : self = .borderStyle

        /// Format `heart_broken` to `.heartBroken`
        case "heart_broken" : self = .heartBroken

        /// Format `certificate` to `.certificate`
        case "certificate" : self = .certificate

        /// Format `hospital_symbol` to `.hospitalSymbol`
        case "hospital_symbol" : self = .hospitalSymbol

        /// Format `redo` to `.redo`
        case "redo" : self = .redo

        /// Format `alternate_redo` to `.alternateRedo`
        case "alternate_redo" : self = .alternateRedo

        /// Format `crying_face` to `.cryingFace`
        case "crying_face" : self = .cryingFace

        /// Format `caret_left` to `.caretLeft`
        case "caret_left" : self = .caretLeft

        /// Format `object_ungroup` to `.objectUngroup`
        case "object_ungroup" : self = .objectUngroup

        /// Format `deaf` to `.deaf`
        case "deaf" : self = .deaf

        /// Format `umbrella_beach` to `.umbrellaBeach`
        case "umbrella_beach" : self = .umbrellaBeach

        /// Format `hand_holding_us_dollar` to `.handHoldingUSDollar`
        case "hand_holding_us_dollar" : self = .handHoldingUSDollar

        /// Format `slash` to `.slash`
        case "slash" : self = .slash

        /// Format `paper_plane` to `.paperPlane`
        case "paper_plane" : self = .paperPlane

        /// Format `pen_nib` to `.penNib`
        case "pen_nib" : self = .penNib

        /// Format `alternate_mobile` to `.alternateMobile`
        case "alternate_mobile" : self = .alternateMobile

        /// Format `bug` to `.bug`
        case "bug" : self = .bug

        /// Format `volume_down` to `.volumeDown`
        case "volume_down" : self = .volumeDown

        /// Format `map-marker` to `.mapMarker`
        case "map-marker" : self = .mapMarker

        /// Format `cowboy_hat` to `.cowboyHat`
        case "cowboy_hat" : self = .cowboyHat

        /// Format `save` to `.save`
        case "save" : self = .save

        /// Format `tape` to `.tape`
        case "tape" : self = .tape

        /// Format `underline` to `.underline`
        case "underline" : self = .underline

        /// Format `user_slash` to `.userSlash`
        case "user_slash" : self = .userSlash

        /// Format `smoking` to `.smoking`
        case "smoking" : self = .smoking

        /// Format `user_tag` to `.userTag`
        case "user_tag" : self = .userTag

        /// Format `angle_double_up` to `.angleDoubleUp`
        case "angle_double_up" : self = .angleDoubleUp

        /// Format `beer` to `.beer`
        case "beer" : self = .beer

        /// Format `grimacing_face` to `.grimacingFace`
        case "grimacing_face" : self = .grimacingFace

        /// Format `glass_cheers` to `.glassCheers`
        case "glass_cheers" : self = .glassCheers

        /// Format `hamburger` to `.hamburger`
        case "hamburger" : self = .hamburger

        /// Format `candy_cane` to `.candyCane`
        case "candy_cane" : self = .candyCane

        /// Format `suitcase` to `.suitcase`
        case "suitcase" : self = .suitcase

        /// Format `neuter` to `.neuter`
        case "neuter" : self = .neuter

        /// Format `map` to `.map`
        case "map" : self = .map

        /// Format `comment` to `.comment`
        case "comment" : self = .comment

        /// Format `weight` to `.weight`
        case "weight" : self = .weight

        /// Format `spray_can` to `.sprayCan`
        case "spray_can" : self = .sprayCan

        /// Format `alternate_sort_amount_up` to `.alternateSortAmountUp`
        case "alternate_sort_amount_up" : self = .alternateSortAmountUp

        /// Format `alternate_map_marked` to `.alternateMapMarked`
        case "alternate_map_marked" : self = .alternateMapMarked

        /// Format `chess_queen` to `.chessQueen`
        case "chess_queen" : self = .chessQueen

        /// Format `alternate_arrow_circle_left` to `.alternateArrowCircleLeft`
        case "alternate_arrow_circle_left" : self = .alternateArrowCircleLeft

        /// Format `dice_six` to `.diceSix`
        case "dice_six" : self = .diceSix

        /// Format `lightbulb` to `.lightbulb`
        case "lightbulb" : self = .lightbulb

        /// Format `chevron_circle_down` to `.chevronCircleDown`
        case "chevron_circle_down" : self = .chevronCircleDown

        /// Format `satellite` to `.satellite`
        case "satellite" : self = .satellite

        /// Format `horizontal_sliders` to `.horizontalSliders`
        case "horizontal_sliders" : self = .horizontalSliders

        /// Format `alternate_store` to `.alternateStore`
        case "alternate_store" : self = .alternateStore

        /// Format `search` to `.search`
        case "search" : self = .search

        /// Format `sort_alphabetical_down` to `.sortAlphabeticalDown`
        case "sort_alphabetical_down" : self = .sortAlphabeticalDown

        /// Format `align-right` to `.alignRight`
        case "align-right" : self = .alignRight

        /// Format `fast-forward` to `.fastForward`
        case "fast-forward" : self = .fastForward

        /// Format `glasses` to `.glasses`
        case "glasses" : self = .glasses

        /// Format `tram` to `.tram`
        case "tram" : self = .tram

        /// Format `restroom` to `.restroom`
        case "restroom" : self = .restroom

        /// Format `mercury` to `.mercury`
        case "mercury" : self = .mercury

        /// Format `church` to `.church`
        case "church" : self = .church

        /// Format `ruble_sign` to `.rubleSign`
        case "ruble_sign" : self = .rubleSign

        /// Format `less_than` to `.lessThan`
        case "less_than" : self = .lessThan

        /// Format `search_location` to `.searchLocation`
        case "search_location" : self = .searchLocation

        /// Format `user_ninja` to `.userNinja`
        case "user_ninja" : self = .userNinja

        /// Format `euro_sign` to `.euroSign`
        case "euro_sign" : self = .euroSign

        /// Format `t-shirt` to `.tShirt`
        case "t-shirt" : self = .tShirt

        /// Format `hanukiah` to `.hanukiah`
        case "hanukiah" : self = .hanukiah

        /// Format `random` to `.random`
        case "random" : self = .random

        /// Format `lightning_bolt` to `.lightningBolt`
        case "lightning_bolt" : self = .lightningBolt

        /// Format `cloud_with_rain` to `.cloudWithRain`
        case "cloud_with_rain" : self = .cloudWithRain

        /// Format `grip_horizontal` to `.gripHorizontal`
        case "grip_horizontal" : self = .gripHorizontal

        /// Format `microphone_slash` to `.microphoneSlash`
        case "microphone_slash" : self = .microphoneSlash

        /// Format `battery_3/4_full` to `.battery34Full`
        case "battery_3/4_full" : self = .battery34Full

        /// Format `transgender` to `.transgender`
        case "transgender" : self = .transgender

        /// Format `snowboarding` to `.snowboarding`
        case "snowboarding" : self = .snowboarding

        /// Format `mail_bulk` to `.mailBulk`
        case "mail_bulk" : self = .mailBulk

        /// Format `database` to `.database`
        case "database" : self = .database

        /// Format `hourglass_start` to `.hourglassStart`
        case "hourglass_start" : self = .hourglassStart

        /// Format `record_vinyl` to `.recordVinyl`
        case "record_vinyl" : self = .recordVinyl

        /// Format `motorcycle` to `.motorcycle`
        case "motorcycle" : self = .motorcycle

        /// Format `credit_card` to `.creditCard`
        case "credit_card" : self = .creditCard

        /// Format `alternate_share` to `.alternateShare`
        case "alternate_share" : self = .alternateShare

        /// Format `chess_pawn` to `.chessPawn`
        case "chess_pawn" : self = .chessPawn

        /// Format `gas_pump` to `.gasPump`
        case "gas_pump" : self = .gasPump

        /// Format `atlas` to `.atlas`
        case "atlas" : self = .atlas

        /// Format `alternate_medical_chat` to `.alternateMedicalChat`
        case "alternate_medical_chat" : self = .alternateMedicalChat

        /// Format `alternate_ticket` to `.alternateTicket`
        case "alternate_ticket" : self = .alternateTicket

        /// Format `grinning_face_with_big_eyes` to `.grinningFaceWithBigEyes`
        case "grinning_face_with_big_eyes" : self = .grinningFaceWithBigEyes

        /// Format `hand_pointing_left` to `.handPointingLeft`
        case "hand_pointing_left" : self = .handPointingLeft

        /// Format `frowning_face` to `.frowningFace`
        case "frowning_face" : self = .frowningFace

        /// Format `winking_face_with_tongue` to `.winkingFaceWithTongue`
        case "winking_face_with_tongue" : self = .winkingFaceWithTongue

        /// Format `lemon` to `.lemon`
        case "lemon" : self = .lemon

        /// Format `angle-left` to `.angleLeft`
        case "angle-left" : self = .angleLeft

        /// Format `child` to `.child`
        case "child" : self = .child

        /// Format `times_circle` to `.timesCircle`
        case "times_circle" : self = .timesCircle

        /// Format `campground` to `.campground`
        case "campground" : self = .campground

        /// Format `pills` to `.pills`
        case "pills" : self = .pills

        /// Format `thermometer_1/4_full` to `.thermometer14Full`
        case "thermometer_1/4_full" : self = .thermometer14Full

        /// Format `gavel` to `.gavel`
        case "gavel" : self = .gavel

        /// Format `map_pin` to `.mapPin`
        case "map_pin" : self = .mapPin

        /// Format `cash_register` to `.cashRegister`
        case "cash_register" : self = .cashRegister

        /// Format `basketball_ball` to `.basketballBall`
        case "basketball_ball" : self = .basketballBall

        /// Format `hands` to `.hands`
        case "hands" : self = .hands

        /// Format `alternate_phone_square` to `.alternatePhoneSquare`
        case "alternate_phone_square" : self = .alternatePhoneSquare

        /// Format `oil_can` to `.oilCan`
        case "oil_can" : self = .oilCan

        /// Format `pound_sign` to `.poundSign`
        case "pound_sign" : self = .poundSign

        /// Format `hippo` to `.hippo`
        case "hippo" : self = .hippo

        /// Format `ice_cream` to `.iceCream`
        case "ice_cream" : self = .iceCream

        /// Format `alternate_car` to `.alternateCar`
        case "alternate_car" : self = .alternateCar

        /// Format `heartbeat` to `.heartbeat`
        case "heartbeat" : self = .heartbeat

        /// Format `running` to `.running`
        case "running" : self = .running

        /// Format `ambulance` to `.ambulance`
        case "ambulance" : self = .ambulance

        /// Format `satellite_dish` to `.satelliteDish`
        case "satellite_dish" : self = .satelliteDish

        /// Format `briefcase` to `.briefcase`
        case "briefcase" : self = .briefcase

        /// Format `book_open` to `.bookOpen`
        case "book_open" : self = .bookOpen

        /// Format `volume_up` to `.volumeUp`
        case "volume_up" : self = .volumeUp

        /// Format `alternate_crop` to `.alternateCrop`
        case "alternate_crop" : self = .alternateCrop

        /// Format `dharmachakra` to `.dharmachakra`
        case "dharmachakra" : self = .dharmachakra

        /// Format `brush` to `.brush`
        case "brush" : self = .brush

        /// Format `bath` to `.bath`
        case "bath" : self = .bath

        /// Format `low_temperature` to `.lowTemperature`
        case "low_temperature" : self = .lowTemperature

        /// Format `crutch` to `.crutch`
        case "crutch" : self = .crutch

        /// Format `arrow-up` to `.arrowUp`
        case "arrow-up" : self = .arrowUp

        /// Format `excel_file` to `.excelFile`
        case "excel_file" : self = .excelFile

        /// Format `stopwatch` to `.stopwatch`
        case "stopwatch" : self = .stopwatch

        /// Format `outdent` to `.outdent`
        case "outdent" : self = .outdent

        /// Format `expand` to `.expand`
        case "expand" : self = .expand

        /// Format `praying_hands` to `.prayingHands`
        case "praying_hands" : self = .prayingHands

        /// Format `wired_network` to `.wiredNetwork`
        case "wired_network" : self = .wiredNetwork

        /// Format `user_tie` to `.userTie`
        case "user_tie" : self = .userTie

        /// Format `broom` to `.broom`
        case "broom" : self = .broom

        /// Format `alternate_sync` to `.alternateSync`
        case "alternate_sync" : self = .alternateSync

        /// Format `baseball_ball` to `.baseballBall`
        case "baseball_ball" : self = .baseballBall

        /// Format `dizzy_face` to `.dizzyFace`
        case "dizzy_face" : self = .dizzyFace

        /// Format `low_vision` to `.lowVision`
        case "low_vision" : self = .lowVision

        /// Format `alternate_pen` to `.alternatePen`
        case "alternate_pen" : self = .alternatePen

        /// Format `medal` to `.medal`
        case "medal" : self = .medal

        /// Format `cloud_with_moon_and_rain` to `.cloudWithMoonAndRain`
        case "cloud_with_moon_and_rain" : self = .cloudWithMoonAndRain

        /// Format `laughing_winking_face` to `.laughingWinkingFace`
        case "laughing_winking_face" : self = .laughingWinkingFace

        /// Format `user_clock` to `.userClock`
        case "user_clock" : self = .userClock

        /// Format `joint` to `.joint`
        case "joint" : self = .joint

        /// Format `toilet_paper` to `.toiletPaper`
        case "toilet_paper" : self = .toiletPaper

        /// Format `wavy_money_bill` to `.wavyMoneyBill`
        case "wavy_money_bill" : self = .wavyMoneyBill

        /// Format `lock` to `.lock`
        case "lock" : self = .lock

        /// Format `flushed_face` to `.flushedFace`
        case "flushed_face" : self = .flushedFace

        /// Format `dice_two` to `.diceTwo`
        case "dice_two" : self = .diceTwo

        /// Format `chess_king` to `.chessKing`
        case "chess_king" : self = .chessKing

        /// Format `car_crash` to `.carCrash`
        case "car_crash" : self = .carCrash

        /// Format `dot_circle` to `.dotCircle`
        case "dot_circle" : self = .dotCircle

        /// Format `alternate_phone` to `.alternatePhone`
        case "alternate_phone" : self = .alternatePhone

        /// Format `cloud_with_moon` to `.cloudWithMoon`
        case "cloud_with_moon" : self = .cloudWithMoon

        /// Format `caret_up` to `.caretUp`
        case "caret_up" : self = .caretUp

        /// Format `mortar_pestle` to `.mortarPestle`
        case "mortar_pestle" : self = .mortarPestle

        /// Format `dice` to `.dice`
        case "dice" : self = .dice

        /// Format `image` to `.image`
        case "image" : self = .image

        /// Format `alternate_cloud_upload` to `.alternateCloudUpload`
        case "alternate_cloud_upload" : self = .alternateCloudUpload

        /// Format `fighter-jet` to `.fighterJet`
        case "fighter-jet" : self = .fighterJet

        /// Format `alternate_comment` to `.alternateComment`
        case "alternate_comment" : self = .alternateComment

        /// Format `braille` to `.braille`
        case "braille" : self = .braille

        /// Format `hourglass_half` to `.hourglassHalf`
        case "hourglass_half" : self = .hourglassHalf

        /// Format `memory` to `.memory`
        case "memory" : self = .memory

        /// Format `check_circle` to `.checkCircle`
        case "check_circle" : self = .checkCircle

        /// Format `chess_rook` to `.chessRook`
        case "chess_rook" : self = .chessRook

        /// Format `user_edit` to `.userEdit`
        case "user_edit" : self = .userEdit

        /// Format `cross` to `.cross`
        case "cross" : self = .cross

        /// Format `recycle` to `.recycle`
        case "recycle" : self = .recycle

        /// Format `voicemail` to `.voicemail`
        case "voicemail" : self = .voicemail

        /// Format `kiwi_bird` to `.kiwiBird`
        case "kiwi_bird" : self = .kiwiBird

        /// Format `link` to `.link`
        case "link" : self = .link

        /// Format `helping_hands` to `.helpingHands`
        case "helping_hands" : self = .helpingHands

        /// Format `user_plus` to `.userPlus`
        case "user_plus" : self = .userPlus

        /// Format `palette` to `.palette`
        case "palette" : self = .palette

        /// Format `diagnoses` to `.diagnoses`
        case "diagnoses" : self = .diagnoses

        /// Format `clone` to `.clone`
        case "clone" : self = .clone

        /// Format `heading` to `.heading`
        case "heading" : self = .heading

        /// Format `traffic_light` to `.trafficLight`
        case "traffic_light" : self = .trafficLight

        /// Format `alternate_long_arrow_left` to `.alternateLongArrowLeft`
        case "alternate_long_arrow_left" : self = .alternateLongArrowLeft

        /// Format `shipping_fast` to `.shippingFast`
        case "shipping_fast" : self = .shippingFast

        /// Format `lock_open` to `.lockOpen`
        case "lock_open" : self = .lockOpen

        /// Format `route` to `.route`
        case "route" : self = .route

        /// Format `blender_phone` to `.blenderPhone`
        case "blender_phone" : self = .blenderPhone

        /// Format `toggle_on` to `.toggleOn`
        case "toggle_on" : self = .toggleOn

        /// Format `skiing_nordic` to `.skiingNordic`
        case "skiing_nordic" : self = .skiingNordic

        /// Format `alternate_wavy_money_bill` to `.alternateWavyMoneyBill`
        case "alternate_wavy_money_bill" : self = .alternateWavyMoneyBill

        /// Format `cubes` to `.cubes`
        case "cubes" : self = .cubes

        /// Format `arrow-down` to `.arrowDown`
        case "arrow-down" : self = .arrowDown

        /// Format `alternate_transgender` to `.alternateTransgender`
        case "alternate_transgender" : self = .alternateTransgender

        /// Format `plane_arrival` to `.planeArrival`
        case "plane_arrival" : self = .planeArrival

        /// Format `flag-checkered` to `.flagCheckered`
        case "flag-checkered" : self = .flagCheckered

        /// Format `alternate_pencil` to `.alternatePencil`
        case "alternate_pencil" : self = .alternatePencil

        /// Format `pdf_file` to `.pDFFile`
        case "pdf_file" : self = .pDFFile

        /// Format `sim_card` to `.sIMCard`
        case "sim_card" : self = .sIMCard

        /// Format `hand_holding_heart` to `.handHoldingHeart`
        case "hand_holding_heart" : self = .handHoldingHeart

        /// Format `divide` to `.divide`
        case "divide" : self = .divide

        /// Format `alternate_long_arrow_up` to `.alternateLongArrowUp`
        case "alternate_long_arrow_up" : self = .alternateLongArrowUp

        /// Format `plus` to `.plus`
        case "plus" : self = .plus

        /// Format `thermometer` to `.thermometer`
        case "thermometer" : self = .thermometer

        /// Format `times` to `.times`
        case "times" : self = .times

        /// Format `fish` to `.fish`
        case "fish" : self = .fish

        /// Format `quote-right` to `.quoteRight`
        case "quote-right" : self = .quoteRight

        /// Format `trash` to `.trash`
        case "trash" : self = .trash

        /// Format `university` to `.university`
        case "university" : self = .university

        /// Format `bus_alt` to `.busAlt`
        case "bus_alt" : self = .busAlt

        /// Format `toggle_off` to `.toggleOff`
        case "toggle_off" : self = .toggleOff

        /// Format `globe` to `.globe`
        case "globe" : self = .globe

        /// Format `face_without_mouth` to `.faceWithoutMouth`
        case "face_without_mouth" : self = .faceWithoutMouth

        /// Format `retweet` to `.retweet`
        case "retweet" : self = .retweet

        /// Format `fan` to `.fan`
        case "fan" : self = .fan

        /// Format `eject` to `.eject`
        case "eject" : self = .eject

        /// Format `border_none` to `.borderNone`
        case "border_none" : self = .borderNone

        /// Format `font_awesome_full_logo` to `.fontAwesomeFullLogo`
        case "font_awesome_full_logo" : self = .fontAwesomeFullLogo

        /// Format `pager` to `.pager`
        case "pager" : self = .pager

        /// Format `add_to_shopping_cart` to `.addToShoppingCart`
        case "add_to_shopping_cart" : self = .addToShoppingCart

        /// Format `fill` to `.fill`
        case "fill" : self = .fill

        /// Format `person_entering_booth` to `.personEnteringBooth`
        case "person_entering_booth" : self = .personEnteringBooth

        /// Format `cookie` to `.cookie`
        case "cookie" : self = .cookie

        /// Format `sign_language` to `.signLanguage`
        case "sign_language" : self = .signLanguage

        /// Format `gamepad` to `.gamepad`
        case "gamepad" : self = .gamepad

        /// Format `hiking` to `.hiking`
        case "hiking" : self = .hiking

        /// Format `bread_slice` to `.breadSlice`
        case "bread_slice" : self = .breadSlice

        /// Format `calendar_minus` to `.calendarMinus`
        case "calendar_minus" : self = .calendarMinus

        /// Format `greater_than` to `.greaterThan`
        case "greater_than" : self = .greaterThan

        /// Format `user_graduate` to `.userGraduate`
        case "user_graduate" : self = .userGraduate

        /// Format `undo` to `.undo`
        case "undo" : self = .undo

        /// Format `file_contract` to `.fileContract`
        case "file_contract" : self = .fileContract

        /// Format `alternate_arrow_circle_right` to `.alternateArrowCircleRight`
        case "alternate_arrow_circle_right" : self = .alternateArrowCircleRight

        /// Format `haykal` to `.haykal`
        case "haykal" : self = .haykal

        /// Format `kissing_face_with_smiling_eyes` to `.kissingFaceWithSmilingEyes`
        case "kissing_face_with_smiling_eyes" : self = .kissingFaceWithSmilingEyes

        /// Format `calculator` to `.calculator`
        case "calculator" : self = .calculator

        /// Format `keyboard` to `.keyboard`
        case "keyboard" : self = .keyboard

        /// Format `portrait` to `.portrait`
        case "portrait" : self = .portrait

        /// Format `mobile_phone` to `.mobilePhone`
        case "mobile_phone" : self = .mobilePhone

        /// Format `minus_square` to `.minusSquare`
        case "minus_square" : self = .minusSquare

        /// Format `compass` to `.compass`
        case "compass" : self = .compass

        /// Format `truck_moving` to `.truckMoving`
        case "truck_moving" : self = .truckMoving

        /// Format `font` to `.font`
        case "font" : self = .font

        /// Format `chair` to `.chair`
        case "chair" : self = .chair

        /// Format `chalkboard` to `.chalkboard`
        case "chalkboard" : self = .chalkboard

        /// Format `solar_panel` to `.solarPanel`
        case "solar_panel" : self = .solarPanel

        /// Format `pray` to `.pray`
        case "pray" : self = .pray

        /// Format `medkit` to `.medkit`
        case "medkit" : self = .medkit

        /// Format `train` to `.train`
        case "train" : self = .train

        /// Format `alternate_sign_in` to `.alternateSignIn`
        case "alternate_sign_in" : self = .alternateSignIn

        /// Format `martini_glass` to `.martiniGlass`
        case "martini_glass" : self = .martiniGlass

        /// Format `khanda` to `.khanda`
        case "khanda" : self = .khanda

        /// Format `map_signs` to `.mapSigns`
        case "map_signs" : self = .mapSigns

        /// Format `fire` to `.fire`
        case "fire" : self = .fire

        /// Format `h_square` to `.hSquare`
        case "h_square" : self = .hSquare

        /// Format `envelope` to `.envelope`
        case "envelope" : self = .envelope

        /// Format `calendar_plus` to `.calendarPlus`
        case "calendar_plus" : self = .calendarPlus

        /// Format `ruler_combined` to `.rulerCombined`
        case "ruler_combined" : self = .rulerCombined

        /// Format `alternate_share_square` to `.alternateShareSquare`
        case "alternate_share_square" : self = .alternateShareSquare

        /// Format `democrat` to `.democrat`
        case "democrat" : self = .democrat

        /// Format `charging_station` to `.chargingStation`
        case "charging_station" : self = .chargingStation

        /// Format `dungeon` to `.dungeon`
        case "dungeon" : self = .dungeon

        /// Format `baby` to `.baby`
        case "baby" : self = .baby

        /// Format `plus_square` to `.plusSquare`
        case "plus_square" : self = .plusSquare

        /// Format `signal` to `.signal`
        case "signal" : self = .signal

        /// Format `suitcase_rolling` to `.suitcaseRolling`
        case "suitcase_rolling" : self = .suitcaseRolling

        /// Format `splotch` to `.splotch`
        case "splotch" : self = .splotch

        /// Format `coins` to `.coins`
        case "coins" : self = .coins

        /// Format `caret_square_right` to `.caretSquareRight`
        case "caret_square_right" : self = .caretSquareRight

        /// Format `meteor` to `.meteor`
        case "meteor" : self = .meteor

        /// Format `chess_bishop` to `.chessBishop`
        case "chess_bishop" : self = .chessBishop

        /// Format `dice_one` to `.diceOne`
        case "dice_one" : self = .diceOne

        /// Format `frog` to `.frog`
        case "frog" : self = .frog

        /// Format `table_tennis` to `.tableTennis`
        case "table_tennis" : self = .tableTennis

        /// Format `text_width` to `.textWidth`
        case "text_width" : self = .textWidth

        /// Format `car_battery` to `.carBattery`
        case "car_battery" : self = .carBattery

        /// Format `folder` to `.folder`
        case "folder" : self = .folder

        /// Format `battery_1/4_full` to `.battery14Full`
        case "battery_1/4_full" : self = .battery14Full

        /// Format `prescription_bottle` to `.prescriptionBottle`
        case "prescription_bottle" : self = .prescriptionBottle

        /// Format `face_blowing_a_kiss` to `.faceBlowingAKiss`
        case "face_blowing_a_kiss" : self = .faceBlowingAKiss

        /// Format `key` to `.key`
        case "key" : self = .key

        /// Format `ruler` to `.ruler`
        case "ruler" : self = .ruler

        /// Format `arrow_circle_left` to `.arrowCircleLeft`
        case "arrow_circle_left" : self = .arrowCircleLeft

        /// Format `print` to `.print`
        case "print" : self = .print

        /// Format `alternate_wine_glas` to `.alternateWineGlas`
        case "alternate_wine_glas" : self = .alternateWineGlas

        /// Format `th` to `.th`
        case "th" : self = .th

        /// Format `volleyball_ball` to `.volleyballBall`
        case "volleyball_ball" : self = .volleyballBall

        /// Format `shopping-cart` to `.shoppingCart`
        case "shopping-cart" : self = .shoppingCart

        /// Format `baby_carriage` to `.babyCarriage`
        case "baby_carriage" : self = .babyCarriage

        /// Format `hand_pointing_right` to `.handPointingRight`
        case "hand_pointing_right" : self = .handPointingRight

        /// Format `truck_side` to `.truckSide`
        case "truck_side" : self = .truckSide

        /// Format `golf_ball` to `.golfBall`
        case "golf_ball" : self = .golfBall

        /// Format `trademark` to `.trademark`
        case "trademark" : self = .trademark

        /// Format `hand_holding` to `.handHolding`
        case "hand_holding" : self = .handHolding

        /// Format `gem` to `.gem`
        case "gem" : self = .gem

        /// Format `pie_chart` to `.pieChart`
        case "pie_chart" : self = .pieChart

        /// Format `helicopter` to `.helicopter`
        case "helicopter" : self = .helicopter

        /// Format `venus` to `.venus`
        case "venus" : self = .venus

        /// Format `alternate_sort_amount_down` to `.alternateSortAmountDown`
        case "alternate_sort_amount_down" : self = .alternateSortAmountDown

        /// Format `igloo` to `.igloo`
        case "igloo" : self = .igloo

        /// Format `alternate_square_root` to `.alternateSquareRoot`
        case "alternate_square_root" : self = .alternateSquareRoot

        /// Format `vertical_ellipsis` to `.verticalEllipsis`
        case "vertical_ellipsis" : self = .verticalEllipsis

        /// Format `address_card` to `.addressCard`
        case "address_card" : self = .addressCard

        /// Format `chess` to `.chess`
        case "chess" : self = .chess

        /// Format `rolling_on_the_floor_laughing` to `.rollingOnTheFloorLaughing`
        case "rolling_on_the_floor_laughing" : self = .rollingOnTheFloorLaughing

        /// Format `face_with_tears_of_joy` to `.faceWithTearsOfJoy`
        case "face_with_tears_of_joy" : self = .faceWithTearsOfJoy

        /// Format `mug_hot` to `.mugHot`
        case "mug_hot" : self = .mugHot

        /// Format `tree` to `.tree`
        case "tree" : self = .tree

        /// Format `hanging_weight` to `.hangingWeight`
        case "hanging_weight" : self = .hangingWeight

        /// Format `file` to `.file`
        case "file" : self = .file

        /// Format `spa` to `.spa`
        case "spa" : self = .spa

        /// Format `handshake` to `.handshake`
        case "handshake" : self = .handshake

        /// Format `paint_brush` to `.paintBrush`
        case "paint_brush" : self = .paintBrush

        /// Format `user_circle` to `.userCircle`
        case "user_circle" : self = .userCircle

        /// Format `truck_loading` to `.truckLoading`
        case "truck_loading" : self = .truckLoading

        /// Format `step-backward` to `.stepBackward`
        case "step-backward" : self = .stepBackward

        /// Format `battery_1/2_full` to `.battery12Full`
        case "battery_1/2_full" : self = .battery12Full

        /// Format `mouse` to `.mouse`
        case "mouse" : self = .mouse

        /// Format `info` to `.info`
        case "info" : self = .info

        /// Format `drum` to `.drum`
        case "drum" : self = .drum

        /// Format `brain` to `.brain`
        case "brain" : self = .brain

        /// Format `school` to `.school`
        case "school" : self = .school

        /// Format `stream` to `.stream`
        case "stream" : self = .stream

        /// Format `mosque` to `.mosque`
        case "mosque" : self = .mosque

        /// Format `alternate_microphone_slash` to `.alternateMicrophoneSlash`
        case "alternate_microphone_slash" : self = .alternateMicrophoneSlash

        /// Format `star_and_crescent` to `.starAndCrescent`
        case "star_and_crescent" : self = .starAndCrescent

        /// Format `bus` to `.bus`
        case "bus" : self = .bus

        /// Format `cardboard_vr` to `.cardboardVR`
        case "cardboard_vr" : self = .cardboardVR

        /// Format `venus_mars` to `.venusMars`
        case "venus_mars" : self = .venusMars

        /// Format `inbox` to `.inbox`
        case "inbox" : self = .inbox

        /// Format `alternate_unlock` to `.alternateUnlock`
        case "alternate_unlock" : self = .alternateUnlock

        /// Format `winking_face` to `.winkingFace`
        case "winking_face" : self = .winkingFace

        /// Format `share_square` to `.shareSquare`
        case "share_square" : self = .shareSquare

        /// Format `video_file` to `.videoFile`
        case "video_file" : self = .videoFile

        /// Format `remove_format` to `.removeFormat`
        case "remove_format" : self = .removeFormat

        /// Format `strikethrough` to `.strikethrough`
        case "strikethrough" : self = .strikethrough

        /// Format `at` to `.at`
        case "at" : self = .at

        /// Format `balance_scale` to `.balanceScale`
        case "balance_scale" : self = .balanceScale

        /// Format `dice_d6` to `.diceD6`
        case "dice_d6" : self = .diceD6

        /// Format `cannabis` to `.cannabis`
        case "cannabis" : self = .cannabis

        /// Format `grinning_face` to `.grinningFace`
        case "grinning_face" : self = .grinningFace

        /// Format `ruler_vertical` to `.rulerVertical`
        case "ruler_vertical" : self = .rulerVertical

        /// Format `toolbox` to `.toolbox`
        case "toolbox" : self = .toolbox

        /// Format `blender` to `.blender`
        case "blender" : self = .blender

        /// Format `alternate_arrows_vertical` to `.alternateArrowsVertical`
        case "alternate_arrows_vertical" : self = .alternateArrowsVertical

        /// Format `mars_double` to `.marsDouble`
        case "mars_double" : self = .marsDouble

        /// Format `ban` to `.ban`
        case "ban" : self = .ban

        /// Format `lizard_(hand)` to `.lizardHand`
        case "lizard_(hand)" : self = .lizardHand

        /// Format `smoking_ban` to `.smokingBan`
        case "smoking_ban" : self = .smokingBan

        /// Format `less_than_equal_to` to `.lessThanEqualTo`
        case "less_than_equal_to" : self = .lessThanEqualTo

        /// Format `alternate_sort_numeric_up` to `.alternateSortNumericUp`
        case "alternate_sort_numeric_up" : self = .alternateSortNumericUp

        /// Format `robot` to `.robot`
        case "robot" : self = .robot

        /// Format `ring` to `.ring`
        case "ring" : self = .ring

        /// Format `caret_square_down` to `.caretSquareDown`
        case "caret_square_down" : self = .caretSquareDown

        /// Format `fax` to `.fax`
        case "fax" : self = .fax

        /// Format `comments_dollar` to `.commentsDollar`
        case "comments_dollar" : self = .commentsDollar

        /// Format `reply-all` to `.replyAll`
        case "reply-all" : self = .replyAll

        /// Format `cogs` to `.cogs`
        case "cogs" : self = .cogs

        /// Format `alternate_arrows_horizontal` to `.alternateArrowsHorizontal`
        case "alternate_arrows_horizontal" : self = .alternateArrowsHorizontal

        /// Format `qrcode` to `.qrcode`
        case "qrcode" : self = .qrcode

        /// Format `mars` to `.mars`
        case "mars" : self = .mars

        /// Format `directions` to `.directions`
        case "directions" : self = .directions

        /// Format `bars` to `.bars`
        case "bars" : self = .bars

        /// Format `dumpster` to `.dumpster`
        case "dumpster" : self = .dumpster

        /// Format `balance_scale_(left-weighted)` to `.balanceScaleLeftWeighted`
        case "balance_scale_(left-weighted)" : self = .balanceScaleLeftWeighted

        /// Format `chevron_circle_left` to `.chevronCircleLeft`
        case "chevron_circle_left" : self = .chevronCircleLeft

        /// Format `magic` to `.magic`
        case "magic" : self = .magic

        /// Format `microscope` to `.microscope`
        case "microscope" : self = .microscope

        /// Format `dragon` to `.dragon`
        case "dragon" : self = .dragon

        /// Format `user_cog` to `.userCog`
        case "user_cog" : self = .userCog

        /// Format `book` to `.book`
        case "book" : self = .book

        /// Format `warehouse` to `.warehouse`
        case "warehouse" : self = .warehouse

        /// Format `alternate_long_arrow_right` to `.alternateLongArrowRight`
        case "alternate_long_arrow_right" : self = .alternateLongArrowRight

        /// Format `cowboy_hat_side` to `.cowboyHatSide`
        case "cowboy_hat_side" : self = .cowboyHatSide

        /// Format `alternate_grinning_face` to `.alternateGrinningFace`
        case "alternate_grinning_face" : self = .alternateGrinningFace

        /// Format `poll` to `.poll`
        case "poll" : self = .poll

        /// Format `radiation` to `.radiation`
        case "radiation" : self = .radiation

        /// Format `venus_double` to `.venusDouble`
        case "venus_double" : self = .venusDouble

        /// Format `anchor` to `.anchor`
        case "anchor" : self = .anchor

        /// Format `text-height` to `.textHeight`
        case "text-height" : self = .textHeight

        /// Format `folder_open` to `.folderOpen`
        case "folder_open" : self = .folderOpen

        /// Format `utensil_spoon` to `.utensilSpoon`
        case "utensil_spoon" : self = .utensilSpoon

        /// Format `file_signature` to `.fileSignature`
        case "file_signature" : self = .fileSignature

        /// Format `greater_than_equal_to` to `.greaterThanEqualTo`
        case "greater_than_equal_to" : self = .greaterThanEqualTo

        /// Format `graduation_cap` to `.graduationCap`
        case "graduation_cap" : self = .graduationCap

        /// Format `bold` to `.bold`
        case "bold" : self = .bold

        /// Format `thermometer_3/4_full` to `.thermometer34Full`
        case "thermometer_3/4_full" : self = .thermometer34Full

        /// Format `bone` to `.bone`
        case "bone" : self = .bone

        /// Format `mitten` to `.mitten`
        case "mitten" : self = .mitten

        /// Format `alternate_trash` to `.alternateTrash`
        case "alternate_trash" : self = .alternateTrash

        /// Format `podcast` to `.podcast`
        case "podcast" : self = .podcast

        /// Format `stop_circle` to `.stopCircle`
        case "stop_circle" : self = .stopCircle

        /// Format `crown` to `.crown`
        case "crown" : self = .crown

        /// Format `paragraph` to `.paragraph`
        case "paragraph" : self = .paragraph

        /// Format `headset` to `.headset`
        case "headset" : self = .headset

        /// Format `alternate_tablet` to `.alternateTablet`
        case "alternate_tablet" : self = .alternateTablet

        /// Format `rocket` to `.rocket`
        case "rocket" : self = .rocket

        /// Format `synagogue` to `.synagogue`
        case "synagogue" : self = .synagogue

        /// Format `laptop_medical` to `.laptopMedical`
        case "laptop_medical" : self = .laptopMedical

        /// Format `grip_vertical` to `.gripVertical`
        case "grip_vertical" : self = .gripVertical

        /// Format `landmark` to `.landmark`
        case "landmark" : self = .landmark

        /// Format `kaaba` to `.kaaba`
        case "kaaba" : self = .kaaba

        /// Format `dog` to `.dog`
        case "dog" : self = .dog

        /// Format `exclamation_triangle` to `.exclamationTriangle`
        case "exclamation_triangle" : self = .exclamationTriangle

        /// Format `thermometer_1/2_full` to `.thermometer12Full`
        case "thermometer_1/2_full" : self = .thermometer12Full

        /// Format `skull` to `.skull`
        case "skull" : self = .skull

        /// Format `republican` to `.republican`
        case "republican" : self = .republican

        /// Format `pointer_(hand)` to `.pointerHand`
        case "pointer_(hand)" : self = .pointerHand

        /// Format `hockey_puck` to `.hockeyPuck`
        case "hockey_puck" : self = .hockeyPuck

        /// Format `tint` to `.tint`
        case "tint" : self = .tint

        /// Format `envelope_open` to `.envelopeOpen`
        case "envelope_open" : self = .envelopeOpen

        /// Format `gifts` to `.gifts`
        case "gifts" : self = .gifts

        /// Format `file_csv` to `.fileCSV`
        case "file_csv" : self = .fileCSV

        /// Format `vials` to `.vials`
        case "vials" : self = .vials

        /// Format `chess_knight` to `.chessKnight`
        case "chess_knight" : self = .chessKnight

        /// Format `crosshairs` to `.crosshairs`
        case "crosshairs" : self = .crosshairs

        /// Format `asterisk` to `.asterisk`
        case "asterisk" : self = .asterisk

        /// Format `alternate_external_link_square` to `.alternateExternalLinkSquare`
        case "alternate_external_link_square" : self = .alternateExternalLinkSquare

        /// Format `poll_h` to `.pollH`
        case "poll_h" : self = .pollH

        /// Format `copyright` to `.copyright`
        case "copyright" : self = .copyright

        /// Format `screwdriver` to `.screwdriver`
        case "screwdriver" : self = .screwdriver

        /// Format `ankh` to `.ankh`
        case "ankh" : self = .ankh

        /// Format `road` to `.road`
        case "road" : self = .road

        /// Format `compress` to `.compress`
        case "compress" : self = .compress

        /// Format `pen_square` to `.penSquare`
        case "pen_square" : self = .penSquare

        /// Format `unlink` to `.unlink`
        case "unlink" : self = .unlink

        /// Format `alternate_file` to `.alternateFile`
        case "alternate_file" : self = .alternateFile

        /// Format `wine_glass` to `.wineGlass`
        case "wine_glass" : self = .wineGlass

        /// Format `bell` to `.bell`
        case "bell" : self = .bell

        /// Format `file_invoice` to `.fileInvoice`
        case "file_invoice" : self = .fileInvoice

        /// Format `ship` to `.ship`
        case "ship" : self = .ship

        /// Format `industry` to `.industry`
        case "industry" : self = .industry

        /// Format `terminal` to `.terminal`
        case "terminal" : self = .terminal

        /// Format `tablet` to `.tablet`
        case "tablet" : self = .tablet

        /// Format `drumstick_with_bite_taken_out` to `.drumstickWithBiteTakenOut`
        case "drumstick_with_bite_taken_out" : self = .drumstickWithBiteTakenOut

        /// Format `receipt` to `.receipt`
        case "receipt" : self = .receipt

        /// Format `thermometer_full` to `.thermometerFull`
        case "thermometer_full" : self = .thermometerFull

        /// Format `atom` to `.atom`
        case "atom" : self = .atom

        /// Format `object_group` to `.objectGroup`
        case "object_group" : self = .objectGroup

        /// Format `money_check` to `.moneyCheck`
        case "money_check" : self = .moneyCheck

        /// Format `hot_dog` to `.hotDog`
        case "hot_dog" : self = .hotDog

        /// Format `swimmer` to `.swimmer`
        case "swimmer" : self = .swimmer

        /// Format `binoculars` to `.binoculars`
        case "binoculars" : self = .binoculars

        /// Format `arrow_circle_down` to `.arrowCircleDown`
        case "arrow_circle_down" : self = .arrowCircleDown

        /// Format `smiling_face_with_heart-eyes` to `.smilingFaceWithHeartEyes`
        case "smiling_face_with_heart-eyes" : self = .smilingFaceWithHeartEyes

        /// Format `not_equal` to `.notEqual`
        case "not_equal" : self = .notEqual

        /// Format `spider` to `.spider`
        case "spider" : self = .spider

        /// Format `plane_departure` to `.planeDeparture`
        case "plane_departure" : self = .planeDeparture

        /// Format `alternate_calendar` to `.alternateCalendar`
        case "alternate_calendar" : self = .alternateCalendar

        /// Format `grinning_face_with_smiling_eyes` to `.grinningFaceWithSmilingEyes`
        case "grinning_face_with_smiling_eyes" : self = .grinningFaceWithSmilingEyes

        /// Format `drum_steelpan` to `.drumSteelpan`
        case "drum_steelpan" : self = .drumSteelpan

        /// Format `vector_square` to `.vectorSquare`
        case "vector_square" : self = .vectorSquare

        /// Format `gopuram` to `.gopuram`
        case "gopuram" : self = .gopuram

        /// Format `swimming_pool` to `.swimmingPool`
        case "swimming_pool" : self = .swimmingPool

        /// Format `otter` to `.otter`
        case "otter" : self = .otter

        /// Format `tractor` to `.tractor`
        case "tractor" : self = .tractor

        /// Format `calendar_check` to `.calendarCheck`
        case "calendar_check" : self = .calendarCheck

        /// Format `car_side` to `.carSide`
        case "car_side" : self = .carSide

        /// Format `medical_file` to `.medicalFile`
        case "medical_file" : self = .medicalFile

        /// Format `arrow_circle_right` to `.arrowCircleRight`
        case "arrow_circle_right" : self = .arrowCircleRight

        /// Format `thermometer_empty` to `.thermometerEmpty`
        case "thermometer_empty" : self = .thermometerEmpty

        /// Format `vote_yea` to `.voteYea`
        case "vote_yea" : self = .voteYea

        /// Format `bell_slash` to `.bellSlash`
        case "bell_slash" : self = .bellSlash

        /// Format `list-ol` to `.listOl`
        case "list-ol" : self = .listOl

        /// Format `wheelchair` to `.wheelchair`
        case "wheelchair" : self = .wheelchair

        /// Format `menorah` to `.menorah`
        case "menorah" : self = .menorah

        /// Format `blog` to `.blog`
        case "blog" : self = .blog

        /// Format `puzzle_piece` to `.puzzlePiece`
        case "puzzle_piece" : self = .puzzlePiece

        /// Format `neutral_face` to `.neutralFace`
        case "neutral_face" : self = .neutralFace

        /// Format `magnet` to `.magnet`
        case "magnet" : self = .magnet

        /// Format `toilet` to `.toilet`
        case "toilet" : self = .toilet

        /// Format `dna` to `.dna`
        case "dna" : self = .dna

        /// Format `chevron-right` to `.chevronRight`
        case "chevron-right" : self = .chevronRight

        /// Format `info_circle` to `.infoCircle`
        case "info_circle" : self = .infoCircle

        /// Format `wifi` to `.wiFi`
        case "wifi" : self = .wiFi

        /// Format `sd_card` to `.sdCard`
        case "sd_card" : self = .sdCard

        /// Format `air_freshener` to `.airFreshener`
        case "air_freshener" : self = .airFreshener

        /// Format `server` to `.server`
        case "server" : self = .server

        /// Format `pallet` to `.pallet`
        case "pallet" : self = .pallet

        /// Format `plane` to `.plane`
        case "plane" : self = .plane

        /// Format `mars_stroke_vertical` to `.marsStrokeVertical`
        case "mars_stroke_vertical" : self = .marsStrokeVertical

        /// Format `alternate_money_bill` to `.alternateMoneyBill`
        case "alternate_money_bill" : self = .alternateMoneyBill

        /// Format `thumbtack` to `.thumbtack`
        case "thumbtack" : self = .thumbtack

        /// Format `hot_pepper` to `.hotPepper`
        case "hot_pepper" : self = .hotPepper

        /// Format `dice_three` to `.diceThree`
        case "dice_three" : self = .diceThree

        /// Format `superscript` to `.superscript`
        case "superscript" : self = .superscript

        /// Format `retro_camera` to `.retroCamera`
        case "retro_camera" : self = .retroCamera

        /// Format `snowplow` to `.snowplow`
        case "snowplow" : self = .snowplow

        /// Format `female` to `.female`
        case "female" : self = .female

        /// Format `sort_up_(ascending)` to `.sortUpAscending`
        case "sort_up_(ascending)" : self = .sortUpAscending

        /// Format `star-struck` to `.starStruck`
        case "star-struck" : self = .starStruck

        /// Format `alternate_map_marker` to `.alternateMapMarker`
        case "alternate_map_marker" : self = .alternateMapMarker

        /// Format `paw` to `.paw`
        case "paw" : self = .paw

        /// Format `newspaper` to `.newspaper`
        case "newspaper" : self = .newspaper

        /// Format `film` to `.film`
        case "film" : self = .film

        /// Format `alternate_exchange` to `.alternateExchange`
        case "alternate_exchange" : self = .alternateExchange

        /// Format `sticky_note` to `.stickyNote`
        case "sticky_note" : self = .stickyNote

        /// Format `signature` to `.signature`
        case "signature" : self = .signature

        /// Format `horizontal_ellipsis` to `.horizontalEllipsis`
        case "horizontal_ellipsis" : self = .horizontalEllipsis

        /// Format `alternate_sort_alphabetical_up` to `.alternateSortAlphabeticalUp`
        case "alternate_sort_alphabetical_up" : self = .alternateSortAlphabeticalUp

        /// Format `arrow-right` to `.arrowRight`
        case "arrow-right" : self = .arrowRight

        /// Format `angle_double_left` to `.angleDoubleLeft`
        case "angle_double_left" : self = .angleDoubleLeft

        /// Format `file_upload` to `.fileUpload`
        case "file_upload" : self = .fileUpload

        /// Format `user_minus` to `.userMinus`
        case "user_minus" : self = .userMinus

        /// Format `eye_dropper` to `.eyeDropper`
        case "eye_dropper" : self = .eyeDropper

        /// Format `icicles` to `.icicles`
        case "icicles" : self = .icicles

        /// Format `quidditch` to `.quidditch`
        case "quidditch" : self = .quidditch

        /// Format `wallet` to `.wallet`
        case "wallet" : self = .wallet

        /// Format `indian_rupee_sign` to `.indianRupeeSign`
        case "indian_rupee_sign" : self = .indianRupeeSign

        /// Format `user_check` to `.userCheck`
        case "user_check" : self = .userCheck

        /// Format `hdd` to `.hdd`
        case "hdd" : self = .hdd

        /// Format `reply` to `.reply`
        case "reply" : self = .reply

        /// Format `plug` to `.plug`
        case "plug" : self = .plug

        /// Format `mask` to `.mask`
        case "mask" : self = .mask

        /// Format `alternate_sort_alphabetical_down` to `.alternateSortAlphabeticalDown`
        case "alternate_sort_alphabetical_down" : self = .alternateSortAlphabeticalDown

        /// Format `om` to `.om`
        case "om" : self = .om

        /// Format `holly_berry` to `.hollyBerry`
        case "holly_berry" : self = .hollyBerry

        /// Format `exclamation_circle` to `.exclamationCircle`
        case "exclamation_circle" : self = .exclamationCircle

        /// Format `rock_(hand)` to `.rockHand`
        case "rock_(hand)" : self = .rockHand

        /// Format `torah` to `.torah`
        case "torah" : self = .torah

        /// Format `bed` to `.bed`
        case "bed" : self = .bed

        /// Format `bacon` to `.bacon`
        case "bacon" : self = .bacon

        /// Format `alternate_money_check` to `.alternateMoneyCheck`
        case "alternate_money_check" : self = .alternateMoneyCheck

        /// Format `shuttle_van` to `.shuttleVan`
        case "shuttle_van" : self = .shuttleVan

        /// Format `utensils` to `.utensils`
        case "utensils" : self = .utensils

        /// Format `torii_gate` to `.toriiGate`
        case "torii_gate" : self = .toriiGate

        /// Format `wine_bottle` to `.wineBottle`
        case "wine_bottle" : self = .wineBottle

        /// Format `shopping_bag` to `.shoppingBag`
        case "shopping_bag" : self = .shoppingBag

        /// Format `marker` to `.marker`
        case "marker" : self = .marker

        /// Format `door_closed` to `.doorClosed`
        case "door_closed" : self = .doorClosed

        /// Format `phone_square` to `.phoneSquare`
        case "phone_square" : self = .phoneSquare

        /// Format `eraser` to `.eraser`
        case "eraser" : self = .eraser

        /// Format `book_reader` to `.bookReader`
        case "book_reader" : self = .bookReader

        /// Format `envelope_square` to `.envelopeSquare`
        case "envelope_square" : self = .envelopeSquare

        /// Format `percent` to `.percent`
        case "percent" : self = .percent

        /// Format `wrench` to `.wrench`
        case "wrench" : self = .wrench

        /// Format `alternate_star_half` to `.alternateStarHalf`
        case "alternate_star_half" : self = .alternateStarHalf

        /// Format `calendar_times` to `.calendarTimes`
        case "calendar_times" : self = .calendarTimes

        /// Format `door_open` to `.doorOpen`
        case "door_open" : self = .doorOpen

        /// Format `alternate_user_slash` to `.alternateUserSlash`
        case "alternate_user_slash" : self = .alternateUserSlash

        /// Format `sort_numeric_up` to `.sortNumericUp`
        case "sort_numeric_up" : self = .sortNumericUp

        /// Format `paper_(hand)` to `.paperHand`
        case "paper_(hand)" : self = .paperHand

        /// Format `remove_user` to `.removeUser`
        case "remove_user" : self = .removeUser

        /// Format `band-aid` to `.bandAid`
        case "band-aid" : self = .bandAid

        /// Format `file_export` to `.fileExport`
        case "file_export" : self = .fileExport

        /// Format `laptop` to `.laptop`
        case "laptop" : self = .laptop

        /// Format `luggage_cart` to `.luggageCart`
        case "luggage_cart" : self = .luggageCart

        /// Format `search_plus` to `.searchPlus`
        case "search_plus" : self = .searchPlus

        /// Format `won_sign` to `.wonSign`
        case "won_sign" : self = .wonSign

        /// Format `fill_drip` to `.fillDrip`
        case "fill_drip" : self = .fillDrip

        /// Format `hourglass` to `.hourglass`
        case "hourglass" : self = .hourglass

        /// Format `forward` to `.forward`
        case "forward" : self = .forward

        /// Format `hashtag` to `.hashtag`
        case "hashtag" : self = .hashtag

        /// Format `microchip` to `.microchip`
        case "microchip" : self = .microchip

        /// Format `peace` to `.peace`
        case "peace" : self = .peace

        /// Format `fruit_apple` to `.fruitApple`
        case "fruit_apple" : self = .fruitApple

        /// Format `stethoscope` to `.stethoscope`
        case "stethoscope" : self = .stethoscope

        /// Format `infinity` to `.infinity`
        case "infinity" : self = .infinity

        /// Format `alternate_arrow_circle_down` to `.alternateArrowCircleDown`
        case "alternate_arrow_circle_down" : self = .alternateArrowCircleDown

        /// Format `check` to `.check`
        case "check" : self = .check

        /// Format `egg` to `.egg`
        case "egg" : self = .egg

        /// Format `leaf` to `.leaf`
        case "leaf" : self = .leaf

        /// Format `sun` to `.sun`
        case "sun" : self = .sun

        /// Format `heart` to `.heart`
        case "heart" : self = .heart

        /// Format `scroll` to `.scroll`
        case "scroll" : self = .scroll

        /// Format `alternate_user` to `.alternateUser`
        case "alternate_user" : self = .alternateUser

        /// Format `alternate_prescription_bottle` to `.alternatePrescriptionBottle`
        case "alternate_prescription_bottle" : self = .alternatePrescriptionBottle

        /// Format `filter` to `.filter`
        case "filter" : self = .filter

        /// Format `backspace` to `.backspace`
        case "backspace" : self = .backspace

        /// Format `box` to `.box`
        case "box" : self = .box

        /// Format `grip_lines` to `.gripLines`
        case "grip_lines" : self = .gripLines

        /// Format `clock` to `.clock`
        case "clock" : self = .clock

        /// Format `camera` to `.camera`
        case "camera" : self = .camera

        /// Format `cube` to `.cube`
        case "cube" : self = .cube

        /// Format `indent` to `.indent`
        case "indent" : self = .indent

        /// Format `teeth` to `.teeth`
        case "teeth" : self = .teeth

        /// Format `medical_book` to `.medicalBook`
        case "medical_book" : self = .medicalBook

        /// Format `chevron-down` to `.chevronDown`
        case "chevron-down" : self = .chevronDown

        /// Format `skating` to `.skating`
        case "skating" : self = .skating

        /// Format `music` to `.music`
        case "music" : self = .music

        /// Format `angle-down` to `.angleDown`
        case "angle-down" : self = .angleDown

        /// Format `ghost` to `.ghost`
        case "ghost" : self = .ghost

        /// Format `space_shuttle` to `.spaceShuttle`
        case "space_shuttle" : self = .spaceShuttle

        /// Format `play_circle` to `.playCircle`
        case "play_circle" : self = .playCircle

        /// Format `dollar_sign` to `.dollarSign`
        case "dollar_sign" : self = .dollarSign

        /// Format `biohazard` to `.biohazard`
        case "biohazard" : self = .biohazard

        /// Format `umbrella` to `.umbrella`
        case "umbrella" : self = .umbrella

        /// Format `tasks` to `.tasks`
        case "tasks" : self = .tasks

        /// Format `crow` to `.crow`
        case "crow" : self = .crow

        /// Format `cloud_with_(a_chance_of)_meatball` to `.cloudWithAChanceOfMeatball`
        case "cloud_with_(a_chance_of)_meatball" : self = .cloudWithAChanceOfMeatball

        /// Format `code` to `.code`
        case "code" : self = .code

        /// Format `globe_with_europe_shown` to `.globeWithEuropeShown`
        case "globe_with_europe_shown" : self = .globeWithEuropeShown

        /// Format `shapes` to `.shapes`
        case "shapes" : self = .shapes

        /// Format `squinting_face_with_tongue` to `.squintingFaceWithTongue`
        case "squinting_face_with_tongue" : self = .squintingFaceWithTongue

        /// Format `people_carry` to `.peopleCarry`
        case "people_carry" : self = .peopleCarry

        /// Format `code_branch` to `.codeBranch`
        case "code_branch" : self = .codeBranch

        /// Format `edit` to `.edit`
        case "edit" : self = .edit

        /// Format `sort_alphabetical_up` to `.sortAlphabeticalUp`
        case "sort_alphabetical_up" : self = .sortAlphabeticalUp

        /// Format `phone_slash` to `.phoneSlash`
        case "phone_slash" : self = .phoneSlash

        /// Format `sleigh` to `.sleigh`
        case "sleigh" : self = .sleigh

        /// Format `mountain` to `.mountain`
        case "mountain" : self = .mountain

        /// Format `thumbs-up` to `.thumbsUp`
        case "thumbs-up" : self = .thumbsUp

        /// Format `user_secret` to `.userSecret`
        case "user_secret" : self = .userSecret

        /// Format `highlighter` to `.highlighter`
        case "highlighter" : self = .highlighter

        /// Format `volume_mute` to `.volumeMute`
        case "volume_mute" : self = .volumeMute

        /// Format `caret_square_up` to `.caretSquareUp`
        case "caret_square_up" : self = .caretSquareUp

        /// Format `water` to `.water`
        case "water" : self = .water

        /// Format `yen_sign` to `.yenSign`
        case "yen_sign" : self = .yenSign

        /// Format `rainbow` to `.rainbow`
        case "rainbow" : self = .rainbow

        /// Format `feather` to `.feather`
        case "feather" : self = .feather

        /// Format `alternate_sign_out` to `.alternateSignOut`
        case "alternate_sign_out" : self = .alternateSignOut

        /// Format `window_minimize` to `.windowMinimize`
        case "window_minimize" : self = .windowMinimize

        /// Format `parachute_box` to `.parachuteBox`
        case "parachute_box" : self = .parachuteBox

        /// Format `alternate_identification_card` to `.alternateIdentificationCard`
        case "alternate_identification_card" : self = .alternateIdentificationCard

        /// Format `beaming_face_with_smiling_eyes` to `.beamingFaceWithSmilingEyes`
        case "beaming_face_with_smiling_eyes" : self = .beamingFaceWithSmilingEyes

        /// Format `sign` to `.sign`
        case "sign" : self = .sign

        /// Format `alternate_cloud_download` to `.alternateCloudDownload`
        case "alternate_cloud_download" : self = .alternateCloudDownload

        /// Format `place_of_worship` to `.placeOfWorship`
        case "place_of_worship" : self = .placeOfWorship

        /// Format `square` to `.square`
        case "square" : self = .square

        /// Format `alternate_compress_arrows` to `.alternateCompressArrows`
        case "alternate_compress_arrows" : self = .alternateCompressArrows

        /// Format `calendar_with_week_focus` to `.calendarWithWeekFocus`
        case "calendar_with_week_focus" : self = .calendarWithWeekFocus

        /// Format `journal_of_the_whills` to `.journalOfTheWhills`
        case "journal_of_the_whills" : self = .journalOfTheWhills

        /// Format `star-half` to `.starHalf`
        case "star-half" : self = .starHalf

        /// Format `images` to `.images`
        case "images" : self = .images

        /// Format `cog` to `.cog`
        case "cog" : self = .cog

        /// Format `align-justify` to `.alignJustify`
        case "align-justify" : self = .alignJustify

        /// Format `crop` to `.crop`
        case "crop" : self = .crop

        /// Format `globe_with_africa_shown` to `.globeWithAfricaShown`
        case "globe_with_africa_shown" : self = .globeWithAfricaShown

        /// Format `dolly_flatbed` to `.dollyFlatbed`
        case "dolly_flatbed" : self = .dollyFlatbed

        /// Format `angle_double_down` to `.angleDoubleDown`
        case "angle_double_down" : self = .angleDoubleDown

        /// Format `sync` to `.sync`
        case "sync" : self = .sync

        /// Format `phone` to `.phone`
        case "phone" : self = .phone

        /// Format `address_book` to `.addressBook`
        case "address_book" : self = .addressBook

        /// Format `stamp` to `.stamp`
        case "stamp" : self = .stamp

        /// Format `double_check` to `.doubleCheck`
        case "double_check" : self = .doubleCheck

        /// Format `monument` to `.monument`
        case "monument" : self = .monument

        /// Format `mouse_pointer` to `.mousePointer`
        case "mouse_pointer" : self = .mousePointer

        /// Format `universal_access` to `.universalAccess`
        case "universal_access" : self = .universalAccess

        /// Format `user_lock` to `.userLock`
        case "user_lock" : self = .userLock

        /// Format `pen_fancy` to `.penFancy`
        case "pen_fancy" : self = .penFancy

        /// Format `line_chart` to `.lineChart`
        case "line_chart" : self = .lineChart

        /// Format `taxi` to `.taxi`
        case "taxi" : self = .taxi

        /// Format `calendar_with_day_focus` to `.calendarWithDayFocus`
        case "calendar_with_day_focus" : self = .calendarWithDayFocus

        /// Format `medical_briefcase` to `.medicalBriefcase`
        case "medical_briefcase" : self = .medicalBriefcase

        /// Format `align-left` to `.alignLeft`
        case "align-left" : self = .alignLeft

        /// Format `trash_restore` to `.trashRestore`
        case "trash_restore" : self = .trashRestore

        /// Format `power_off` to `.powerOff`
        case "power_off" : self = .powerOff

        /// Format `alternate_arrows` to `.alternateArrows`
        case "alternate_arrows" : self = .alternateArrows

        /// Format `clipboard_list` to `.clipboardList`
        case "clipboard_list" : self = .clipboardList

        /// Format `quote-left` to `.quoteLeft`
        case "quote-left" : self = .quoteLeft

        /// Format `poo_storm` to `.pooStorm`
        case "poo_storm" : self = .pooStorm

        /// Format `angle-up` to `.angleUp`
        case "angle-up" : self = .angleUp

        /// Format `alternate_fire` to `.alternateFire`
        case "alternate_fire" : self = .alternateFire

        /// Format `adjust` to `.adjust`
        case "adjust" : self = .adjust

        /// Format `comments` to `.comments`
        case "comments" : self = .comments

        /// Format `donate` to `.donate`
        case "donate" : self = .donate

        /// Format `male` to `.male`
        case "male" : self = .male

        /// Format `italic` to `.italic`
        case "italic" : self = .italic

        /// Format `file_invoice_with_us_dollar` to `.fileInvoiceWithUSDollar`
        case "file_invoice_with_us_dollar" : self = .fileInvoiceWithUSDollar

        /// Format `minus_circle` to `.minusCircle`
        case "minus_circle" : self = .minusCircle

        /// Format `project_diagram` to `.projectDiagram`
        case "project_diagram" : self = .projectDiagram

        /// Format `smog` to `.smog`
        case "smog" : self = .smog

        /// Format `sms` to `.sms`
        case "sms" : self = .sms

        /// Format `mars_stroke` to `.marsStroke`
        case "mars_stroke" : self = .marsStroke

        /// Format `share` to `.share`
        case "share" : self = .share

        /// Format `closed_captioning` to `.closedCaptioning`
        case "closed_captioning" : self = .closedCaptioning

        /// Format `skull_&_crossbones` to `.skullCrossbones`
        case "skull_&_crossbones" : self = .skullCrossbones

        /// Format `subway` to `.subway`
        case "subway" : self = .subway

        /// Format `comment_dots` to `.commentDots`
        case "comment_dots" : self = .commentDots

        /// Format `list-ul` to `.listUl`
        case "list-ul" : self = .listUl

        /// Format `horse_head` to `.horseHead`
        case "horse_head" : self = .horseHead

        /// Format `face_with_rolling_eyes` to `.faceWithRollingEyes`
        case "face_with_rolling_eyes" : self = .faceWithRollingEyes

        /// Format `square_wave` to `.squareWave`
        case "square_wave" : self = .squareWave

        /// Format `hammer` to `.hammer`
        case "hammer" : self = .hammer

        /// Format `burn` to `.burn`
        case "burn" : self = .burn

        /// Format `headphones` to `.headphones`
        case "headphones" : self = .headphones

        /// Format `skiing` to `.skiing`
        case "skiing" : self = .skiing

        /// Format `caret_square_left` to `.caretSquareLeft`
        case "caret_square_left" : self = .caretSquareLeft

        /// Format `bar_chart` to `.barChart`
        case "bar_chart" : self = .barChart

        /// Format `loudly_crying_face` to `.loudlyCryingFace`
        case "loudly_crying_face" : self = .loudlyCryingFace

        /// Format `tint_slash` to `.tintSlash`
        case "tint_slash" : self = .tintSlash

        /// Format `th-large` to `.thLarge`
        case "th-large" : self = .thLarge

        /// Format `caret_down` to `.caretDown`
        case "caret_down" : self = .caretDown

        /// Format `question_circle` to `.questionCircle`
        case "question_circle" : self = .questionCircle

        /// Format `procedures` to `.procedures`
        case "procedures" : self = .procedures

        /// Format `battery_full` to `.batteryFull`
        case "battery_full" : self = .batteryFull

        /// Format `alternate_list` to `.alternateList`
        case "alternate_list" : self = .alternateList

        /// Format `window_close` to `.windowClose`
        case "window_close" : self = .windowClose

        /// Format `circle` to `.circle`
        case "circle" : self = .circle

        /// Format `medical_notes` to `.medicalNotes`
        case "medical_notes" : self = .medicalNotes

        /// Format `border_all` to `.borderAll`
        case "border_all" : self = .borderAll

        /// Format `cookie_bite` to `.cookieBite`
        case "cookie_bite" : self = .cookieBite

        /// Format `cloud` to `.cloud`
        case "cloud" : self = .cloud

        /// Format `grinning_winking_face` to `.grinningWinkingFace`
        case "grinning_winking_face" : self = .grinningWinkingFace

        /// Format `pen` to `.pen`
        case "pen" : self = .pen

        /// Format `cheese` to `.cheese`
        case "cheese" : self = .cheese

        /// Format `code_file` to `.codeFile`
        case "code_file" : self = .codeFile

        /// Format `user_shield` to `.userShield`
        case "user_shield" : self = .userShield

        /// Format `nurse` to `.nurse`
        case "nurse" : self = .nurse

        /// Format `cloud_with_sun_and_rain` to `.cloudWithSunAndRain`
        case "cloud_with_sun_and_rain" : self = .cloudWithSunAndRain

        /// Format `star` to `.star`
        case "star" : self = .star

        /// Format `stop` to `.stop`
        case "stop" : self = .stop

        /// Format `user` to `.user`
        case "user" : self = .user

        /// Format `bomb` to `.bomb`
        case "bomb" : self = .bomb

        /// Format `raised_fist` to `.raisedFist`
        case "raised_fist" : self = .raisedFist

        /// Format `user_astronaut` to `.userAstronaut`
        case "user_astronaut" : self = .userAstronaut

        /// Format `poop` to `.poop`
        case "poop" : self = .poop

        /// Format `alternate_sort_numeric_down` to `.alternateSortNumericDown`
        case "alternate_sort_numeric_down" : self = .alternateSortNumericDown

        /// Format `archway` to `.archway`
        case "archway" : self = .archway

        /// Format `shopping_cart_arrow_down` to `.shoppingCartArrowDown`
        case "shopping_cart_arrow_down" : self = .shoppingCartArrowDown

        /// Format `draw_polygon` to `.drawPolygon`
        case "draw_polygon" : self = .drawPolygon

        /// Format `file_import` to `.fileImport`
        case "file_import" : self = .fileImport

        /// Format `file_download` to `.fileDownload`
        case "file_download" : self = .fileDownload

        /// Format `minus` to `.minus`
        case "minus" : self = .minus

        /// Format `question` to `.question`
        case "question" : self = .question

        /// Format `tooth` to `.tooth`
        case "tooth" : self = .tooth

        /// Format `turkish_lira_sign` to `.turkishLiraSign`
        case "turkish_lira_sign" : self = .turkishLiraSign

        /// Format `archive_file` to `.archiveFile`
        case "archive_file" : self = .archiveFile

        /// Format `socks` to `.socks`
        case "socks" : self = .socks

        /// Format `eye_slash` to `.eyeSlash`
        case "eye_slash" : self = .eyeSlash

        /// Format `angle-right` to `.angleRight`
        case "angle-right" : self = .angleRight

        /// Format `chevron_circle_right` to `.chevronCircleRight`
        case "chevron_circle_right" : self = .chevronCircleRight

        /// Format `kissing_face` to `.kissingFace`
        case "kissing_face" : self = .kissingFace

        /// Format `star_of_david` to `.starOfDavid`
        case "star_of_david" : self = .starOfDavid

        /// Format `barcode` to `.barcode`
        case "barcode" : self = .barcode

        /// Format `plus_circle` to `.plusCircle`
        case "plus_circle" : self = .plusCircle

        /// Format `users` to `.users`
        case "users" : self = .users

        /// Format `biking` to `.biking`
        case "biking" : self = .biking

        /// Format `laugh_face_with_beaming_eyes` to `.laughFaceWithBeamingEyes`
        case "laugh_face_with_beaming_eyes" : self = .laughFaceWithBeamingEyes

        /// Format `paste` to `.paste`
        case "paste" : self = .paste

        /// Format `building` to `.building`
        case "building" : self = .building

        /// Format `download` to `.download`
        case "download" : self = .download

        /// Format `layer_group` to `.layerGroup`
        case "layer_group" : self = .layerGroup

        /// Format `coffee` to `.coffee`
        case "coffee" : self = .coffee

        /// Format `folder_plus` to `.folderPlus`
        case "folder_plus" : self = .folderPlus

        /// Format `united_states_of_america_flag` to `.unitedStatesOfAmericaFlag`
        case "united_states_of_america_flag" : self = .unitedStatesOfAmericaFlag

        /// Format `horse` to `.horse`
        case "horse" : self = .horse

        /// Format `blind` to `.blind`
        case "blind" : self = .blind

        /// Format `walking` to `.walking`
        case "walking" : self = .walking

        /// Format `alternate_microphone` to `.alternateMicrophone`
        case "alternate_microphone" : self = .alternateMicrophone

        /// Format `city` to `.city`
        case "city" : self = .city

        /// Format `prescription` to `.prescription`
        case "prescription" : self = .prescription

        /// Format `spell_check` to `.spellCheck`
        case "spell_check" : self = .spellCheck

        /// Format `alternate_glass_martini` to `.alternateGlassMartini`
        case "alternate_glass_martini" : self = .alternateGlassMartini

        /// Format `truck` to `.truck`
        case "truck" : self = .truck

        default: return nil
        }
    }
}

/// Extension `FontAwesomeSolid` for `FontAwesome`
extension FontAwesomeSolid: FontFamily {
    public static var fontConvertible: FontConvertible = FontConvertible(name: "FontAwesome5Free-Solid", family: "Font Awesome 5 Free-Solid-900", fileName: "fa-solid-900", bundle: .module, ratio: 1.28571429)
}
